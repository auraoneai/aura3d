import type { ProductionAppUiDefinition } from "../../common/src/runtime";

export const ui: ProductionAppUiDefinition = {
  primaryActionLabel: "Toggle Budget",
  secondaryLabel: "Resource-budget workflow with real imported assets and renderer diagnostics."
};
