# A3D Production Character Viewer

Production skinned-character startup sanity page. It loads a single imported GLB, reports each startup phase, renders the first WebGL2 frame through `renderFrame`, and only then starts the animation loop. This page is not a readPixels/proof screenshot path.
