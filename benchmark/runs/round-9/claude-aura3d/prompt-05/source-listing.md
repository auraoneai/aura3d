# Generated Source Listing

This file is generated by the Round 9 capture runner for neutral scoring.

## index.html

```
<!doctype html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Aura3D Round 9 Benchmark</title>
  </head>
  <body>
    <div id="app"></div>
    <script type="module" src="/src/main.ts"></script>
  </body>
</html>

```

## src/main.ts

```
import {
  camera,
  createAuraApp,
  interactions,
  lights,
  prefabs,
  scene,
  ui
} from "@aura3d/engine";

// Readable axis labels drawn as a DOM overlay around the single Aura app.
// The dataBars3D prefab already renders the 6x6 bar field, color-by-height,
// pulse height animation, axis rails, and per-bar hover-highlight; the prompt
// additionally requires visible, readable axis labels, so we add them here.
ui.html(
  "#app",
  `
  <div style="position:absolute;left:18px;top:18px;z-index:20;padding:10px 12px;border-radius:10px;background:rgba(7,16,23,.74);color:#e6f6ff;font:600 13px system-ui;letter-spacing:.02em">
    <div style="font-size:15px;font-weight:800;margin-bottom:4px">3D Data Visualization</div>
    <div style="opacity:.85">6&times;6 grid &middot; 36 bars &middot; color = height</div>
    <div style="opacity:.85;margin-top:2px">hover a bar to highlight it</div>
  </div>
  <div style="position:absolute;left:50%;bottom:20px;transform:translateX(-50%);z-index:20;padding:6px 12px;border-radius:8px;background:rgba(217,248,255,.16);color:#d9f8ff;font:800 14px system-ui">
    X axis &rarr; columns
  </div>
  <div style="position:absolute;left:18px;top:50%;transform:translateY(-50%);z-index:20;padding:6px 12px;border-radius:8px;background:rgba(255,209,102,.18);color:#ffd166;font:800 14px system-ui">
    Z axis &uarr; rows
  </div>
  <div style="position:absolute;right:18px;top:50%;transform:translateY(-50%);z-index:20;padding:6px 12px;border-radius:8px;background:rgba(244,251,255,.16);color:#f4fbff;font:800 14px system-ui">
    Y axis &uarr; value (height)
  </div>
`
);

createAuraApp("#app", {
  scene: scene()
    .background("#071017")
    .addMany(prefabs.dataBars3D({ grid: 6 }))
    .add(lights.studio({ intensity: 1.1 }))
    .add(interactions.orbit())
    .camera(camera.orbit({ distance: 5.4, target: [0, 0.9, 0] }))
});

```
