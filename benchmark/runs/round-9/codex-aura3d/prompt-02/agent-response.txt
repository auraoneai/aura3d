Implemented the Particle Fountain app in `source/src`.

Build command:
```bash
cd source && npm run build
```

Run command for runner:
```bash
cd source && npm run dev
```

Build result: passed.

Assumptions:
- The visible emission-rate slider controls particle count as the rate proxy.
- No assets are used; the scene is fully procedural.
- I did not run dev/preview or visual/browser verification, per benchmark rules.