Implemented the mini-golf browser 3D app in `source/src/main.ts` using `prefabs.miniGolfHole()`, pointer interaction, a follow camera targeting the white physics golf ball, studio lighting, and a visible strokes/aim HUD.

Build completed successfully.

Build command:
```bash
cd source && npm run build
```

Run command for the runner:
```bash
cd source && npm run dev
```

Assumptions: the Aura3D mini-golf prefab provides the procedural green, obstacle, ball physics, and click/drag shot behavior required by the prompt. No assets were used.