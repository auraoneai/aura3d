# Generated Source Listing

This file is generated by the Round 9 capture runner for neutral scoring.

## index.html

```
<!doctype html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Sneaker Product Viewer</title>
    <style>
      html, body { margin: 0; height: 100%; overflow: hidden; background: #1a1c20; }
      #app, canvas { display: block; width: 100%; height: 100%; }
    </style>
  </head>
  <body>
    <div id="app"></div>
    <script type="module" src="/src/main.ts"></script>
  </body>
</html>

```

## src/main.ts

```
// Prompt 10: Product Viewer With Provided Sneaker
// Centered, auto-scaled sneaker on a plinth, studio lighting, orbit controls,
// and a rotating turntable. Uses only `three` and bundled `three/addons/*`.

import * as THREE from 'three';
import { OrbitControls } from 'three/addons/controls/OrbitControls.js';
import { GLTFLoader } from 'three/addons/loaders/GLTFLoader.js';
import { RoomEnvironment } from 'three/addons/environments/RoomEnvironment.js';

// The provided asset lives in public/benchmark/assets and is served from the
// site root by Vite, so the public URL is `/benchmark/assets/sneaker.glb`.
const SNEAKER_URL = '/benchmark/assets/sneaker.glb';

// ---------------------------------------------------------------------------
// Renderer
// ---------------------------------------------------------------------------

const container = document.getElementById('app') ?? document.body;

const renderer = new THREE.WebGLRenderer({ antialias: true });
renderer.setPixelRatio(Math.min(window.devicePixelRatio, 2));
renderer.setSize(window.innerWidth, window.innerHeight);
renderer.outputColorSpace = THREE.SRGBColorSpace;
renderer.toneMapping = THREE.ACESFilmicToneMapping;
renderer.toneMappingExposure = 1.0;
renderer.shadowMap.enabled = true;
renderer.shadowMap.type = THREE.PCFSoftShadowMap;
container.appendChild(renderer.domElement);

// ---------------------------------------------------------------------------
// Scene & camera
// ---------------------------------------------------------------------------

const scene = new THREE.Scene();
scene.background = new THREE.Color(0x1a1c20);

const camera = new THREE.PerspectiveCamera(
  45,
  window.innerWidth / window.innerHeight,
  0.1,
  100,
);
camera.position.set(2.6, 1.9, 3.4);

// Studio image-based lighting so PBR materials read correctly.
const pmrem = new THREE.PMREMGenerator(renderer);
const roomEnv = new RoomEnvironment();
scene.environment = pmrem.fromScene(roomEnv, 0.04).texture;
roomEnv.dispose?.();

// ---------------------------------------------------------------------------
// Studio lighting
// ---------------------------------------------------------------------------

scene.add(new THREE.AmbientLight(0xffffff, 0.35));

// Key light (soft shadow caster).
const keyLight = new THREE.DirectionalLight(0xffffff, 2.4);
keyLight.position.set(4, 6, 4);
keyLight.castShadow = true;
keyLight.shadow.mapSize.set(2048, 2048);
keyLight.shadow.camera.near = 0.5;
keyLight.shadow.camera.far = 25;
keyLight.shadow.camera.left = -5;
keyLight.shadow.camera.right = 5;
keyLight.shadow.camera.top = 5;
keyLight.shadow.camera.bottom = -5;
keyLight.shadow.bias = -0.0004;
scene.add(keyLight);

// Fill light to lift the shadow side.
const fillLight = new THREE.DirectionalLight(0xbfd4ff, 0.8);
fillLight.position.set(-5, 3, 2);
scene.add(fillLight);

// Rim / back light to separate product from background.
const rimLight = new THREE.SpotLight(0xffffff, 60, 20, Math.PI / 6, 0.4, 1.5);
rimLight.position.set(-3, 5, -5);
scene.add(rimLight);

// ---------------------------------------------------------------------------
// Plinth (product base)
// ---------------------------------------------------------------------------

const PLINTH_RADIUS = 1.4;
const PLINTH_HEIGHT = 0.35;

const plinth = new THREE.Mesh(
  new THREE.CylinderGeometry(PLINTH_RADIUS, PLINTH_RADIUS * 1.05, PLINTH_HEIGHT, 64),
  new THREE.MeshStandardMaterial({ color: 0x2a2d33, roughness: 0.6, metalness: 0.1 }),
);
plinth.position.y = -PLINTH_HEIGHT / 2;
plinth.receiveShadow = true;
plinth.castShadow = true;
scene.add(plinth);

// Ground plane catches the soft contact shadow.
const ground = new THREE.Mesh(
  new THREE.PlaneGeometry(50, 50),
  new THREE.MeshStandardMaterial({ color: 0x131417, roughness: 1 }),
);
ground.rotation.x = -Math.PI / 2;
ground.position.y = -PLINTH_HEIGHT;
ground.receiveShadow = true;
scene.add(ground);

// Turntable: the model is parented here and the whole group spins.
const turntable = new THREE.Group();
scene.add(turntable);

// ---------------------------------------------------------------------------
// Orbit controls
// ---------------------------------------------------------------------------

const controls = new OrbitControls(camera, renderer.domElement);
controls.enableDamping = true;
controls.dampingFactor = 0.05;
controls.minDistance = 2;
controls.maxDistance = 12;
controls.maxPolarAngle = Math.PI / 2 + 0.05;
controls.target.set(0, 0.7, 0);
controls.update();

// ---------------------------------------------------------------------------
// Load, center and auto-scale the sneaker
// ---------------------------------------------------------------------------

const TARGET_SIZE = 1.6; // desired largest dimension in world units

const loader = new GLTFLoader();
loader.load(
  SNEAKER_URL,
  (gltf) => {
    const model = gltf.scene;

    // Center the model on the origin.
    const box = new THREE.Box3().setFromObject(model);
    const size = new THREE.Vector3();
    const center = new THREE.Vector3();
    box.getSize(size);
    box.getCenter(center);
    model.position.sub(center);

    // Auto-scale so the largest dimension matches TARGET_SIZE.
    const maxDim = Math.max(size.x, size.y, size.z) || 1;
    const scale = TARGET_SIZE / maxDim;
    model.scale.setScalar(scale);

    // Wrap in a recenter group, then drop it so its base sits on the plinth top.
    const wrapper = new THREE.Group();
    wrapper.add(model);

    const scaledBox = new THREE.Box3().setFromObject(wrapper);
    wrapper.position.y = -scaledBox.min.y; // base rests on plinth top (y=0)

    model.traverse((child) => {
      if ((child as THREE.Mesh).isMesh) {
        child.castShadow = true;
        child.receiveShadow = true;
      }
    });

    turntable.add(wrapper);

    // Re-aim controls at the product's vertical midpoint.
    const finalBox = new THREE.Box3().setFromObject(wrapper);
    const finalCenter = new THREE.Vector3();
    finalBox.getCenter(finalCenter);
    controls.target.copy(finalCenter);
    controls.update();
  },
  undefined,
  (error) => {
    console.error('Failed to load sneaker.glb', error);
  },
);

// ---------------------------------------------------------------------------
// Resize & render loop
// ---------------------------------------------------------------------------

window.addEventListener('resize', () => {
  camera.aspect = window.innerWidth / window.innerHeight;
  camera.updateProjectionMatrix();
  renderer.setSize(window.innerWidth, window.innerHeight);
});

const TURNTABLE_SPEED = 0.4; // radians per second
const clock = new THREE.Clock();

renderer.setAnimationLoop(() => {
  const delta = clock.getDelta();
  turntable.rotation.y += TURNTABLE_SPEED * delta;
  controls.update();
  renderer.render(scene, camera);
});

```
