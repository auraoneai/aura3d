The build is complete and verified. Stopping here per the benchmark rules.

## Summary

Implemented a Three.js product viewer in `source/src/main.ts` using only `three` 0.165.0 and bundled `three/addons/*` modules. It satisfies every required evidence item:

- **Loaded & visible** — `GLTFLoader` loads the provided sneaker from `/benchmark/assets/sneaker.glb` (the served location of the only allowed asset, `benchmark/assets/sneaker.glb`; no invented paths).
- **Centered & auto-scaled** — `Box3` re-centers the model on the origin and scales its largest dimension to a fixed target size, then rests its base on the plinth top.
- **Plinth/base** — a `CylinderGeometry` plinth plus a shadow-catching ground plane.
- **Studio lighting** — `RoomEnvironment` IBL via `PMREMGenerator` for PBR readability, plus key/fill/rim lights, ACES tone mapping, and soft shadows.
- **Orbit controls + turntable** — `OrbitControls` (damped) and a `Group` that auto-rotates around Y in the `setAnimationLoop` using delta time.

## Commands for the runner

- **Build command:** `npm run build`
- **Run command:** `npm run dev -- --port <assigned-port>`

(`npm install` was already run; build succeeded — typecheck clean, asset copied to `dist/benchmark/assets/sneaker.glb`.)

## Assumptions

- Despite the project `CLAUDE.md` referencing Aura3D, this prompt's `context/llms.txt` defines a **raw Three.js** benchmark, so I used `three` + official addons only (no Aura3D APIs).
- The asset, placed in `source/public/benchmark/assets/sneaker.glb` by the scaffold, is served by Vite at the root path `/benchmark/assets/sneaker.glb` — used directly rather than via `import.meta.env` to keep the strict tsconfig clean without adding Vite client types.
- No `context/docs/agents/benchmark-recipes.md` exists in the bundle, so no recipe shape was copied.
- The ~575 kB bundle / chunk-size warning is benign (Three.js core) and does not affect the build.
