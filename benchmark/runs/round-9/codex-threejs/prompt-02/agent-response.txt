Implemented the procedural Three.js particle fountain in `source/src/main.ts`.

Build completed successfully.

Build command:
```bash
npm run build
```

Run command for the benchmark runner:
```bash
npm run dev
```

Assumptions:
- No external assets are used.
- The runner will perform visual verification later, since the prompt forbids starting dev/preview servers or screenshots inside the agent process.