# Generated Source Listing

This file is generated by the Round 9 capture runner for neutral scoring.

## index.html

```
<!doctype html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Aura3D Round 9 Benchmark</title>
  </head>
  <body>
    <div id="app"></div>
    <script type="module" src="/src/main.ts"></script>
  </body>
</html>

```

## src/main.ts

```
import * as THREE from 'three';

const app = document.querySelector<HTMLDivElement>('#app');

if (!app) {
  throw new Error('Missing #app container');
}

document.body.style.margin = '0';
document.body.style.overflow = 'hidden';
document.body.style.background = '#b9d7ee';
app.style.width = '100vw';
app.style.height = '100vh';

const scene = new THREE.Scene();
scene.background = new THREE.Color(0xb9d7ee);
scene.fog = new THREE.Fog(0xb9d7ee, 18, 42);

const camera = new THREE.PerspectiveCamera(
  45,
  window.innerWidth / window.innerHeight,
  0.1,
  100,
);
camera.position.set(6.5, 4.2, 9);
camera.lookAt(0, 1.15, 0);

const renderer = new THREE.WebGLRenderer({ antialias: true });
renderer.setPixelRatio(Math.min(window.devicePixelRatio, 2));
renderer.setSize(window.innerWidth, window.innerHeight);
renderer.shadowMap.enabled = true;
renderer.shadowMap.type = THREE.PCFSoftShadowMap;
renderer.outputColorSpace = THREE.SRGBColorSpace;
app.appendChild(renderer.domElement);

const hemiLight = new THREE.HemisphereLight(0xf7fbff, 0x667066, 2.4);
scene.add(hemiLight);

const keyLight = new THREE.DirectionalLight(0xffffff, 3.2);
keyLight.position.set(5, 8, 4);
keyLight.castShadow = true;
keyLight.shadow.mapSize.set(2048, 2048);
keyLight.shadow.camera.near = 1;
keyLight.shadow.camera.far = 30;
keyLight.shadow.camera.left = -12;
keyLight.shadow.camera.right = 12;
keyLight.shadow.camera.top = 12;
keyLight.shadow.camera.bottom = -12;
scene.add(keyLight);

const groundMaterial = new THREE.MeshStandardMaterial({
  color: 0x6f945c,
  roughness: 0.84,
  metalness: 0.02,
});
const ground = new THREE.Mesh(new THREE.PlaneGeometry(28, 18), groundMaterial);
ground.rotation.x = -Math.PI / 2;
ground.receiveShadow = true;
scene.add(ground);

const grid = new THREE.GridHelper(28, 28, 0x36523a, 0x7fa870);
grid.position.y = 0.012;
scene.add(grid);

const trackMaterial = new THREE.MeshBasicMaterial({
  color: 0xffffff,
  transparent: true,
  opacity: 0.32,
});
const track = new THREE.Mesh(new THREE.PlaneGeometry(10.8, 0.07), trackMaterial);
track.rotation.x = -Math.PI / 2;
track.position.set(0, 0.018, 0);
scene.add(track);

const skin = new THREE.MeshStandardMaterial({ color: 0xffc58b, roughness: 0.55 });
const shirt = new THREE.MeshStandardMaterial({ color: 0x2457b8, roughness: 0.68 });
const limb = new THREE.MeshStandardMaterial({ color: 0x233047, roughness: 0.62 });
const shoeMat = new THREE.MeshStandardMaterial({ color: 0x1b1f26, roughness: 0.72 });

function mesh<T extends THREE.BufferGeometry>(
  geometry: T,
  material: THREE.Material,
  parent: THREE.Object3D,
): THREE.Mesh<T> {
  const item = new THREE.Mesh(geometry, material);
  item.castShadow = true;
  item.receiveShadow = true;
  parent.add(item);
  return item;
}

const walker = new THREE.Group();
walker.position.set(-3.7, 0, 0);
scene.add(walker);

const body = new THREE.Group();
body.position.y = 1.58;
walker.add(body);

const torso = mesh(new THREE.CylinderGeometry(0.42, 0.5, 1.18, 24), shirt, body);
torso.position.y = 0.18;

const neck = mesh(new THREE.CylinderGeometry(0.12, 0.14, 0.18, 18), skin, body);
neck.position.y = 0.86;

const head = mesh(new THREE.SphereGeometry(0.34, 32, 20), skin, body);
head.position.y = 1.22;

const nose = mesh(new THREE.BoxGeometry(0.08, 0.06, 0.12), skin, body);
nose.position.set(0, 1.22, 0.34);

const leftEye = mesh(new THREE.SphereGeometry(0.035, 12, 8), shoeMat, body);
leftEye.position.set(-0.11, 1.3, 0.315);
const rightEye = mesh(new THREE.SphereGeometry(0.035, 12, 8), shoeMat, body);
rightEye.position.set(0.11, 1.3, 0.315);

type Limb = {
  pivot: THREE.Group;
  lowerPivot?: THREE.Group;
  foot?: THREE.Mesh;
};

function createArm(side: -1 | 1): Limb {
  const shoulder = new THREE.Group();
  shoulder.position.set(side * 0.52, 0.65, 0);
  body.add(shoulder);

  const upper = mesh(new THREE.BoxGeometry(0.18, 0.72, 0.18), limb, shoulder);
  upper.position.y = -0.36;

  const forearmPivot = new THREE.Group();
  forearmPivot.position.y = -0.72;
  shoulder.add(forearmPivot);

  const forearm = mesh(new THREE.BoxGeometry(0.16, 0.62, 0.16), skin, forearmPivot);
  forearm.position.y = -0.31;

  const hand = mesh(new THREE.SphereGeometry(0.105, 16, 12), skin, forearmPivot);
  hand.position.y = -0.66;

  return { pivot: shoulder, lowerPivot: forearmPivot };
}

function createLeg(side: -1 | 1): Limb {
  const hip = new THREE.Group();
  hip.position.set(side * 0.24, -0.42, 0);
  body.add(hip);

  const thigh = mesh(new THREE.BoxGeometry(0.22, 0.72, 0.22), limb, hip);
  thigh.position.y = -0.36;

  const knee = new THREE.Group();
  knee.position.y = -0.72;
  hip.add(knee);

  const shin = mesh(new THREE.BoxGeometry(0.2, 0.7, 0.2), limb, knee);
  shin.position.y = -0.35;

  const foot = mesh(new THREE.BoxGeometry(0.24, 0.14, 0.46), shoeMat, knee);
  foot.position.set(0, -0.75, 0.12);

  return { pivot: hip, lowerPivot: knee, foot };
}

const leftArm = createArm(-1);
const rightArm = createArm(1);
const leftLeg = createLeg(-1);
const rightLeg = createLeg(1);

const shadowBlob = new THREE.Mesh(
  new THREE.CircleGeometry(0.9, 48),
  new THREE.MeshBasicMaterial({ color: 0x1b2719, transparent: true, opacity: 0.16 }),
);
shadowBlob.rotation.x = -Math.PI / 2;
shadowBlob.position.y = 0.025;
walker.add(shadowBlob);

for (let i = 0; i < 9; i += 1) {
  const marker = new THREE.Mesh(
    new THREE.BoxGeometry(0.16, 0.025, 0.42),
    new THREE.MeshBasicMaterial({ color: i % 2 === 0 ? 0xf5f0d0 : 0xe6e0bb }),
  );
  marker.position.set(-4.8 + i * 1.2, 0.04, i % 2 === 0 ? -0.27 : 0.27);
  marker.rotation.y = i % 2 === 0 ? 0.34 : -0.34;
  marker.castShadow = false;
  marker.receiveShadow = false;
  scene.add(marker);
}

const clock = new THREE.Clock();
const stride = 1.45;
const travel = 8.4;
const startPhase = 0.85;

function setWalkPose(time: number): void {
  const phase = time * 5.2 + startPhase;
  const step = Math.sin(phase);
  const counterStep = Math.sin(phase + Math.PI);
  const lift = Math.max(0, Math.sin(phase));
  const counterLift = Math.max(0, Math.sin(phase + Math.PI));
  const bob = Math.abs(Math.sin(phase)) * 0.11;

  const loop = (time * 0.68) % 1;
  walker.position.x = -travel / 2 + loop * travel;
  walker.position.z = Math.sin(time * 0.7) * 0.18;
  walker.rotation.y = Math.sin(time * 0.7) * 0.04;

  body.position.y = 1.55 + bob;
  body.rotation.z = step * 0.045;
  body.rotation.x = Math.sin(phase * 2) * 0.03;
  head.rotation.x = -step * 0.08;
  head.rotation.z = -step * 0.05;

  leftLeg.pivot.rotation.x = step * 0.72;
  rightLeg.pivot.rotation.x = counterStep * 0.72;
  leftLeg.lowerPivot!.rotation.x = -0.18 - lift * 0.9;
  rightLeg.lowerPivot!.rotation.x = -0.18 - counterLift * 0.9;
  leftLeg.foot!.rotation.x = 0.16 + lift * 0.62;
  rightLeg.foot!.rotation.x = 0.16 + counterLift * 0.62;

  leftArm.pivot.rotation.x = counterStep * 0.82;
  rightArm.pivot.rotation.x = step * 0.82;
  leftArm.pivot.rotation.z = -0.18;
  rightArm.pivot.rotation.z = 0.18;
  leftArm.lowerPivot!.rotation.x = -0.18 + lift * 0.25;
  rightArm.lowerPivot!.rotation.x = -0.18 + counterLift * 0.25;

  shadowBlob.scale.set(1 + bob * 0.7, 0.72 + bob * 0.35, 1);
}

setWalkPose(0.2);

function animate(): void {
  const elapsed = clock.getElapsedTime() + 0.2;
  setWalkPose(elapsed);
  renderer.render(scene, camera);
  requestAnimationFrame(animate);
}

animate();

window.addEventListener('resize', () => {
  camera.aspect = window.innerWidth / window.innerHeight;
  camera.updateProjectionMatrix();
  renderer.setSize(window.innerWidth, window.innerHeight);
});

```
