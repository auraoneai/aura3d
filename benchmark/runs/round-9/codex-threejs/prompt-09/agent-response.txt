Implemented the procedural Three.js humanoid in `source/src/main.ts`.

Build command:
```bash
cd source && npm run build
```

Run command for the runner:
```bash
cd source && npm run dev
```

Build result: passed.

Assumptions:
- The runner will handle browser launch and screenshot/runtime evidence.
- No assets are required or used; the character and ground are fully procedural primitives.