The build succeeded (typecheck + Vite production build). Per the benchmark rules, I'm stopping here and reporting.

## Summary

Built **Prompt 07: Material Lab** by copying the matching benchmark recipe (07 Material Lab) and making only prompt-required edits. The scene uses `prefabs.materialSwatches()` (the five distinct material spheres: mirror metal, transparent glass, matte rubber, glowing emissive, glossy clearcoat), `lights.studio({ intensity: 1.55 })` for controlled studio lighting and environment reflections, `interactions.orbit()` for orbit controls, and a framed `camera.perspective(...)`. All imports are from `@aura3d/engine` public API; no custom material renderer was built.

**File changed:** `source/src/main.ts` (only file edited; no files outside the source directory touched).

### Commands
- **Build command:** `npm run build` (run inside `source/`) — already executed, passed (typecheck + Vite build clean).
- **Run command (for the runner):** `npm run dev -- --port <assigned-port>`

### Assumptions
- The `prefabs.materialSwatches()` prefab is the intended source for the five distinct material types and supplies environment-map reflection/contrast cues, per the recipe and llms.txt guidance to prefer prefabs over custom primitives.
- `lights.studio()` provides the "environment map" lighting/reflection the prompt requires; no separate asset-based env map is needed (prompt states "built procedurally," no assets).
- `npm install` was run before build; the esbuild postinstall warning is benign and did not affect the build.
