Implemented in `source/src/main.ts` using the matching Aura3D humanoid prefab recipe. Build completed successfully.

Build command:
```bash
cd source && npm run build
```

Run command for runner:
```bash
cd source && npm run dev
```

Assumptions:
- No external assets are needed or used.
- `prefabs.primitiveHumanoid({ showJoints: true, motionTrail: true })` supplies the procedural primitive humanoid, visible ground plane, path markers, and looped walk animation evidence.