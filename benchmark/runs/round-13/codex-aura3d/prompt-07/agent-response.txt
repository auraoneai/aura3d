Implemented Prompt 07 in [source/src/main.ts](/Users/gurbakshchahal/aura3d/benchmark/runs/round-13/codex-aura3d/prompt-07/source/src/main.ts:1) using the matching `prefabs.materialSwatches()` recipe with studio lighting, environment/reflection-ready material swatches, orbit controls, and the prescribed camera.

Build command run:

```bash
cd source && npm run build
```

Build failed:

```text
src/main.ts(1,77): error TS2307: Cannot find module '@aura3d/engine' or its corresponding type declarations.
```

Runner command for later:

```bash
cd source && npm run dev
```

Assumption: dependencies may need to be installed or the local packaged `@aura3d/engine` dependency resolved by the runner before building.