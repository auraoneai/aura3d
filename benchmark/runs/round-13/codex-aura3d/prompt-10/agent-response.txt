Implemented Prompt 10 in `source`.

Build command:
```bash
cd source && npm run build
```

Run command for runner:
```bash
cd source && npm run dev
```

Build result: passed.

Assumptions:
- Used only the provided `source/public/benchmark/assets/sneaker.glb`.
- `@aura3d/cli@latest` was unavailable in npm, so I used the local Aura3D asset pipeline from the installed engine package to generate `assets.sneaker`.
- No dev server, preview, Playwright, or screenshot capture was run.