# Generated Source Listing

This file is generated by the Round 13 capture runner for neutral scoring.

## index.html

```
<!doctype html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Aura3D Round 13 Benchmark</title>
  </head>
  <body>
    <div id="app"></div>
    <script type="module" src="/src/main.ts"></script>
  </body>
</html>

```

## src/main.ts

```
import * as THREE from 'three';
import { OrbitControls } from 'three/addons/controls/OrbitControls.js';

const app = document.querySelector<HTMLDivElement>('#app');

if (!app) {
  throw new Error('Missing #app element');
}

document.body.style.margin = '0';
document.body.style.overflow = 'hidden';
document.body.style.fontFamily =
  'Inter, ui-sans-serif, system-ui, -apple-system, BlinkMacSystemFont, "Segoe UI", sans-serif';

app.innerHTML = `
  <div class="toolbar">
    <div>
      <strong>Procedural City Block</strong>
      <span>20 buildings · window grids · street lights</span>
    </div>
    <label class="toggle">
      <input id="dayNightToggle" type="checkbox" />
      <span class="track"><span class="thumb"></span></span>
      <span id="modeLabel">Day</span>
    </label>
  </div>
`;

const style = document.createElement('style');
style.textContent = `
  .toolbar {
    position: fixed;
    z-index: 10;
    top: 18px;
    left: 18px;
    display: flex;
    align-items: center;
    gap: 18px;
    padding: 12px 14px;
    color: #111827;
    background: rgba(255, 255, 255, 0.88);
    border: 1px solid rgba(17, 24, 39, 0.18);
    border-radius: 8px;
    box-shadow: 0 14px 30px rgba(15, 23, 42, 0.16);
    backdrop-filter: blur(10px);
  }

  .toolbar strong,
  .toolbar span {
    display: block;
    white-space: nowrap;
  }

  .toolbar strong {
    font-size: 14px;
    line-height: 18px;
    letter-spacing: 0;
  }

  .toolbar div > span {
    font-size: 12px;
    line-height: 16px;
    color: #4b5563;
  }

  .toggle {
    display: grid;
    grid-template-columns: 46px 44px;
    align-items: center;
    gap: 8px;
    cursor: pointer;
    font-size: 13px;
    font-weight: 700;
  }

  .toggle input {
    position: absolute;
    opacity: 0;
    pointer-events: none;
  }

  .track {
    position: relative;
    width: 46px;
    height: 26px;
    border-radius: 999px;
    background: #f59e0b;
    box-shadow: inset 0 0 0 1px rgba(17, 24, 39, 0.16);
  }

  .thumb {
    position: absolute;
    top: 4px;
    left: 4px;
    width: 18px;
    height: 18px;
    border-radius: 50%;
    background: #ffffff;
    transition: transform 160ms ease;
    box-shadow: 0 2px 5px rgba(17, 24, 39, 0.28);
  }

  .toggle input:checked + .track {
    background: #1e3a8a;
  }

  .toggle input:checked + .track .thumb {
    transform: translateX(20px);
  }

  canvas {
    display: block;
  }

  @media (max-width: 620px) {
    .toolbar {
      left: 10px;
      right: 10px;
      top: 10px;
      justify-content: space-between;
      gap: 10px;
    }

    .toolbar strong,
    .toolbar div > span {
      white-space: normal;
    }
  }
`;
document.head.appendChild(style);

const scene = new THREE.Scene();
scene.background = new THREE.Color(0x9fc5e8);
scene.fog = new THREE.Fog(0x9fc5e8, 60, 135);

const camera = new THREE.PerspectiveCamera(52, window.innerWidth / window.innerHeight, 0.1, 300);
camera.position.set(33, 31, 43);

const renderer = new THREE.WebGLRenderer({ antialias: true });
renderer.setPixelRatio(Math.min(window.devicePixelRatio, 2));
renderer.setSize(window.innerWidth, window.innerHeight);
renderer.shadowMap.enabled = true;
renderer.shadowMap.type = THREE.PCFSoftShadowMap;
renderer.outputColorSpace = THREE.SRGBColorSpace;
app.appendChild(renderer.domElement);

const controls = new OrbitControls(camera, renderer.domElement);
controls.target.set(0, 8, 0);
controls.enableDamping = true;
controls.maxPolarAngle = Math.PI * 0.47;
controls.minDistance = 24;
controls.maxDistance = 90;
controls.update();

const sun = new THREE.DirectionalLight(0xffffff, 3.4);
sun.position.set(24, 48, 18);
sun.castShadow = true;
sun.shadow.mapSize.set(2048, 2048);
sun.shadow.camera.left = -55;
sun.shadow.camera.right = 55;
sun.shadow.camera.top = 55;
sun.shadow.camera.bottom = -55;
sun.shadow.camera.near = 1;
sun.shadow.camera.far = 110;
scene.add(sun);

const hemisphere = new THREE.HemisphereLight(0xbfe3ff, 0x72806d, 1.5);
scene.add(hemisphere);

const moon = new THREE.DirectionalLight(0x9dbbff, 0);
moon.position.set(-28, 42, -24);
scene.add(moon);

const groundMaterial = new THREE.MeshStandardMaterial({ color: 0x5d7154, roughness: 0.95 });
const ground = new THREE.Mesh(new THREE.BoxGeometry(86, 0.35, 86), groundMaterial);
ground.position.y = -0.2;
ground.receiveShadow = true;
scene.add(ground);

const roadMaterial = new THREE.MeshStandardMaterial({ color: 0x30333a, roughness: 0.88 });
const sidewalkMaterial = new THREE.MeshStandardMaterial({ color: 0xaeb4b9, roughness: 0.78 });
const laneMaterial = new THREE.MeshBasicMaterial({ color: 0xf7f1c8 });

function addBox(
  name: string,
  size: THREE.Vector3Tuple,
  position: THREE.Vector3Tuple,
  material: THREE.Material,
): THREE.Mesh {
  const mesh = new THREE.Mesh(new THREE.BoxGeometry(...size), material);
  mesh.name = name;
  mesh.position.set(...position);
  mesh.castShadow = true;
  mesh.receiveShadow = true;
  scene.add(mesh);
  return mesh;
}

addBox('north-south street', [13, 0.08, 86], [0, 0.05, 0], roadMaterial);
addBox('east-west street', [86, 0.09, 13], [0, 0.08, 0], roadMaterial);
addBox('north sidewalk', [86, 0.16, 3.2], [0, 0.18, 8.1], sidewalkMaterial);
addBox('south sidewalk', [86, 0.16, 3.2], [0, 0.18, -8.1], sidewalkMaterial);
addBox('east sidewalk', [3.2, 0.17, 86], [8.1, 0.2, 0], sidewalkMaterial);
addBox('west sidewalk', [3.2, 0.17, 86], [-8.1, 0.2, 0], sidewalkMaterial);

for (let z = -36; z <= 36; z += 8) {
  addBox('center lane dash', [0.35, 0.035, 3.5], [0, 0.18, z], laneMaterial);
}

for (let x = -36; x <= 36; x += 8) {
  addBox('cross lane dash', [3.5, 0.035, 0.35], [x, 0.2, 0], laneMaterial);
}

const buildingColors = [
  0x8a97a5, 0x64748b, 0xb0a090, 0x7b8794, 0x9b7f6d, 0x596579, 0xa6adb8, 0x8b948b,
];
const buildingMaterialCache = new Map<number, THREE.MeshStandardMaterial>();
const litWindowMaterial = new THREE.MeshStandardMaterial({
  color: 0xffd978,
  emissive: 0xffc34a,
  emissiveIntensity: 0.75,
  roughness: 0.42,
});
const dayWindowMaterial = new THREE.MeshStandardMaterial({
  color: 0x99c7dd,
  emissive: 0x08121a,
  emissiveIntensity: 0.04,
  roughness: 0.24,
  metalness: 0.05,
});
const windowMeshes: THREE.Mesh[] = [];

function materialFor(color: number): THREE.MeshStandardMaterial {
  const existing = buildingMaterialCache.get(color);
  if (existing) return existing;

  const material = new THREE.MeshStandardMaterial({
    color,
    roughness: 0.72,
    metalness: 0.04,
  });
  buildingMaterialCache.set(color, material);
  return material;
}

function addWindowStrip(
  parent: THREE.Object3D,
  width: number,
  depth: number,
  height: number,
  face: 'front' | 'back' | 'left' | 'right',
): void {
  const rows = Math.max(2, Math.floor(height / 2.7));
  const columns = face === 'front' || face === 'back' ? Math.max(2, Math.floor(width / 1.7)) : Math.max(2, Math.floor(depth / 1.7));
  const plane = new THREE.PlaneGeometry(0.62, 0.78);

  for (let row = 0; row < rows; row += 1) {
    for (let col = 0; col < columns; col += 1) {
      if ((row + col) % 7 === 0) continue;

      const windowMesh = new THREE.Mesh(plane, dayWindowMaterial);
      const x = face === 'left' ? -width / 2 - 0.012 : face === 'right' ? width / 2 + 0.012 : -width / 2 + ((col + 1) * width) / (columns + 1);
      const z = face === 'front' ? depth / 2 + 0.012 : face === 'back' ? -depth / 2 - 0.012 : -depth / 2 + ((col + 1) * depth) / (columns + 1);
      const y = -height / 2 + 1.25 + row * 1.75;

      windowMesh.position.set(x, y, z);
      if (face === 'back') windowMesh.rotation.y = Math.PI;
      if (face === 'left') windowMesh.rotation.y = -Math.PI / 2;
      if (face === 'right') windowMesh.rotation.y = Math.PI / 2;
      windowMesh.name = 'visible building window';
      parent.add(windowMesh);
      windowMeshes.push(windowMesh);
    }
  }
}

const buildingPositions: Array<[number, number]> = [
  [-30, -30], [-21, -31], [-30, -20], [-20, -21], [-12, -30],
  [14, -30], [24, -31], [34, -27], [14, -19], [27, -18],
  [-31, 15], [-21, 16], [-12, 24], [-31, 29], [-20, 31],
  [14, 15], [25, 16], [35, 19], [16, 30], [29, 31],
];

buildingPositions.forEach(([x, z], index) => {
  const width = 4.6 + (index % 4) * 0.9;
  const depth = 4.4 + ((index + 2) % 3) * 1.1;
  const height = 7 + ((index * 7) % 19);
  const building = addBox(
    `building ${index + 1}`,
    [width, height, depth],
    [x, height / 2, z],
    materialFor(buildingColors[index % buildingColors.length]),
  );

  addWindowStrip(building, width, depth, height, 'front');
  addWindowStrip(building, width, depth, height, 'back');
  addWindowStrip(building, width, depth, height, 'left');
  addWindowStrip(building, width, depth, height, 'right');

  const roof = addBox(`roof cap ${index + 1}`, [width + 0.25, 0.24, depth + 0.25], [x, height + 0.16, z], new THREE.MeshStandardMaterial({ color: 0x2f3540, roughness: 0.82 }));
  roof.castShadow = true;
});

const lampGroup = new THREE.Group();
scene.add(lampGroup);

const lampPoleMaterial = new THREE.MeshStandardMaterial({ color: 0x23262c, roughness: 0.58, metalness: 0.35 });
const lampBulbDay = new THREE.MeshStandardMaterial({ color: 0xfff1b8, emissive: 0xeab308, emissiveIntensity: 0.15 });
const lampBulbNight = new THREE.MeshStandardMaterial({ color: 0xfff7c2, emissive: 0xffd166, emissiveIntensity: 2.2 });
const lampLights: THREE.PointLight[] = [];
const bulbMeshes: THREE.Mesh[] = [];

function addStreetLight(x: number, z: number): void {
  const pole = new THREE.Mesh(new THREE.CylinderGeometry(0.09, 0.12, 4.4, 16), lampPoleMaterial);
  pole.position.set(x, 2.2, z);
  pole.castShadow = true;
  pole.receiveShadow = true;
  lampGroup.add(pole);

  const arm = new THREE.Mesh(new THREE.BoxGeometry(1.15, 0.12, 0.12), lampPoleMaterial);
  arm.position.set(x + (x < 0 ? 0.45 : -0.45), 4.25, z);
  arm.castShadow = true;
  lampGroup.add(arm);

  const bulb = new THREE.Mesh(new THREE.SphereGeometry(0.24, 18, 12), lampBulbDay);
  bulb.position.set(x + (x < 0 ? 1 : -1), 4.14, z);
  bulb.name = 'street light bulb';
  lampGroup.add(bulb);
  bulbMeshes.push(bulb);

  const light = new THREE.PointLight(0xffcc66, 0.15, 14, 2);
  light.position.copy(bulb.position);
  light.castShadow = true;
  lampGroup.add(light);
  lampLights.push(light);
}

for (const z of [-34, -22, -10, 10, 22, 34]) {
  addStreetLight(-7.8, z);
  addStreetLight(7.8, z);
}

for (const x of [-34, -22, -10, 10, 22, 34]) {
  addStreetLight(x, -7.8);
  addStreetLight(x, 7.8);
}

const plazaMaterial = new THREE.MeshStandardMaterial({ color: 0xc9c3b5, roughness: 0.8 });
addBox('central intersection paving', [15, 0.12, 15], [0, 0.26, 0], plazaMaterial);

const toggle = document.querySelector<HTMLInputElement>('#dayNightToggle');
const modeLabel = document.querySelector<HTMLSpanElement>('#modeLabel');

function setNight(isNight: boolean): void {
  scene.background = new THREE.Color(isNight ? 0x07111f : 0x9fc5e8);
  scene.fog = new THREE.Fog(isNight ? 0x07111f : 0x9fc5e8, isNight ? 34 : 60, isNight ? 120 : 135);
  sun.intensity = isNight ? 0.08 : 3.4;
  moon.intensity = isNight ? 1.05 : 0;
  hemisphere.intensity = isNight ? 0.42 : 1.5;
  hemisphere.color.setHex(isNight ? 0x1c3359 : 0xbfe3ff);
  hemisphere.groundColor.setHex(isNight ? 0x10151c : 0x72806d);
  groundMaterial.color.setHex(isNight ? 0x2f3f34 : 0x5d7154);
  windowMeshes.forEach((mesh) => {
    mesh.material = isNight ? litWindowMaterial : dayWindowMaterial;
  });
  bulbMeshes.forEach((mesh) => {
    mesh.material = isNight ? lampBulbNight : lampBulbDay;
  });
  lampLights.forEach((light) => {
    light.intensity = isNight ? 1.8 : 0.15;
  });
  if (modeLabel) modeLabel.textContent = isNight ? 'Night' : 'Day';
}

toggle?.addEventListener('change', () => setNight(toggle.checked));
setNight(false);

window.addEventListener('resize', () => {
  camera.aspect = window.innerWidth / window.innerHeight;
  camera.updateProjectionMatrix();
  renderer.setSize(window.innerWidth, window.innerHeight);
});

function animate(): void {
  controls.update();
  renderer.render(scene, camera);
  requestAnimationFrame(animate);
}

animate();

```
