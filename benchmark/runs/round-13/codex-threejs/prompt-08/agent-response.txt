Implemented the procedural Three.js city block in `source/src/main.ts`.

Build completed successfully.

Build command:
```bash
cd source && npm run build
```

Run command for the runner:
```bash
cd source && npm run dev
```

Assumptions:
- The runner will perform visual verification later.
- No external assets are used; the city is fully procedural.
- The day/night toggle is the visible UI control in the top-left and changes sky, lighting, windows, and street light intensity.