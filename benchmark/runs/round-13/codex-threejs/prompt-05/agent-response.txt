Implemented the 3D data visualization in [source/src/main.ts](/Users/gurbakshchahal/aura3d/benchmark/runs/round-13/codex-threejs/prompt-05/source/src/main.ts).

Build completed successfully.

Build command:
```bash
cd source && npm run build
```

Run command for runner:
```bash
cd source && npm run dev
```

Assumptions / run notes:
- Procedural only; no assets used.
- Scene contains exactly 36 bars in a 6x6 grid.
- Heights animate continuously from seeded random values.
- Bar color updates with height using a low-to-high color scale and visible legend.
- Hover-highlight is implemented with a raycaster: hovered bars glow slightly and widen on X/Z while the cursor changes to pointer.
- Axis labels are rendered with CSS2D labels for readability.