# Generated Source Listing

This file is generated by the Round 13 capture runner for neutral scoring.

## index.html

```
<!doctype html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Aura3D Round 13 Benchmark</title>
  </head>
  <body>
    <div id="app"></div>
    <script type="module" src="/src/main.ts"></script>
  </body>
</html>

```

## src/main.ts

```
import * as THREE from 'three';

const root = document.querySelector<HTMLDivElement>('#app');

if (!root) {
  throw new Error('Missing #app root element');
}

document.body.style.margin = '0';
document.body.style.overflow = 'hidden';
root.style.width = '100vw';
root.style.height = '100vh';

const scene = new THREE.Scene();
scene.background = new THREE.Color(0xdfe9f3);
scene.fog = new THREE.Fog(0xdfe9f3, 18, 42);

const camera = new THREE.PerspectiveCamera(48, window.innerWidth / window.innerHeight, 0.1, 100);
camera.position.set(6, 4.2, 7.5);
camera.lookAt(0, 1.4, 0);

const renderer = new THREE.WebGLRenderer({ antialias: true });
renderer.setPixelRatio(Math.min(window.devicePixelRatio, 2));
renderer.setSize(window.innerWidth, window.innerHeight);
renderer.shadowMap.enabled = true;
renderer.shadowMap.type = THREE.PCFSoftShadowMap;
root.appendChild(renderer.domElement);

const hemiLight = new THREE.HemisphereLight(0xffffff, 0x6c7f8d, 2.1);
scene.add(hemiLight);

const sun = new THREE.DirectionalLight(0xffffff, 2.4);
sun.position.set(5, 8, 4);
sun.castShadow = true;
sun.shadow.mapSize.set(2048, 2048);
sun.shadow.camera.left = -12;
sun.shadow.camera.right = 12;
sun.shadow.camera.top = 12;
sun.shadow.camera.bottom = -12;
scene.add(sun);

const groundMaterial = new THREE.MeshStandardMaterial({
  color: 0x7aa36f,
  roughness: 0.82,
  metalness: 0.02,
});
const ground = new THREE.Mesh(new THREE.PlaneGeometry(26, 14), groundMaterial);
ground.rotation.x = -Math.PI / 2;
ground.receiveShadow = true;
scene.add(ground);

const grid = new THREE.GridHelper(26, 26, 0x426746, 0x92b083);
grid.position.y = 0.012;
scene.add(grid);

const humanoid = new THREE.Group();
scene.add(humanoid);

const skin = new THREE.MeshStandardMaterial({ color: 0xf1c27d, roughness: 0.55 });
const shirt = new THREE.MeshStandardMaterial({ color: 0x2c6fbb, roughness: 0.62 });
const pants = new THREE.MeshStandardMaterial({ color: 0x24364f, roughness: 0.68 });
const shoeMat = new THREE.MeshStandardMaterial({ color: 0x202326, roughness: 0.7 });

function shadowed(mesh: THREE.Mesh): THREE.Mesh {
  mesh.castShadow = true;
  mesh.receiveShadow = true;
  return mesh;
}

function makeLimb(
  name: string,
  geometry: THREE.BufferGeometry,
  material: THREE.Material,
  pivot: THREE.Vector3,
  segmentCenterY: number,
): THREE.Group {
  const limb = new THREE.Group();
  limb.name = name;
  limb.position.copy(pivot);

  const mesh = shadowed(new THREE.Mesh(geometry, material));
  mesh.position.y = segmentCenterY;
  limb.add(mesh);
  humanoid.add(limb);

  return limb;
}

const torso = shadowed(new THREE.Mesh(new THREE.CylinderGeometry(0.48, 0.56, 1.35, 28), shirt));
torso.position.y = 1.65;
humanoid.add(torso);

const neck = shadowed(new THREE.Mesh(new THREE.CylinderGeometry(0.15, 0.16, 0.22, 20), skin));
neck.position.y = 2.43;
humanoid.add(neck);

const head = shadowed(new THREE.Mesh(new THREE.SphereGeometry(0.38, 32, 20), skin));
head.position.y = 2.86;
humanoid.add(head);

const nose = shadowed(new THREE.Mesh(new THREE.ConeGeometry(0.055, 0.18, 16), skin));
nose.position.set(0, 2.86, 0.38);
nose.rotation.x = Math.PI / 2;
humanoid.add(nose);

const leftArm = makeLimb(
  'left arm',
  new THREE.BoxGeometry(0.24, 1.05, 0.24),
  shirt,
  new THREE.Vector3(-0.66, 2.18, 0),
  -0.48,
);
const rightArm = makeLimb(
  'right arm',
  new THREE.BoxGeometry(0.24, 1.05, 0.24),
  shirt,
  new THREE.Vector3(0.66, 2.18, 0),
  -0.48,
);

const leftLeg = makeLimb(
  'left leg',
  new THREE.BoxGeometry(0.28, 1.22, 0.28),
  pants,
  new THREE.Vector3(-0.25, 1.02, 0),
  -0.55,
);
const rightLeg = makeLimb(
  'right leg',
  new THREE.BoxGeometry(0.28, 1.22, 0.28),
  pants,
  new THREE.Vector3(0.25, 1.02, 0),
  -0.55,
);

const leftShoe = shadowed(new THREE.Mesh(new THREE.BoxGeometry(0.36, 0.16, 0.58), shoeMat));
leftShoe.position.set(0, -1.18, 0.12);
leftLeg.add(leftShoe);

const rightShoe = shadowed(new THREE.Mesh(new THREE.BoxGeometry(0.36, 0.16, 0.58), shoeMat));
rightShoe.position.set(0, -1.18, 0.12);
rightLeg.add(rightShoe);

const path = new THREE.CatmullRomCurve3([
  new THREE.Vector3(-5.2, 0.02, -1.1),
  new THREE.Vector3(-2.7, 0.02, -0.25),
  new THREE.Vector3(0, 0.02, 0.15),
  new THREE.Vector3(2.7, 0.02, -0.35),
  new THREE.Vector3(5.2, 0.02, 1.0),
]);

const pathPoints = path.getPoints(96);
const pathLine = new THREE.Line(
  new THREE.BufferGeometry().setFromPoints(pathPoints),
  new THREE.LineBasicMaterial({ color: 0xffffff, transparent: true, opacity: 0.52 }),
);
pathLine.position.y = 0.035;
scene.add(pathLine);

const markers = new THREE.Group();
for (let i = 0; i < 9; i += 1) {
  const marker = new THREE.Mesh(
    new THREE.CylinderGeometry(0.045, 0.045, 0.012, 16),
    new THREE.MeshBasicMaterial({ color: i % 2 === 0 ? 0xf4f8fb : 0xd4e4ef }),
  );
  marker.position.copy(path.getPoint(i / 8));
  marker.position.y = 0.045;
  markers.add(marker);
}
scene.add(markers);

const clock = new THREE.Clock();
const cycleSpeed = 6.2;
const stride = 0.72;

function animate(): void {
  const elapsed = clock.getElapsedTime();
  const cycle = elapsed * cycleSpeed;
  const progress = (elapsed * 0.075) % 1;
  const position = path.getPoint(progress);
  const next = path.getPoint((progress + 0.01) % 1);
  const heading = Math.atan2(next.x - position.x, next.z - position.z);

  humanoid.position.copy(position);
  humanoid.rotation.y = heading;
  humanoid.position.y += 0.08 + Math.abs(Math.sin(cycle * 2)) * 0.08;
  humanoid.rotation.z = Math.sin(cycle) * 0.045;

  const leftPhase = Math.sin(cycle);
  const rightPhase = Math.sin(cycle + Math.PI);
  leftLeg.rotation.x = leftPhase * stride;
  rightLeg.rotation.x = rightPhase * stride;
  leftArm.rotation.x = rightPhase * 0.92;
  rightArm.rotation.x = leftPhase * 0.92;

  leftLeg.rotation.z = -0.08 + Math.max(0, -leftPhase) * 0.08;
  rightLeg.rotation.z = 0.08 - Math.max(0, -rightPhase) * 0.08;
  leftArm.rotation.z = -0.15 + leftPhase * 0.05;
  rightArm.rotation.z = 0.15 + rightPhase * 0.05;

  torso.rotation.x = Math.sin(cycle * 2) * 0.035;
  head.rotation.y = Math.sin(cycle * 0.5) * 0.18;

  renderer.render(scene, camera);
  requestAnimationFrame(animate);
}

window.addEventListener('resize', () => {
  camera.aspect = window.innerWidth / window.innerHeight;
  camera.updateProjectionMatrix();
  renderer.setSize(window.innerWidth, window.innerHeight);
});

animate();

```
