Implemented the procedural Three.js humanoid in `source/src/main.ts`. It uses only primitives: sphere head, cylinder torso/neck, box arms and legs, shoes, a visible ground plane/grid, and a runtime walk cycle with opposing arm/leg swings while moving along a ground path.

Build completed successfully.

Build command:
```bash
cd source && npm run build
```

Run command for the runner:
```bash
cd source && npm run dev
```

Assumptions: no assets are used, per Prompt 09; visual verification is left to the benchmark runner because the instructions prohibit running dev/preview or screenshots in-agent.