Implemented the procedural Neon Tunnel Flythrough in `source/src/main.ts`.

Build completed successfully.

Build command:
```bash
cd source && npm run build
```

Run command for the runner:
```bash
cd source && npm run dev
```

Assumptions:
- Runtime runner will capture the animated browser scene.
- No external assets are needed; all tunnel geometry, emissive segments, bloom, fog, and camera motion are procedural.
- I did not run the dev server or visual verification, per benchmark rules.