Implemented the Three.js product viewer in `source/src/main.ts`.

Build completed successfully with:

```bash
npm run build
```

Runner command to execute later from `source/`:

```bash
npm run dev
```

Assumptions:
- The benchmark runner serves `source/public` at the site root, so the allowed asset resolves as `/benchmark/assets/sneaker.glb`.
- No matching `context/docs/agents/benchmark-recipes.md` recipe was present.
- I followed `context/llms.txt` and used raw Three.js with the provided local sneaker asset only.