# Generated Source Listing

This file is generated by the Round 13 capture runner for neutral scoring.

## index.html

```
<!doctype html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Aura3D Round 13 Benchmark</title>
  </head>
  <body>
    <div id="app"></div>
    <script type="module" src="/src/main.ts"></script>
  </body>
</html>

```

## src/main.ts

```
import * as THREE from 'three';
import { OrbitControls } from 'three/addons/controls/OrbitControls.js';
import { CSS2DObject, CSS2DRenderer } from 'three/addons/renderers/CSS2DRenderer.js';
import { EffectComposer } from 'three/addons/postprocessing/EffectComposer.js';
import { RenderPass } from 'three/addons/postprocessing/RenderPass.js';
import { UnrealBloomPass } from 'three/addons/postprocessing/UnrealBloomPass.js';

type PlanetSpec = {
  name: string;
  radius: number;
  orbitRadius: number;
  speed: number;
  color: THREE.ColorRepresentation;
  accent: THREE.ColorRepresentation;
  startAngle: number;
};

const app = document.querySelector<HTMLDivElement>('#app');

if (!app) {
  throw new Error('Missing #app root element.');
}

document.body.style.margin = '0';
document.body.style.overflow = 'hidden';
document.body.style.background = '#03050b';
app.style.width = '100vw';
app.style.height = '100vh';

const scene = new THREE.Scene();
scene.background = new THREE.Color(0x03050b);

const camera = new THREE.PerspectiveCamera(45, window.innerWidth / window.innerHeight, 0.1, 500);
camera.position.set(0, 42, 76);
camera.lookAt(0, 0, 0);

const renderer = new THREE.WebGLRenderer({ antialias: true });
renderer.setPixelRatio(Math.min(window.devicePixelRatio, 2));
renderer.setSize(window.innerWidth, window.innerHeight);
renderer.outputColorSpace = THREE.SRGBColorSpace;
renderer.toneMapping = THREE.ACESFilmicToneMapping;
renderer.toneMappingExposure = 1.2;
app.appendChild(renderer.domElement);

const labelRenderer = new CSS2DRenderer();
labelRenderer.setSize(window.innerWidth, window.innerHeight);
labelRenderer.domElement.style.position = 'fixed';
labelRenderer.domElement.style.inset = '0';
labelRenderer.domElement.style.pointerEvents = 'none';
app.appendChild(labelRenderer.domElement);

const controls = new OrbitControls(camera, renderer.domElement);
controls.enableDamping = true;
controls.dampingFactor = 0.06;
controls.minDistance = 28;
controls.maxDistance = 140;
controls.target.set(0, 0, 0);

scene.add(new THREE.AmbientLight(0x667799, 0.22));

const starGeometry = new THREE.BufferGeometry();
const starPositions: number[] = [];
for (let i = 0; i < 900; i += 1) {
  const radius = 95 + Math.random() * 105;
  const theta = Math.random() * Math.PI * 2;
  const phi = Math.acos(THREE.MathUtils.randFloatSpread(2));
  starPositions.push(
    radius * Math.sin(phi) * Math.cos(theta),
    radius * Math.cos(phi),
    radius * Math.sin(phi) * Math.sin(theta),
  );
}
starGeometry.setAttribute('position', new THREE.Float32BufferAttribute(starPositions, 3));
const stars = new THREE.Points(
  starGeometry,
  new THREE.PointsMaterial({ color: 0xbfd8ff, size: 0.35, sizeAttenuation: true }),
);
scene.add(stars);

const sun = new THREE.Mesh(
  new THREE.SphereGeometry(5.2, 64, 64),
  new THREE.MeshBasicMaterial({ color: 0xffb52e }),
);
scene.add(sun);

const sunCorona = new THREE.Mesh(
  new THREE.SphereGeometry(6.7, 64, 64),
  new THREE.MeshBasicMaterial({
    color: 0xff6f1c,
    transparent: true,
    opacity: 0.24,
    blending: THREE.AdditiveBlending,
    depthWrite: false,
  }),
);
scene.add(sunCorona);

const sunLight = new THREE.PointLight(0xffcf79, 650, 150, 1.5);
scene.add(sunLight);

const planetSpecs: PlanetSpec[] = [
  { name: 'Aster', radius: 0.9, orbitRadius: 10, speed: 0.9, color: 0xb9b3a5, accent: 0xf1d9b6, startAngle: 0.3 },
  { name: 'Vela', radius: 1.25, orbitRadius: 15.5, speed: 0.62, color: 0xd78a4b, accent: 0xffc081, startAngle: 1.8 },
  { name: 'Nereid', radius: 1.45, orbitRadius: 22, speed: 0.43, color: 0x3a8bd7, accent: 0x83d6ff, startAngle: 3.1 },
  { name: 'Kora', radius: 1.1, orbitRadius: 29.5, speed: 0.31, color: 0xc84d44, accent: 0xff8d7d, startAngle: 4.4 },
  { name: 'Juno', radius: 2.2, orbitRadius: 39, speed: 0.2, color: 0xd9b175, accent: 0x7c5a3d, startAngle: 2.55 },
  { name: 'Oris', radius: 1.75, orbitRadius: 51, speed: 0.14, color: 0x82d1d6, accent: 0xd0ffff, startAngle: 5.25 },
];

const planets = planetSpecs.map((spec) => {
  const pivot = new THREE.Group();
  pivot.rotation.y = spec.startAngle;

  const planet = new THREE.Mesh(
    new THREE.SphereGeometry(spec.radius, 40, 40),
    new THREE.MeshStandardMaterial({
      color: spec.color,
      roughness: 0.74,
      metalness: 0.02,
      emissive: spec.color,
      emissiveIntensity: 0.03,
    }),
  );
  planet.position.x = spec.orbitRadius;
  pivot.add(planet);

  const stripe = new THREE.Mesh(
    new THREE.TorusGeometry(spec.radius * 1.03, spec.radius * 0.035, 8, 96),
    new THREE.MeshBasicMaterial({ color: spec.accent, transparent: true, opacity: 0.75 }),
  );
  stripe.position.copy(planet.position);
  stripe.rotation.x = Math.PI / 2.35;
  pivot.add(stripe);

  const orbit = new THREE.LineLoop(
    new THREE.BufferGeometry().setFromPoints(
      Array.from({ length: 192 }, (_, index) => {
        const angle = (index / 192) * Math.PI * 2;
        return new THREE.Vector3(Math.cos(angle) * spec.orbitRadius, 0, Math.sin(angle) * spec.orbitRadius);
      }),
    ),
    new THREE.LineBasicMaterial({ color: 0x496584, transparent: true, opacity: 0.42 }),
  );
  scene.add(orbit);

  const labelElement = document.createElement('div');
  labelElement.textContent = spec.name;
  labelElement.style.padding = '3px 8px';
  labelElement.style.border = '1px solid rgba(205, 225, 255, 0.72)';
  labelElement.style.borderRadius = '999px';
  labelElement.style.background = 'rgba(5, 9, 18, 0.76)';
  labelElement.style.color = '#f6fbff';
  labelElement.style.font = '600 13px/1.25 system-ui, -apple-system, BlinkMacSystemFont, "Segoe UI", sans-serif';
  labelElement.style.textShadow = '0 1px 3px #000';
  labelElement.style.whiteSpace = 'nowrap';

  const label = new CSS2DObject(labelElement);
  label.position.set(0, spec.radius + 0.95, 0);
  planet.add(label);

  scene.add(pivot);
  return { spec, pivot, planet, stripe };
});

const sunLabelElement = document.createElement('div');
sunLabelElement.textContent = 'Sun';
sunLabelElement.style.padding = '4px 10px';
sunLabelElement.style.border = '1px solid rgba(255, 225, 148, 0.85)';
sunLabelElement.style.borderRadius = '999px';
sunLabelElement.style.background = 'rgba(46, 18, 0, 0.72)';
sunLabelElement.style.color = '#fff4cf';
sunLabelElement.style.font = '700 14px/1.25 system-ui, -apple-system, BlinkMacSystemFont, "Segoe UI", sans-serif';
sunLabelElement.style.textShadow = '0 1px 4px #000';
const sunLabel = new CSS2DObject(sunLabelElement);
sunLabel.position.set(0, 7.2, 0);
sun.add(sunLabel);

const composer = new EffectComposer(renderer);
composer.addPass(new RenderPass(scene, camera));
composer.addPass(
  new UnrealBloomPass(
    new THREE.Vector2(window.innerWidth, window.innerHeight),
    1.15,
    0.72,
    0.16,
  ),
);

const clock = new THREE.Clock();

function animate() {
  const elapsed = clock.getElapsedTime();

  sun.rotation.y = elapsed * 0.18;
  sunCorona.rotation.y = -elapsed * 0.12;
  sunCorona.scale.setScalar(1 + Math.sin(elapsed * 1.8) * 0.025);
  stars.rotation.y = elapsed * 0.006;

  for (const entry of planets) {
    entry.pivot.rotation.y = entry.spec.startAngle + elapsed * entry.spec.speed;
    entry.planet.rotation.y = elapsed * (0.9 + entry.spec.speed);
    entry.stripe.rotation.z = elapsed * 0.18;
  }

  controls.update();
  composer.render();
  labelRenderer.render(scene, camera);
  requestAnimationFrame(animate);
}

function resize() {
  const width = window.innerWidth;
  const height = window.innerHeight;
  camera.aspect = width / height;
  camera.updateProjectionMatrix();
  renderer.setSize(width, height);
  composer.setSize(width, height);
  labelRenderer.setSize(width, height);
}

window.addEventListener('resize', resize);
animate();

```
