Build succeeded — TypeScript strict typecheck passed and Vite produced the production bundle. Per the benchmark rules I'm stopping here.

## Summary

Built a procedural **Material Lab** in `source/src/main.ts` using raw Three.js 0.165.0 (Vite + strict TS scaffold).

**What it renders:**
- **Five spheres** in a row, each a visually distinct material:
  - **Metal** — `MeshStandardMaterial`, metalness 1.0, roughness 0.12 (sharp env reflections)
  - **Glass** — `MeshPhysicalMaterial`, transmission 1.0, roughness 0, ior 1.5, thickness 1.5 (refractive/transparent)
  - **Rubber** — `MeshStandardMaterial`, metalness 0, roughness 0.95, dark (matte)
  - **Emissive** — `MeshStandardMaterial` with red emissive @ intensity 2.5 (self-lit glow)
  - **Clearcoat** — `MeshPhysicalMaterial`, clearcoat 1.0 over a diffuse red base (dual-spec lobes)
- **Studio lighting**: ambient + key (shadow-casting), fill, and rim directional lights; `PCFSoftShadowMap` soft shadows onto a ground plane; ACES filmic tone mapping.
- **Environment map**: `RoomEnvironment` baked through `PMREMGenerator` and assigned to `scene.environment` for IBL + reflections on all PBR materials.
- **Orbit controls**: `OrbitControls` with damping, distance/polar clamps.

All Required Visual Evidence items are covered (five spheres, distinct materials, studio highlights/shadows, env reflections, orbit controls). Plus a resize handler and full-viewport canvas via an index.html CSS reset.

**Commands for the runner:**
- Install: `npm install`
- Build: `npm run build`
- Run: `npm run dev -- --port <assigned-port>` (Vite dev server, host 127.0.0.1)

**Assumptions:**
- "Environment map" satisfied procedurally via the bundled official `RoomEnvironment` addon (no image asset, per the no-asset constraint) baked through PMREM — gives genuine IBL and mirror reflections.
- Studio lighting interpreted as a 3-point rig (key/fill/rim) plus soft shadows and a neutral ground plane.
- Only `three` and bundled official `three/addons/*` modules used; no external assets fetched.
- Build output (~481 kB JS) is expected for a Three.js app; the `esbuild` install-script warning is benign and the build completed cleanly.
