Implemented the physics playground in `source/`.

What changed:
- Aura3D scene with `prefabs.physicsPlayground({ cubes: 50 })`
- Visible tilted ramp, 50 cubes, contact normal/contact glow evidence, and fall-path streaks
- Orbit camera interaction
- HUD with live `Contact count`, rigid body count, and `Reset` button
- Empty Aura asset manifest/type module since this prompt uses no assets

Verification run:
- `npm run build` passed
- Local packaged Aura CLI `assets validate` passed
- Local packaged Aura CLI `check-deploy --dist dist` passed
- Started Vite briefly, confirmed HTTP 200, then stopped it

Build command:
```bash
cd source
npm run build
```

Run command:
```bash
cd source
npm run dev -- --port 4174
```

Assumptions:
- No external assets are used.
- Aura3D’s public procedural physics prefab is the supported API surface for this benchmark prompt; contact behavior is made visible with the prefab’s collision layout plus procedural contact markers and live telemetry.