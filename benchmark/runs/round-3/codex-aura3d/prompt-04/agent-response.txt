Implemented the procedural neon tunnel flythrough in `source/src/main.ts`.

Build command:
`cd source && npm run build`

Run command:
`cd source && npm run dev`

Verification:
`npm run build` passed, and I captured a runtime headless Chrome screenshot showing the tunnel interior, emissive ribs, bloom/glow, fog/depth falloff, and dolly flythrough framing. The temporary dev server was stopped.

Assumptions:
No assets are used; the scene is fully procedural as required.