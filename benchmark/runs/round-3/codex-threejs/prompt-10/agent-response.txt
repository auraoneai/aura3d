Implemented the Three.js product viewer in [source/src/main.ts](/Users/gurbakshchahal/aura3d/benchmark/runs/round-3/codex-threejs/prompt-10/source/src/main.ts).

It loads only `benchmark/assets/sneaker.glb` via the relative path that resolves to that asset, centers and auto-scales it onto a visible plinth, adds studio-style lighting, OrbitControls, shadows, and continuous turntable rotation.

Build command:
```bash
cd source
npm run build
```

Run command:
```bash
cd source
npm run dev -- --port 5177
```

Verification completed:
```bash
npm run build
```
I also ran a temporary local Vite server, captured a Playwright screenshot, confirmed the sneaker/plinth rendered, then stopped the server. No long-running dev server is left attached.

Assumption: `benchmark/assets/sneaker.glb` remains at the benchmark-level `assets` directory relative to this prompt run.