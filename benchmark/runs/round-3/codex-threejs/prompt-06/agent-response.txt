Implemented the mini-golf hole in [source/src/main.ts](/Users/gurbakshchahal/aura3d/benchmark/runs/round-3/codex-threejs/prompt-06/source/src/main.ts). It is fully procedural: green, rails, obstacle, cup/flag, ball physics, click-drag-release aiming, stroke counter, and a follow camera centered on the ball.

Verification passed:
- `npm run typecheck`
- `npm run build`
- Short `npm run dev -- --port 5178` smoke check returned HTTP 200, then the server was stopped.

Build command:
```bash
cd source
npm run build
```

Run command:
```bash
cd source
npm run dev
```

Assumptions:
- No assets are used because Prompt 06 requires procedural content.
- `llms.txt` was requested by AGENTS.md but was not present in the provided prompt directory/context.