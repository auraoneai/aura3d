Implemented the procedural particle fountain in [source/src/main.ts](/Users/gurbakshchahal/aura3d/benchmark/runs/round-3/codex-threejs/prompt-02/source/src/main.ts): emitter, gravity-driven arcing particles, lifetime color ramp, ground collision plane, and visible emission-rate slider.

Build command:
```bash
cd source
npm run build
```

Run command:
```bash
cd source
npm run dev
```

Verification:
```bash
npm run build
```
passes, and a finite Vite smoke test returned HTTP 200. No dev server is left running.

Assumptions: `llms.txt` was not present in the provided directory tree, so I used only the prompt, context bundle, and clean `source` directory. No assets were used.