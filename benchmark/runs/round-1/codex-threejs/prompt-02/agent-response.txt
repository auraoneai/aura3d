Implemented the procedural particle fountain in `source/src/main.ts`.

It includes a visible emitter/nozzle, animated gravity-driven particles with ground collision and bounce, lifetime-based color variation, a checker ground plane, and an emission-rate slider.

Build command:
```bash
cd source
npm run build
```

Run command:
```bash
cd source
npm run dev
```

Current dev server is running at:
```text
http://127.0.0.1:5174/
```

Verification:
```bash
npm run build
```
passed, and I also checked a browser render screenshot.

Assumptions:
- `llms.txt` was not present in the provided directory, so I used only `prompt.md`, `context`, and `source`.
- No external assets were used; the scene is fully procedural.