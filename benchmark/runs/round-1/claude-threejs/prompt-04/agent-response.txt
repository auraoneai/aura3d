Reverted and the production build passes (typecheck + bundle). The app is complete.

## Summary

I built a **Neon Tunnel Flythrough** in `source/src/main.ts` using Three.js 0.165.0 (the version pinned in the provided source skeleton). All required visual evidence is present and verified via headless-browser screenshots.

### How it satisfies the prompt

- **Procedural tube geometry** — A smooth winding 3D path is generated from a sine/cosine-driven `CatmullRomCurve3` (60 control points over 220 units), fed into a `TubeGeometry` (1400×48 segments, radius 6) rendered with `BackSide` so the camera sees the tunnel *interior*.
- **Emissive segments** — Two layers: (1) a custom `ShaderMaterial` on the wall that emits bright color-cycling neon rings (cyan→magenta→violet) and longitudinal strips that scroll toward the camera; (2) 70 discrete `MeshBasicMaterial` torus rings placed and oriented along the curve tangent as the strongest glow sources.
- **Bloom postprocess** — `EffectComposer` → `RenderPass` → `UnrealBloomPass` (strength 1.1, radius 0.55, threshold 0.7 so only neon blooms) → `OutputPass`, with ACES tone mapping.
- **Fog falloff** — `FogExp2` gives exponential depth falloff; rings clearly fade into darkness down the tube.
- **Camera animation** — Each frame the camera is positioned along the curve (`getPointAt`) looking slightly ahead, with a gentle roll, looping a full pass every 30s. Two screenshots at different path positions confirmed distinct flythrough views (off-center vanishing point as the tube bends, different ring colors).

### Commands

- **Install:** `npm install`
- **Build:** `npm run build` (runs `tsc --noEmit` typecheck + `vite build`)
- **Run (dev):** `npm run dev -- --port <assigned-port>` (e.g. `npm run dev -- --port 5174`)
- **Preview built output:** `npm run preview`

### Assumptions

- Three.js addons (`EffectComposer`, `RenderPass`, `UnrealBloomPass`, `OutputPass`) are imported via the `three/addons/...` path that ships with the npm `three@0.165.0` package — verified present after `npm install`. No external assets are used; everything is procedural.
- I pinned the camera canvas to a fixed full-viewport position and reset body margins in JS, because the skeleton's `#app` div has no intrinsic height (a plain `width:100%/height:100%` canvas would otherwise collapse to zero height).
- The dev server binds to `127.0.0.1` (from the skeleton's `vite --host` config); pass `--port` as the harness specifies.

All edits were confined to `source/` (only `src/main.ts`).
