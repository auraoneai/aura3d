Implemented in `source`.

Build command: `npm run build`

Run command: `npm run dev`

Current dev server: `http://127.0.0.1:5175/`
Vite chose `5175` because `5173` and `5174` were already in use.

Verified:
- `npm run build` passes.
- Browser screenshot smoke check shows one sun, six planets, distinct orbit rings, readable labels, sun bloom/glow, and the full system framed.

Assumption: Aura3D’s public API here has no text primitive, so planet labels are HTML overlays anchored over the Aura3D scene. No external assets were used.