Implemented in `./source`.

Build command:
```bash
cd source
npm run build
```

Run command:
```bash
cd source
npm run dev -- --port 4178
```

Current dev URL:
```text
http://127.0.0.1:4178/
```

Verification done:
- `npm run build` passed.
- Aura asset validation passed.
- Deploy check passed.
- App and `/aura-assets/sneaker.e1d7cb19.glb` both return `200 OK`.

Assumptions:
- Used only the provided `source/public/benchmark/assets/sneaker.glb`.
- Registered it as typed Aura asset `assets.sneaker`.
- The public npm `@aura3d/cli@latest` was unavailable, so I used the bundled Aura3D CLI from the local engine package.