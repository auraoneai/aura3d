Implemented the procedural Neon Tunnel Flythrough in `source/src/main.ts`.

Build completed successfully.

Build command:
```bash
cd source && npm run build
```

Run command for the runner:
```bash
cd source && npm run dev
```

Assumptions:
- Runner will handle runtime screenshot/evidence capture.
- No external assets are used; the tunnel, emissive segments, glow, fog, and camera motion are all procedural.
- I did not run a dev server, preview, Playwright, or screenshot capture per benchmark rules.