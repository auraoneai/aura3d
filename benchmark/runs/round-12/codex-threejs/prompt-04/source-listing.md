# Generated Source Listing

This file is generated by the Round 12 capture runner for neutral scoring.

## index.html

```
<!doctype html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Aura3D Round 12 Benchmark</title>
  </head>
  <body>
    <div id="app"></div>
    <script type="module" src="/src/main.ts"></script>
  </body>
</html>

```

## src/main.ts

```
import * as THREE from 'three';
import { EffectComposer } from 'three/addons/postprocessing/EffectComposer.js';
import { RenderPass } from 'three/addons/postprocessing/RenderPass.js';
import { UnrealBloomPass } from 'three/addons/postprocessing/UnrealBloomPass.js';

const app = document.querySelector<HTMLDivElement>('#app');

if (!app) {
  throw new Error('Missing #app element');
}

document.body.style.margin = '0';
document.body.style.overflow = 'hidden';
document.body.style.background = '#02030a';
app.style.width = '100vw';
app.style.height = '100vh';

const scene = new THREE.Scene();
scene.background = new THREE.Color(0x02030a);
scene.fog = new THREE.FogExp2(0x06081f, 0.028);

const camera = new THREE.PerspectiveCamera(72, window.innerWidth / window.innerHeight, 0.08, 280);
camera.position.set(0, 0, 7);

const renderer = new THREE.WebGLRenderer({ antialias: true, powerPreference: 'high-performance' });
renderer.setPixelRatio(Math.min(window.devicePixelRatio, 2));
renderer.setSize(window.innerWidth, window.innerHeight);
renderer.toneMapping = THREE.ACESFilmicToneMapping;
renderer.toneMappingExposure = 1.45;
renderer.outputColorSpace = THREE.SRGBColorSpace;
app.appendChild(renderer.domElement);

const composer = new EffectComposer(renderer);
composer.addPass(new RenderPass(scene, camera));

const bloomPass = new UnrealBloomPass(
  new THREE.Vector2(window.innerWidth, window.innerHeight),
  1.55,
  0.72,
  0.08,
);
composer.addPass(bloomPass);

const tunnelLength = 240;
const axialSegments = 220;
const radialSegments = 56;
const baseRadius = 5.2;
const positions: number[] = [];
const normals: number[] = [];
const uvs: number[] = [];
const indices: number[] = [];

function tunnelRadius(z: number, theta: number): number {
  return (
    baseRadius +
    Math.sin(z * 0.13 + theta * 3.0) * 0.34 +
    Math.cos(z * 0.047 - theta * 5.0) * 0.18
  );
}

for (let i = 0; i <= axialSegments; i += 1) {
  const v = i / axialSegments;
  const z = -v * tunnelLength;

  for (let j = 0; j <= radialSegments; j += 1) {
    const u = j / radialSegments;
    const theta = u * Math.PI * 2;
    const radius = tunnelRadius(z, theta);
    const x = Math.cos(theta) * radius;
    const y = Math.sin(theta) * radius;

    positions.push(x, y, z);
    normals.push(-Math.cos(theta), -Math.sin(theta), 0);
    uvs.push(u, v);
  }
}

for (let i = 0; i < axialSegments; i += 1) {
  for (let j = 0; j < radialSegments; j += 1) {
    const a = i * (radialSegments + 1) + j;
    const b = a + radialSegments + 1;
    const c = b + 1;
    const d = a + 1;

    indices.push(a, b, d, b, c, d);
  }
}

const tunnelGeometry = new THREE.BufferGeometry();
tunnelGeometry.setAttribute('position', new THREE.Float32BufferAttribute(positions, 3));
tunnelGeometry.setAttribute('normal', new THREE.Float32BufferAttribute(normals, 3));
tunnelGeometry.setAttribute('uv', new THREE.Float32BufferAttribute(uvs, 2));
tunnelGeometry.setIndex(indices);
tunnelGeometry.computeBoundingSphere();

const tunnelMaterial = new THREE.MeshStandardMaterial({
  color: 0x10142b,
  emissive: 0x070a19,
  emissiveIntensity: 0.42,
  metalness: 0.15,
  roughness: 0.48,
  side: THREE.BackSide,
});

const tunnel = new THREE.Mesh(tunnelGeometry, tunnelMaterial);
scene.add(tunnel);

const ringGroup = new THREE.Group();
scene.add(ringGroup);

const neonColors = [0x00f5ff, 0xff1fd2, 0xfff36b, 0x7bff6a];

for (let i = 0; i < 62; i += 1) {
  const z = -i * 3.85 - 2.5;
  const color = new THREE.Color(neonColors[i % neonColors.length]);
  const ringGeometry = new THREE.TorusGeometry(baseRadius - 0.08, i % 3 === 0 ? 0.055 : 0.035, 8, 96);
  const ringMaterial = new THREE.MeshBasicMaterial({
    color,
    transparent: true,
    opacity: i % 4 === 0 ? 0.98 : 0.72,
  });
  const ring = new THREE.Mesh(ringGeometry, ringMaterial);
  ring.position.z = z;
  ring.rotation.z = i * 0.31;
  ring.scale.setScalar(1 + Math.sin(i * 0.73) * 0.035);
  ringGroup.add(ring);
}

const panelGroup = new THREE.Group();
scene.add(panelGroup);

for (let i = 0; i < 84; i += 1) {
  const z = -i * 2.8 - 1.2;
  const theta = i * 1.74;
  const radius = baseRadius - 0.16;
  const color = new THREE.Color(neonColors[(i + 1) % neonColors.length]);
  const panelGeometry = new THREE.PlaneGeometry(0.42, 1.85);
  const panelMaterial = new THREE.MeshBasicMaterial({
    color,
    transparent: true,
    opacity: 0.82,
    side: THREE.DoubleSide,
  });
  const panel = new THREE.Mesh(panelGeometry, panelMaterial);
  panel.position.set(Math.cos(theta) * radius, Math.sin(theta) * radius, z);
  panel.lookAt(0, 0, z);
  panel.rotateZ(Math.PI / 2);
  panelGroup.add(panel);
}

const coreGlowGeometry = new THREE.CylinderGeometry(0.18, 0.18, tunnelLength, 18, 1, true);
const coreGlowMaterial = new THREE.MeshBasicMaterial({
  color: 0x10d7ff,
  transparent: true,
  opacity: 0.18,
  blending: THREE.AdditiveBlending,
  depthWrite: false,
});
const coreGlow = new THREE.Mesh(coreGlowGeometry, coreGlowMaterial);
coreGlow.rotation.x = Math.PI / 2;
coreGlow.position.z = -tunnelLength / 2;
scene.add(coreGlow);

const ambientLight = new THREE.AmbientLight(0x4c5cff, 0.35);
scene.add(ambientLight);

const pointLight = new THREE.PointLight(0x36e7ff, 38, 24, 1.4);
scene.add(pointLight);

const clock = new THREE.Clock();
const cameraRig = new THREE.Group();
scene.add(cameraRig);
cameraRig.add(camera);

function updateCamera(elapsed: number): void {
  const travel = (elapsed * 10.5) % (tunnelLength - 34);
  const z = -travel - 4;
  const swayX = Math.sin(elapsed * 0.93) * 0.62 + Math.sin(elapsed * 0.31) * 0.28;
  const swayY = Math.cos(elapsed * 0.77) * 0.48;
  const aheadZ = z - 9.5;
  const aheadX = Math.sin((elapsed + 0.7) * 0.93) * 0.62 + Math.sin((elapsed + 0.7) * 0.31) * 0.28;
  const aheadY = Math.cos((elapsed + 0.7) * 0.77) * 0.48;

  cameraRig.position.set(swayX, swayY, z);
  camera.lookAt(aheadX - swayX, aheadY - swayY, aheadZ - z);
  camera.rotation.z += Math.sin(elapsed * 0.55) * 0.08;
  pointLight.position.set(swayX, swayY, z - 2);
}

function animate(): void {
  const elapsed = clock.getElapsedTime();
  updateCamera(elapsed);

  ringGroup.children.forEach((child, index) => {
    const material = (child as THREE.Mesh).material as THREE.MeshBasicMaterial;
    material.opacity = 0.58 + Math.sin(elapsed * 3.5 + index * 0.67) * 0.22;
  });

  panelGroup.rotation.z = elapsed * 0.035;
  coreGlowMaterial.opacity = 0.14 + Math.sin(elapsed * 2.4) * 0.035;
  composer.render();
  requestAnimationFrame(animate);
}

window.addEventListener('resize', () => {
  camera.aspect = window.innerWidth / window.innerHeight;
  camera.updateProjectionMatrix();
  renderer.setSize(window.innerWidth, window.innerHeight);
  composer.setSize(window.innerWidth, window.innerHeight);
  bloomPass.setSize(window.innerWidth, window.innerHeight);
});

animate();

```
