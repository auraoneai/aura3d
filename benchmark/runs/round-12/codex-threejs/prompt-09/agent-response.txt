Implemented the procedural Three.js humanoid in `source/src/main.ts`. It uses primitive shapes for head, torso, arms, legs, hands, and feet, with a visible ground plane/path and a time-based walk cycle that moves the figure across the scene.

Build completed successfully.

Build command:
```bash
cd source && npm run build
```

Run command for the runner:
```bash
cd source && npm run dev
```

Assumptions: no assets are used, and runtime visual verification is left to the benchmark runner as required.