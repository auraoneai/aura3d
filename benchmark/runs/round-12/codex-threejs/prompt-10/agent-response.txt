Implemented the Three.js product viewer in [source/src/main.ts](/Users/gurbakshchahal/aura3d/benchmark/runs/round-12/codex-threejs/prompt-10/source/src/main.ts). It uses only `benchmark/assets/sneaker.glb`, loads it via `GLTFLoader`, centers and auto-scales it, places it on a plinth, adds studio lighting/shadows, orbit controls, and turntable rotation.

Build completed successfully.

Build command:
```bash
cd source && npm run build
```

Run command for the runner:
```bash
cd source && npm run dev
```

Assumptions: the benchmark runner will serve Vite from `source`, so the public asset is available at `benchmark/assets/sneaker.glb`.