Implemented the procedural Three.js solar system in `source/src/main.ts`.

Build completed successfully.

Build command:
```bash
cd source && npm run build
```

Run command for the benchmark runner:
```bash
cd source && npm run dev
```

Assumptions:
- Raw Three.js rules from `context/llms.txt` take precedence for this run.
- No recipe file existed at `context/docs/agents/benchmark-recipes.md`, so no recipe shape was copied.
- The app is fully procedural and uses no assets.