# Generated Source Listing

This file is generated by the Round 12 capture runner for neutral scoring.

## index.html

```
<!doctype html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Aura3D Round 12 Benchmark</title>
  </head>
  <body>
    <div id="app"></div>
    <script type="module" src="/src/main.ts"></script>
  </body>
</html>

```

## src/main.ts

```
import * as THREE from 'three';
import { OrbitControls } from 'three/addons/controls/OrbitControls.js';
import { CSS2DObject, CSS2DRenderer } from 'three/addons/renderers/CSS2DRenderer.js';
import { EffectComposer } from 'three/addons/postprocessing/EffectComposer.js';
import { RenderPass } from 'three/addons/postprocessing/RenderPass.js';
import { UnrealBloomPass } from 'three/addons/postprocessing/UnrealBloomPass.js';

type PlanetConfig = {
  name: string;
  radius: number;
  orbitRadius: number;
  speed: number;
  color: number;
  roughness: number;
  initialAngle: number;
};

type Planet = {
  config: PlanetConfig;
  pivot: THREE.Group;
  mesh: THREE.Mesh<THREE.SphereGeometry, THREE.MeshStandardMaterial>;
};

const root = document.querySelector<HTMLDivElement>('#app');

if (!root) {
  throw new Error('Missing #app element');
}

const style = document.createElement('style');
style.textContent = `
  html,
  body,
  #app {
    width: 100%;
    height: 100%;
    margin: 0;
    overflow: hidden;
    background: #02030a;
    color: #f6f1dd;
    font-family: Inter, ui-sans-serif, system-ui, -apple-system, BlinkMacSystemFont, "Segoe UI", sans-serif;
  }

  canvas {
    display: block;
  }

  .planet-label {
    padding: 4px 8px;
    border: 1px solid rgba(255, 255, 255, 0.35);
    border-radius: 4px;
    background: rgba(5, 8, 20, 0.78);
    box-shadow: 0 0 14px rgba(255, 222, 128, 0.18);
    color: #fff7d8;
    font-size: 12px;
    font-weight: 700;
    line-height: 1;
    letter-spacing: 0;
    text-shadow: 0 1px 3px #000;
    user-select: none;
    white-space: nowrap;
  }
`;
document.head.append(style);

const scene = new THREE.Scene();
scene.background = new THREE.Color(0x02030a);

const camera = new THREE.PerspectiveCamera(45, window.innerWidth / window.innerHeight, 0.1, 300);
camera.position.set(0, 42, 64);
camera.lookAt(0, 0, 0);

const renderer = new THREE.WebGLRenderer({ antialias: true });
renderer.setPixelRatio(Math.min(window.devicePixelRatio, 2));
renderer.setSize(window.innerWidth, window.innerHeight);
renderer.outputColorSpace = THREE.SRGBColorSpace;
renderer.toneMapping = THREE.ACESFilmicToneMapping;
renderer.toneMappingExposure = 1.2;
root.append(renderer.domElement);

const labelRenderer = new CSS2DRenderer();
labelRenderer.setSize(window.innerWidth, window.innerHeight);
labelRenderer.domElement.style.position = 'fixed';
labelRenderer.domElement.style.inset = '0';
labelRenderer.domElement.style.pointerEvents = 'none';
root.append(labelRenderer.domElement);

const controls = new OrbitControls(camera, renderer.domElement);
controls.enableDamping = true;
controls.target.set(0, 0, 0);
controls.minDistance = 20;
controls.maxDistance = 130;
controls.maxPolarAngle = Math.PI * 0.48;

const composer = new EffectComposer(renderer);
composer.addPass(new RenderPass(scene, camera));
const bloomPass = new UnrealBloomPass(
  new THREE.Vector2(window.innerWidth, window.innerHeight),
  1.85,
  0.6,
  0.08,
);
composer.addPass(bloomPass);

scene.add(new THREE.AmbientLight(0x22283f, 1.1));

const sunLight = new THREE.PointLight(0xffd27a, 760, 130, 1.15);
scene.add(sunLight);

const sun = new THREE.Mesh(
  new THREE.SphereGeometry(4.4, 96, 48),
  new THREE.MeshBasicMaterial({ color: 0xffb21f }),
);
sun.name = 'Procedural Sun';
scene.add(sun);

const sunHalo = new THREE.Mesh(
  new THREE.SphereGeometry(6.3, 96, 48),
  new THREE.MeshBasicMaterial({
    color: 0xff9f1a,
    transparent: true,
    opacity: 0.18,
    blending: THREE.AdditiveBlending,
    depthWrite: false,
  }),
);
scene.add(sunHalo);

const starGeometry = new THREE.BufferGeometry();
const starPositions: number[] = [];
for (let i = 0; i < 900; i += 1) {
  const radius = 80 + Math.random() * 80;
  const theta = Math.random() * Math.PI * 2;
  const phi = Math.acos(THREE.MathUtils.randFloatSpread(2));
  starPositions.push(
    radius * Math.sin(phi) * Math.cos(theta),
    radius * Math.cos(phi) * 0.65,
    radius * Math.sin(phi) * Math.sin(theta),
  );
}
starGeometry.setAttribute('position', new THREE.Float32BufferAttribute(starPositions, 3));
scene.add(
  new THREE.Points(
    starGeometry,
    new THREE.PointsMaterial({
      color: 0xffffff,
      size: 0.16,
      sizeAttenuation: true,
      transparent: true,
      opacity: 0.78,
    }),
  ),
);

const planetConfigs: PlanetConfig[] = [
  { name: 'Aster', radius: 0.9, orbitRadius: 9, speed: 0.95, color: 0xc9a46b, roughness: 0.72, initialAngle: 0.25 },
  { name: 'Borealis', radius: 1.25, orbitRadius: 14, speed: 0.68, color: 0x58b4d8, roughness: 0.48, initialAngle: 1.25 },
  { name: 'Cyra', radius: 1.05, orbitRadius: 20, speed: 0.48, color: 0x6fd07f, roughness: 0.62, initialAngle: 2.35 },
  { name: 'Dione', radius: 1.65, orbitRadius: 27, speed: 0.34, color: 0xd97855, roughness: 0.58, initialAngle: 3.2 },
  { name: 'Erebus', radius: 1.45, orbitRadius: 35, speed: 0.23, color: 0x8d72d9, roughness: 0.54, initialAngle: 4.3 },
  { name: 'Frost', radius: 1.9, orbitRadius: 44, speed: 0.16, color: 0xaed8ff, roughness: 0.38, initialAngle: 5.4 },
];

const orbitMaterial = new THREE.LineBasicMaterial({
  color: 0x6d728a,
  transparent: true,
  opacity: 0.34,
});

function createOrbitRing(radius: number): THREE.LineLoop {
  const segments = 160;
  const points: THREE.Vector3[] = [];
  for (let i = 0; i < segments; i += 1) {
    const angle = (i / segments) * Math.PI * 2;
    points.push(new THREE.Vector3(Math.cos(angle) * radius, 0, Math.sin(angle) * radius));
  }

  return new THREE.LineLoop(new THREE.BufferGeometry().setFromPoints(points), orbitMaterial);
}

function createPlanet(config: PlanetConfig): Planet {
  const pivot = new THREE.Group();
  pivot.rotation.y = config.initialAngle;
  scene.add(pivot);

  const mesh = new THREE.Mesh(
    new THREE.SphereGeometry(config.radius, 48, 32),
    new THREE.MeshStandardMaterial({
      color: config.color,
      roughness: config.roughness,
      metalness: 0.04,
    }),
  );
  mesh.position.set(config.orbitRadius, 0, 0);
  mesh.castShadow = false;
  mesh.receiveShadow = false;
  pivot.add(mesh);

  const labelElement = document.createElement('div');
  labelElement.className = 'planet-label';
  labelElement.textContent = config.name;
  const label = new CSS2DObject(labelElement);
  label.position.set(0, config.radius + 0.9, 0);
  mesh.add(label);

  scene.add(createOrbitRing(config.orbitRadius));

  return { config, pivot, mesh };
}

const planets = planetConfigs.map(createPlanet);
const clock = new THREE.Clock();

function animate(): void {
  requestAnimationFrame(animate);

  const elapsed = clock.getElapsedTime();
  sun.rotation.y = elapsed * 0.18;
  sunHalo.scale.setScalar(1 + Math.sin(elapsed * 1.7) * 0.035);

  for (const planet of planets) {
    planet.pivot.rotation.y = planet.config.initialAngle + elapsed * planet.config.speed;
    planet.mesh.rotation.y = elapsed * (1.1 + planet.config.speed);
  }

  controls.update();
  composer.render();
  labelRenderer.render(scene, camera);
}

function resize(): void {
  const width = window.innerWidth;
  const height = window.innerHeight;

  camera.aspect = width / height;
  camera.updateProjectionMatrix();

  renderer.setSize(width, height);
  composer.setSize(width, height);
  labelRenderer.setSize(width, height);
  bloomPass.setSize(width, height);
}

window.addEventListener('resize', resize);
animate();

```
