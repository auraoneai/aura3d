# Generated Source Listing

This file is generated by the Round 12 capture runner for neutral scoring.

## index.html

```
<!doctype html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Aura3D Round 12 Benchmark</title>
  </head>
  <body>
    <div id="app"></div>
    <script type="module" src="/src/main.ts"></script>
  </body>
</html>

```

## src/main.ts

```
import * as THREE from 'three';
import { OrbitControls } from 'three/addons/controls/OrbitControls.js';
import { RoomEnvironment } from 'three/addons/environments/RoomEnvironment.js';

const app = document.querySelector<HTMLDivElement>('#app');

if (!app) {
  throw new Error('Missing #app root element');
}

document.body.style.margin = '0';
document.body.style.overflow = 'hidden';
document.body.style.background = '#101216';
app.style.width = '100vw';
app.style.height = '100vh';

const renderer = new THREE.WebGLRenderer({ antialias: true });
renderer.setPixelRatio(Math.min(window.devicePixelRatio, 2));
renderer.setSize(window.innerWidth, window.innerHeight);
renderer.outputColorSpace = THREE.SRGBColorSpace;
renderer.toneMapping = THREE.ACESFilmicToneMapping;
renderer.toneMappingExposure = 1.15;
renderer.shadowMap.enabled = true;
renderer.shadowMap.type = THREE.PCFSoftShadowMap;
app.appendChild(renderer.domElement);

const scene = new THREE.Scene();
scene.background = new THREE.Color(0x11151d);

const pmremGenerator = new THREE.PMREMGenerator(renderer);
scene.environment = pmremGenerator.fromScene(new RoomEnvironment(renderer), 0.04).texture;

const camera = new THREE.PerspectiveCamera(
  45,
  window.innerWidth / window.innerHeight,
  0.1,
  100,
);
camera.position.set(0, 3.1, 8.8);

const controls = new OrbitControls(camera, renderer.domElement);
controls.enableDamping = true;
controls.target.set(0, 0.85, 0);
controls.minDistance = 4.5;
controls.maxDistance = 13;
controls.maxPolarAngle = Math.PI * 0.48;
controls.update();

const studio = new THREE.Group();
scene.add(studio);

const floorMaterial = new THREE.MeshStandardMaterial({
  color: 0x8c9297,
  roughness: 0.55,
  metalness: 0.0,
});

const floor = new THREE.Mesh(new THREE.PlaneGeometry(13, 8), floorMaterial);
floor.rotation.x = -Math.PI / 2;
floor.receiveShadow = true;
studio.add(floor);

const backWall = new THREE.Mesh(
  new THREE.PlaneGeometry(13, 5.5),
  new THREE.MeshStandardMaterial({ color: 0x5c626a, roughness: 0.62 }),
);
backWall.position.set(0, 2.65, -3.15);
backWall.receiveShadow = true;
studio.add(backWall);

const sidePanelMaterial = new THREE.MeshBasicMaterial({
  color: 0xffffff,
  toneMapped: false,
});

function addVisibleLightPanel(
  name: string,
  position: THREE.Vector3,
  scale: THREE.Vector3,
  intensity: number,
): void {
  const panel = new THREE.Mesh(new THREE.BoxGeometry(1, 1, 0.03), sidePanelMaterial);
  panel.name = name;
  panel.position.copy(position);
  panel.scale.copy(scale);
  panel.lookAt(0, 1, 0);
  studio.add(panel);

  const light = new THREE.RectAreaLight(0xffffff, intensity, scale.x, scale.y);
  light.position.copy(position);
  light.lookAt(0, 1, 0);
  studio.add(light);
}

addVisibleLightPanel('visible softbox key light', new THREE.Vector3(-4.8, 3.4, 1.8), new THREE.Vector3(1.8, 2.4, 1), 4.6);
addVisibleLightPanel('visible strip rim light', new THREE.Vector3(4.9, 2.8, -0.8), new THREE.Vector3(1.1, 2.8, 1), 2.8);
addVisibleLightPanel('visible overhead studio light', new THREE.Vector3(0, 4.8, 1.2), new THREE.Vector3(3.4, 0.75, 1), 2.2);

const fill = new THREE.HemisphereLight(0xd8e4ff, 0x3c342d, 1.4);
scene.add(fill);

const spot = new THREE.SpotLight(0xffffff, 220, 11, Math.PI / 5, 0.4, 1.1);
spot.position.set(-3.6, 5.2, 3.4);
spot.target.position.set(0, 0.65, -0.2);
spot.castShadow = true;
spot.shadow.mapSize.set(2048, 2048);
spot.shadow.bias = -0.00008;
scene.add(spot, spot.target);

const sphereGeometry = new THREE.SphereGeometry(0.72, 96, 64);
const pedestalGeometry = new THREE.CylinderGeometry(0.88, 0.98, 0.18, 48);
const labelGeometry = new THREE.BoxGeometry(1.15, 0.04, 0.08);

const materialStations = [
  {
    name: 'metal',
    x: -4,
    material: new THREE.MeshPhysicalMaterial({
      color: 0xd9dce0,
      metalness: 1,
      roughness: 0.16,
      envMapIntensity: 1.8,
    }),
    accent: 0xd9dce0,
  },
  {
    name: 'glass',
    x: -2,
    material: new THREE.MeshPhysicalMaterial({
      color: 0xbfe8ff,
      metalness: 0,
      roughness: 0.02,
      transmission: 1,
      thickness: 0.6,
      ior: 1.48,
      transparent: true,
      opacity: 0.45,
      envMapIntensity: 2.1,
    }),
    accent: 0x94dfff,
  },
  {
    name: 'rubber',
    x: 0,
    material: new THREE.MeshStandardMaterial({
      color: 0x121212,
      roughness: 0.92,
      metalness: 0,
    }),
    accent: 0x2b2b2b,
  },
  {
    name: 'emissive',
    x: 2,
    material: new THREE.MeshStandardMaterial({
      color: 0x30150a,
      roughness: 0.3,
      metalness: 0,
      emissive: 0xff6a00,
      emissiveIntensity: 2.7,
    }),
    accent: 0xff7a1a,
    glow: true,
  },
  {
    name: 'clearcoat',
    x: 4,
    material: new THREE.MeshPhysicalMaterial({
      color: 0xbe123c,
      roughness: 0.34,
      metalness: 0,
      clearcoat: 1,
      clearcoatRoughness: 0.035,
      envMapIntensity: 1.7,
    }),
    accent: 0xf43f5e,
  },
];

for (const station of materialStations) {
  const pedestal = new THREE.Mesh(
    pedestalGeometry,
    new THREE.MeshStandardMaterial({ color: 0x2a3038, roughness: 0.44, metalness: 0.12 }),
  );
  pedestal.position.set(station.x, 0.09, 0);
  pedestal.castShadow = true;
  pedestal.receiveShadow = true;
  studio.add(pedestal);

  const sphere = new THREE.Mesh(sphereGeometry, station.material);
  sphere.name = `${station.name} material sphere`;
  sphere.position.set(station.x, 0.95, 0);
  sphere.castShadow = true;
  sphere.receiveShadow = true;
  studio.add(sphere);

  const accent = new THREE.Mesh(
    labelGeometry,
    new THREE.MeshBasicMaterial({ color: station.accent, toneMapped: false }),
  );
  accent.position.set(station.x, 0.23, 0.92);
  studio.add(accent);

  if (station.glow) {
    const glowLight = new THREE.PointLight(0xff7a1a, 13, 3.2, 1.8);
    glowLight.position.copy(sphere.position);
    scene.add(glowLight);
  }
}

const reflectionStrips = new THREE.Group();
const stripMaterial = new THREE.MeshBasicMaterial({ color: 0xffffff, toneMapped: false });
for (const [x, y, z, sx, sy] of [
  [-5.6, 1.8, -3.08, 0.05, 1.8],
  [5.6, 1.8, -3.08, 0.05, 1.8],
  [0, 4.05, -3.08, 4.4, 0.06],
] as const) {
  const strip = new THREE.Mesh(new THREE.PlaneGeometry(1, 1), stripMaterial);
  strip.position.set(x, y, z);
  strip.scale.set(sx, sy, 1);
  reflectionStrips.add(strip);
}
studio.add(reflectionStrips);

function animate(): void {
  controls.update();
  renderer.render(scene, camera);
  requestAnimationFrame(animate);
}

animate();

window.addEventListener('resize', () => {
  camera.aspect = window.innerWidth / window.innerHeight;
  camera.updateProjectionMatrix();
  renderer.setSize(window.innerWidth, window.innerHeight);
});

```
