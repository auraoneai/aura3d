Implemented the procedural city block in `source/src/main.ts`.

Build completed successfully.

Build command:
```bash
cd source && npm run build
```

Run command for the runner:
```bash
cd source && npm run dev
```

Assumptions:
- The editable source directory is `source/`.
- No assets are required or used.
- The missing `context/docs/agents/benchmark-recipes.md` means there was no matching recipe to copy.