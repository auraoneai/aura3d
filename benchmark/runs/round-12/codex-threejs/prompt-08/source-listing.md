# Generated Source Listing

This file is generated by the Round 12 capture runner for neutral scoring.

## index.html

```
<!doctype html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Aura3D Round 12 Benchmark</title>
  </head>
  <body>
    <div id="app"></div>
    <script type="module" src="/src/main.ts"></script>
  </body>
</html>

```

## src/main.ts

```
import * as THREE from 'three';
import { OrbitControls } from 'three/addons/controls/OrbitControls.js';

const app = document.querySelector<HTMLDivElement>('#app');

if (!app) {
  throw new Error('Missing #app element');
}

document.body.style.margin = '0';
document.body.style.overflow = 'hidden';
document.body.style.fontFamily =
  'Inter, ui-sans-serif, system-ui, -apple-system, BlinkMacSystemFont, "Segoe UI", sans-serif';

app.style.width = '100vw';
app.style.height = '100vh';

const renderer = new THREE.WebGLRenderer({ antialias: true });
renderer.setPixelRatio(Math.min(window.devicePixelRatio, 2));
renderer.setSize(window.innerWidth, window.innerHeight);
renderer.shadowMap.enabled = true;
renderer.shadowMap.type = THREE.PCFSoftShadowMap;
renderer.outputColorSpace = THREE.SRGBColorSpace;
app.appendChild(renderer.domElement);

const scene = new THREE.Scene();
scene.background = new THREE.Color(0x8fc9ff);
scene.fog = new THREE.Fog(0x8fc9ff, 70, 190);

const camera = new THREE.PerspectiveCamera(
  50,
  window.innerWidth / window.innerHeight,
  0.1,
  300,
);
camera.position.set(48, 46, 58);

const controls = new OrbitControls(camera, renderer.domElement);
controls.target.set(0, 6, 0);
controls.enableDamping = true;
controls.maxPolarAngle = Math.PI * 0.47;
controls.minDistance = 34;
controls.maxDistance = 120;

const sunlight = new THREE.DirectionalLight(0xffffff, 3.2);
sunlight.position.set(-26, 54, 20);
sunlight.castShadow = true;
sunlight.shadow.mapSize.set(2048, 2048);
sunlight.shadow.camera.left = -80;
sunlight.shadow.camera.right = 80;
sunlight.shadow.camera.top = 80;
sunlight.shadow.camera.bottom = -80;
scene.add(sunlight);

const ambient = new THREE.HemisphereLight(0xb9ddff, 0x404045, 1.35);
scene.add(ambient);

const ground = new THREE.Mesh(
  new THREE.PlaneGeometry(130, 130),
  new THREE.MeshStandardMaterial({ color: 0x4f6f58, roughness: 0.9 }),
);
ground.rotation.x = -Math.PI / 2;
ground.receiveShadow = true;
scene.add(ground);

const roadMaterial = new THREE.MeshStandardMaterial({
  color: 0x20242a,
  roughness: 0.82,
  metalness: 0.05,
});
const lineMaterial = new THREE.MeshStandardMaterial({
  color: 0xf5d662,
  roughness: 0.5,
  emissive: 0x201600,
});

function addRoad(width: number, length: number, x: number, z: number, rotation = 0) {
  const road = new THREE.Mesh(new THREE.BoxGeometry(width, 0.08, length), roadMaterial);
  road.position.set(x, 0.05, z);
  road.rotation.y = rotation;
  road.receiveShadow = true;
  scene.add(road);
}

addRoad(12, 126, 0, 0);
addRoad(12, 126, -34, 0);
addRoad(12, 126, 34, 0);
addRoad(12, 126, 0, -30, Math.PI / 2);
addRoad(12, 126, 0, 30, Math.PI / 2);

for (let i = -58; i <= 58; i += 8) {
  const lineA = new THREE.Mesh(new THREE.BoxGeometry(0.34, 0.1, 3.6), lineMaterial);
  lineA.position.set(0, 0.12, i);
  scene.add(lineA);

  const lineB = new THREE.Mesh(new THREE.BoxGeometry(3.6, 0.1, 0.34), lineMaterial);
  lineB.position.set(i, 0.12, 30);
  scene.add(lineB);
}

const sidewalkMaterial = new THREE.MeshStandardMaterial({
  color: 0x8b8d8f,
  roughness: 0.92,
});

for (const x of [-48, -20, 20, 48]) {
  for (const z of [-46, -14, 14, 46]) {
    const block = new THREE.Mesh(new THREE.BoxGeometry(20, 0.35, 18), sidewalkMaterial);
    block.position.set(x, 0.19, z);
    block.receiveShadow = true;
    scene.add(block);
  }
}

const buildingPalette = [0x6d7682, 0x9a8f82, 0x536474, 0x817d76, 0x6e6a86];
const windowDay = new THREE.Color(0x7db6db);
const windowNight = new THREE.Color(0xffd86b);
const windows: THREE.Mesh<THREE.PlaneGeometry, THREE.MeshStandardMaterial>[] = [];

const buildingPositions = [
  [-50, -47], [-36, -47], [-18, -47], [18, -47], [36, -47],
  [50, -47], [-50, -15], [-18, -15], [18, -15], [50, -15],
  [-50, 15], [-18, 15], [18, 15], [50, 15], [-50, 47],
  [-36, 47], [-18, 47], [18, 47], [36, 47], [50, 47],
] as const;

buildingPositions.forEach(([x, z], index) => {
  const width = 7 + (index % 3) * 1.25;
  const depth = 7.5 + ((index + 1) % 4) * 0.9;
  const height = 7 + ((index * 7) % 22);
  const building = new THREE.Mesh(
    new THREE.BoxGeometry(width, height, depth),
    new THREE.MeshStandardMaterial({
      color: buildingPalette[index % buildingPalette.length],
      roughness: 0.78,
      metalness: 0.08,
    }),
  );
  building.position.set(x, height / 2 + 0.35, z);
  building.castShadow = true;
  building.receiveShadow = true;
  scene.add(building);

  const rows = Math.max(2, Math.floor(height / 3));
  const columns = Math.max(2, Math.floor(width / 2.1));
  const windowGeometry = new THREE.PlaneGeometry(0.58, 0.82);

  for (let row = 0; row < rows; row += 1) {
    for (let column = 0; column < columns; column += 1) {
      if ((row + column + index) % 5 === 0) {
        continue;
      }

      const localX = (column - (columns - 1) / 2) * 1.55;
      const localY = 2.2 + row * 2.45;

      for (const side of [-1, 1]) {
        const windowMesh = new THREE.Mesh(
          windowGeometry,
          new THREE.MeshStandardMaterial({
            color: windowDay,
            emissive: 0x0d2638,
            emissiveIntensity: 0.08,
            roughness: 0.2,
          }),
        );
        windowMesh.position.set(x + localX, 0.35 + localY, z + side * (depth / 2 + 0.012));
        windowMesh.rotation.y = side < 0 ? Math.PI : 0;
        windows.push(windowMesh);
        scene.add(windowMesh);
      }
    }
  }
});

const lampLights: THREE.PointLight[] = [];
const lampMaterial = new THREE.MeshStandardMaterial({ color: 0x15181b, roughness: 0.55 });
const bulbMaterial = new THREE.MeshStandardMaterial({
  color: 0xfff2bf,
  emissive: 0xffcc55,
  emissiveIntensity: 0.15,
});

function addStreetLight(x: number, z: number) {
  const pole = new THREE.Mesh(new THREE.CylinderGeometry(0.12, 0.16, 5.2, 14), lampMaterial);
  pole.position.set(x, 2.75, z);
  pole.castShadow = true;
  scene.add(pole);

  const arm = new THREE.Mesh(new THREE.BoxGeometry(1.4, 0.14, 0.14), lampMaterial);
  arm.position.set(x + (x < 0 ? 0.55 : -0.55), 5.25, z);
  arm.castShadow = true;
  scene.add(arm);

  const bulb = new THREE.Mesh(new THREE.SphereGeometry(0.32, 18, 12), bulbMaterial);
  bulb.position.set(x + (x < 0 ? 1.22 : -1.22), 4.95, z);
  scene.add(bulb);

  const light = new THREE.PointLight(0xffc55a, 0.25, 19, 2.2);
  light.position.copy(bulb.position);
  lampLights.push(light);
  scene.add(light);
}

for (const z of [-52, -38, -22, -8, 8, 22, 38, 52]) {
  addStreetLight(-7.2, z);
  addStreetLight(7.2, z);
}

for (const x of [-54, -40, -24, -10, 10, 24, 40, 54]) {
  addStreetLight(x, 22.8);
  addStreetLight(x, 37.2);
}

const ui = document.createElement('div');
ui.style.position = 'fixed';
ui.style.top = '18px';
ui.style.left = '18px';
ui.style.display = 'flex';
ui.style.alignItems = 'center';
ui.style.gap = '12px';
ui.style.padding = '10px 12px';
ui.style.border = '1px solid rgba(255,255,255,0.45)';
ui.style.background = 'rgba(12, 17, 24, 0.68)';
ui.style.color = '#ffffff';
ui.style.backdropFilter = 'blur(10px)';
ui.style.borderRadius = '8px';
ui.style.boxShadow = '0 8px 24px rgba(0,0,0,0.22)';
ui.style.zIndex = '10';

const label = document.createElement('span');
label.textContent = 'City lighting';
label.style.fontSize = '14px';
label.style.fontWeight = '700';

const toggle = document.createElement('button');
toggle.type = 'button';
toggle.textContent = 'Night';
toggle.setAttribute('aria-pressed', 'false');
toggle.style.border = '0';
toggle.style.borderRadius = '6px';
toggle.style.padding = '8px 12px';
toggle.style.minWidth = '76px';
toggle.style.color = '#101318';
toggle.style.background = '#ffd166';
toggle.style.fontWeight = '800';
toggle.style.cursor = 'pointer';

ui.append(label, toggle);
document.body.appendChild(ui);

let isNight = false;

function setMode(nextNight: boolean) {
  isNight = nextNight;
  const skyColor = isNight ? 0x07111f : 0x8fc9ff;
  scene.background = new THREE.Color(skyColor);
  scene.fog = new THREE.Fog(skyColor, isNight ? 52 : 70, isNight ? 155 : 190);
  sunlight.intensity = isNight ? 0.18 : 3.2;
  ambient.intensity = isNight ? 0.55 : 1.35;
  ambient.color.setHex(isNight ? 0x243b68 : 0xb9ddff);
  ambient.groundColor.setHex(isNight ? 0x08090e : 0x404045);
  lampLights.forEach((light) => {
    light.intensity = isNight ? 2.6 : 0.25;
  });
  bulbMaterial.emissiveIntensity = isNight ? 1.8 : 0.15;
  windows.forEach((windowMesh, index) => {
    windowMesh.material.color.copy(isNight ? windowNight : windowDay);
    windowMesh.material.emissive.setHex(isNight ? 0xffb640 : 0x0d2638);
    windowMesh.material.emissiveIntensity = isNight ? (index % 4 === 0 ? 0.35 : 1.25) : 0.08;
  });
  toggle.textContent = isNight ? 'Day' : 'Night';
  toggle.setAttribute('aria-pressed', String(isNight));
  toggle.style.background = isNight ? '#9ec5ff' : '#ffd166';
}

toggle.addEventListener('click', () => setMode(!isNight));
setMode(false);

window.addEventListener('resize', () => {
  camera.aspect = window.innerWidth / window.innerHeight;
  camera.updateProjectionMatrix();
  renderer.setSize(window.innerWidth, window.innerHeight);
});

renderer.setAnimationLoop(() => {
  controls.update();
  renderer.render(scene, camera);
});

```
