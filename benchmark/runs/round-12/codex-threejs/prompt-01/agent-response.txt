Implemented the app in `source/src/main.ts`, then ran the required finite commands.

Build result: failed during TypeScript typecheck.

Error:
```text
src/main.ts(124,45): error TS2552: Cannot find name 'HTMLStrongElement'. Did you mean 'HTMLButtonElement'?
```

Build command:
```bash
cd source && npm run build
```

Run command for the runner:
```bash
cd source && npm run dev
```

Assumption: raw Three.js rules from `context/llms.txt` override the Aura3D note, so the implementation uses procedural Three.js only and no external assets.