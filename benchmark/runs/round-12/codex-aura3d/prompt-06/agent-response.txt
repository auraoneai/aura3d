Implemented the mini-golf app in [source/src/main.ts](/Users/gurbakshchahal/aura3d/benchmark/runs/round-12/codex-aura3d/prompt-06/source/src/main.ts:1).

Build command run:

```bash
cd source && npm run build
```

Build result: failed at typecheck because `@aura3d/engine` could not be resolved:

```text
src/main.ts(10,8): error TS2307: Cannot find module '@aura3d/engine' or its corresponding type declarations.
```

Run command for the runner later:

```bash
cd source && npm run dev
```

Assumption: the runner will install/resolve the local `@aura3d/engine` package dependency before running build/dev.