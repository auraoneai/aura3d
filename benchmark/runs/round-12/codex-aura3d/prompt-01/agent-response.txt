Implemented the app in `source/src/main.ts` with `prefabs.physicsPlayground({ cubes: 50 })`, orbit camera, studio lighting, reset button, and a visible live contact count overlay. Added overlay styling in `source/src/style.css`.

Build command run:

```bash
npm run build
```

Result: failed at typecheck because dependencies are not installed/resolved:

```text
src/main.ts(1,81): error TS2307: Cannot find module '@aura3d/engine' or its corresponding type declarations.
```

Run command for the runner later:

```bash
npm run dev
```

Assumption: the runner will run `npm install` in `source/` before building/running so the local `@aura3d/engine` package dependency is available.