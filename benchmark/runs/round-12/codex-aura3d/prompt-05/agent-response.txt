Implemented Prompt 05 in `source/src/main.ts` using the Aura3D `prefabs.dataBars3D({ grid: 6 })` recipe with orbit camera, studio lighting, visible overlay notes, and hover-highlight guidance.

Build completed successfully.

Build command:
```bash
cd source && npm run build
```

Run command for runner:
```bash
cd source && npm run dev
```

Assumptions:
- Hover highlighting is provided by the Aura3D data bars prefab’s built-in hover metadata/interaction behavior.
- No external assets are required or used.