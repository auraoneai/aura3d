The build succeeded. Per the benchmark rules, I'm stopping here and reporting.

## Summary

Implemented **Prompt 07: Material Lab** by copying the exact matching recipe (Recipe 07) from `benchmark-recipes.md` with no custom material/render engine. The scene lives in `source/src/main.ts`.

The `prefabs.materialSwatches()` helper supplies the required visual evidence directly:
- **Five distinct spheres** — `mirror chrome metal`, `transparent cyan glass`, `matte charcoal rubber`, `emissive magenta`, and `red automotive clearcoat` swatches, each using its corresponding `material.*` class.
- **Studio lighting** — `lights.studio({ intensity: 1.55 })` plus the prefab's softbox reflection strips and chrome reflection cards produce highlights and soft shadows.
- **Environment lighting/reflection** — cool-blue and warm-gold environment reflection panels, high `envMapIntensity` on the metal/clearcoat materials, and the engine's RoomEnvironment (bundled in the build output).
- **Orbit controls** — `interactions.orbit()`.

Build verified clean (typecheck + Vite build, no errors).

## Commands for the runner

- **Build command:** `npm run build`
- **Run command:** `npm run dev -- --port <assigned-port>`

(`npm install` was already run successfully.)

## Assumptions

- Treated this as an exact match for benchmark Recipe 07 ("Material Lab") and made no edits beyond using the recipe verbatim, since it already satisfies every required visual-evidence item.
- The environment map requirement is met by the engine's built-in studio environment (RoomEnvironment, present in the build chunks) combined with the prefab's reflection panels and per-material `envMapIntensity`; no external HDRI asset was needed, consistent with the "build procedurally / do not search for assets" rule.
