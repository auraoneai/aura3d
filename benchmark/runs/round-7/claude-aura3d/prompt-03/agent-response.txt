Build succeeded (typecheck + vite build, clean). Stopping here per benchmark rules.

## Summary

Built **Prompt 03: Procedural Solar System** by copying recipe 03 from `benchmark-recipes.md` (the matching recipe family), with no custom engine code. The scene uses the public `prefabs.solarSystem()` helper, which procedurally generates the sun, 6 planets at distinct orbit radii/speeds, orbit paths, label plinths, Saturn's ring, background stars, and sun bloom. An HTML overlay legend (`ui.html`) provides readable, color-coded planet labels, and `camera.perspective` frames the whole system.

**Build command**
```
npm install
npm run build
```

**Run command**
```
npm run preview
```
(or `npm run dev` for development — both serve on 127.0.0.1)

**Files changed:** only `source/src/main.ts` (within the source directory).

**Assumptions**
- The matching recipe (03 Procedural Solar System) is the intended shape; `prefabs.solarSystem()` takes no arguments and already satisfies all required visual evidence (one sun, 6 planets at visibly different orbital distances, per-planet label plinths plus the HTML legend, sun bloom via `effects.bloom` baked into the prefab, and whole-system framing).
- No assets needed — built fully procedurally as the prompt requires.
- Per benchmark rules, I did not run a dev server, Playwright, or screenshot verification; correctness was confirmed via `npm run build` (TypeScript typecheck + Vite production build both passed).
