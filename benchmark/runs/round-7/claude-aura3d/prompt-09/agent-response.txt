Build succeeded. Stopping here per benchmark rules.

## Summary

Built the **Animated Primitive Humanoid** app by copying Recipe 09 from `docs/agents/benchmark-recipes.md` (the matching recipe family), making no edits since the recipe already targets this exact prompt.

**Implementation** (`source/src/main.ts`): uses the public `prefabs.primitiveHumanoid()` helper — which provides connected primitive body parts (sphere head, cylinder torso, box limbs), face details, a ground plane, path cues, and a procedural `walk` animation clip — composed via `scene().addMany(...)`, with `lights.studio`, a `camera.perspective` framing, and `timeline.loop({ seconds: 4 })` to drive the walk cycle. I verified `prefabs.primitiveHumanoid` exists in the engine's `.d.ts` before writing.

**Build command:** `npm run build` (runs `tsc --noEmit` then `vite build`)

**Run command:** `npm run preview` (serves the built `dist/`), or `npm run dev` for development

**Assumptions:**
- The `prefabs.primitiveHumanoid()` prefab supplies the humanoid figure, ground plane, and procedural walk-cycle clip as documented in `llms.txt` and the recipe; no custom primitive placement or animation system was needed.
- `timeline.loop({ seconds: 4 })` drives the prefab's procedural walk animation so runtime evidence shows movement and a mid-stride (non-static) pose.
- Per benchmark rules, I ran only `npm install` and `npm run build`; no dev server, Playwright, or screenshot verification was run. Build completed successfully (8 modules transformed).
