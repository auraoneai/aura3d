The build succeeded (typecheck passed, Vite bundle produced). Stopping here per the benchmark rules.

## Summary

Built a particle fountain in `source/src/main.ts` using Aura3D's matching prefab (Recipe 02), with the prompt-required emission-rate control added.

**Implementation**
- `prefabs.particleFountain({ count })` supplies the gravity-arc particles, identifiable emitter, lifetime color variation, and ground-plane collision — no custom particle engine.
- A visible HUD range slider controls the emission rate, mapped to the prefab's `count` (400–4000). The label updates live on drag; on release the fountain rebuilds at the new rate (a discrete user action, not a per-frame `dispose`/recreate loop).
- The canvas mounts in a dedicated `#stage` container so rebuilds never disturb the HUD overlay.
- `lights.studio` + `camera.orbit` framing on the fountain, per the recipe.

**Build command:** `npm run build` (runs `tsc --noEmit` then `vite build`)

**Run command:** `npm run dev` (Vite dev server) — or `npm run preview` after a build.

**Assumptions**
- "Controllable emission rate" maps to the fountain's particle `count`, the only emission knob the prefab exposes; changes apply on slider release via `app.dispose()` + recreate (discrete event), avoiding the prohibited per-frame recreation.
- The `particleFountain` prefab is treated as providing the emitter, lifetime-colored arc, and collidable ground plane (per the Round 1 repair notes in `llms.txt`); no custom primitives were added.
- No assets are used (prompt is procedural-only).
