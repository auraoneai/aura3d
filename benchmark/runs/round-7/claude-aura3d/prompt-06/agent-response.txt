The build succeeded (typecheck passed, Vite bundle produced). Per the benchmark rules, I'm stopping here.

## Summary

Built **Prompt 06: Mini-Golf Hole** by copying the canonical **Recipe 06 (Mini Golf)** from `docs/agents/benchmark-recipes.md`, which both `llms.txt` and the README designate as the required shape for this prompt. Only the single file `source/src/main.ts` was modified.

The scene uses `prefabs.miniGolfHole()` (procedural green + obstacle + white physics golf ball), `interactions.pointer()` for click-to-aim-and-shoot, `camera.follow({ targetNode: "white physics golf ball" })` for the ball-following camera, and a `ui.html` strokes/score HUD — covering all required visual evidence.

**Build command**
```bash
npm install && npm run build
```

**Run command**
```bash
npm run preview   # or: npm run dev
```

**Assumptions**
- Treated the recipe-provided `strokes` HUD as the required "score counter" (strokes are the score in golf).
- Used `@aura3d/engine` public helpers/prefabs only — no custom physics, game, or animation engine, and no invented asset paths (prompt requires fully procedural; no assets used).
- Left `index.html`, `package.json`, and `tsconfig.json` (the provided scaffold) unchanged; all edits confined to the `source` directory.
- Per benchmark rules, I did not run `npm run dev`/`preview`, Playwright, or any screenshot/visual verification; verification was the finite `npm run build` (typecheck + Vite bundle), which passed.
