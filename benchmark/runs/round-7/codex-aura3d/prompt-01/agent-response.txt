Implemented the app in `./source` using the Prompt 01 Aura3D recipe shape:

- `src/main.ts`: physics playground scene with `prefabs.physicsPlayground({ cubes: 50 })`, orbit camera, studio lighting, reset button, and contact count overlay.
- `src/style.css`: visible HUD styling.

Build command:

```bash
npm run build
```

Run command:

```bash
npm run dev
```

Build result: failed at typecheck because `@aura3d/engine` was not installed/resolvable in `./source`:

```text
src/main.ts(1,81): error TS2307: Cannot find module '@aura3d/engine' or its corresponding type declarations.
```

Assumption: run `npm install` in `./source` before building/running so the local `.tgz` dependency in `package.json` is installed.