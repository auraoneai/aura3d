Implemented the Particle Fountain app in `./source`.

Build command:
`npm run build`

Run command:
`npm run dev`

Build result:
`npm run build` failed during typecheck with:

```text
src/main.ts(12,8): error TS2307: Cannot find module '@aura3d/engine' or its corresponding type declarations.
```

Assumptions:
- Used only procedural content, no assets.
- Followed the matching `prefabs.particleFountain(...)` benchmark recipe.
- Added a visible emitter, visible ground plane, lifetime color legend, and an emission-rate slider that remounts the scene with a different particle count on slider change.