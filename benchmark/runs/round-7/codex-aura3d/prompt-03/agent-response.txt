Implemented the procedural solar system in [source/src/main.ts](/Users/gurbakshchahal/aura3d/benchmark/runs/round-7/codex-aura3d/prompt-03/source/src/main.ts:1). It uses `prefabs.solarSystem()`, bloom, orbit interaction/camera, timeline animation, and visible labels for the six planets.

Build command:
```bash
cd source && npm run build
```

Run command:
```bash
cd source && npm run dev
```

Assumptions:
- `prefabs.solarSystem()` provides the procedural sun, six planets, visible orbit distances/speeds, and orbit paths per the Aura3D benchmark recipe.
- No external assets are required or used.

Verification: `npm install && npm run build` completed successfully.