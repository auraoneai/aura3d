Implemented the Material Lab in [source/src/main.ts](/Users/gurbakshchahal/aura3d/benchmark/runs/round-7/codex-aura3d/prompt-07/source/src/main.ts:1) using the matching Aura3D benchmark recipe.

Build result: failed at typecheck because `@aura3d/engine` could not be resolved.

Build command:
```bash
npm run build
```

Run command:
```bash
npm run dev
```

Assumptions:
- The provided `@aura3d/engine` package dependency is expected to be installable from the local tarball path in `package.json`.
- No assets are required; the scene is fully procedural via `prefabs.materialSwatches()`.