Implemented the neon tunnel flythrough in [source/src/main.ts](/Users/gurbakshchahal/aura3d/benchmark/runs/round-7/codex-aura3d/prompt-04/source/src/main.ts:1) using `prefabs.neonTunnel`, dolly camera animation, bloom, fog, and a looping timeline.

Build command:
```bash
npm run build
```

Run command:
```bash
npm run dev
```

Build result: failed at typecheck because `@aura3d/engine` is not installed/resolvable in `source`:
```text
src/main.ts(1,74): error TS2307: Cannot find module '@aura3d/engine' or its corresponding type declarations.
```

Assumptions: no assets are needed; the procedural tunnel prefab provides the tube interior and emissive segments.