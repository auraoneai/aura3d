Build succeeded — typecheck and vite build both passed (the chunk-size note is a benign warning).

## Summary

Built a three.js product viewer for the provided sneaker in `source/src/main.ts`. It satisfies all required visual evidence:

- **Loaded & visible** — `GLTFLoader` loads `/benchmark/assets/sneaker.glb` (the only allowed asset, already placed in `public/benchmark/assets/`, served by Vite at that path).
- **Centered & auto-scaled** — computes the model `Box3`, recenters on X/Z, rests its base on the plinth, and scales the largest dimension to a fixed `TARGET_SIZE` (1.4) so any model size is normalized.
- **Plinth/base** — a cylindrical plinth mesh under the sneaker, plus a soft studio floor to catch shadows.
- **Studio lighting** — `RoomEnvironment` (via `PMREMGenerator`) for readable PBR reflections, plus key/fill/rim directional lights and ambient, with ACES tone mapping and sRGB output.
- **Orbit controls + turntable** — `OrbitControls` with damping/distance/polar clamps, and a `Group` turntable that rotates continuously in the render loop.

## Commands

- **Build:** `npm install && npm run build` (run from `source/`)
- **Run:** `npm run dev` — then open the printed `http://127.0.0.1:5173` URL (or `npm run preview` after a build)

## Assumptions

- The `sneaker.glb` was already staged at `source/public/benchmark/assets/sneaker.glb`, so I referenced it via the Vite-served absolute path `/benchmark/assets/sneaker.glb` (no invented path, no asset discovery).
- This benchmark slot uses plain three.js `0.165.0` (addons via `three/addons/...`), not an Aura3D engine — there was no `llms.txt` or recipes file in the bundle, so I used three.js public addons (`OrbitControls`, `GLTFLoader`, `RoomEnvironment`) rather than custom primitives.
- Per instructions I did not run the dev/preview server; verification was limited to `npm install` and `npm run build`.
