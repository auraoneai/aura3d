Build succeeded. Per the benchmark rules, I'm stopping here.

## Summary

Built an animated primitive humanoid in `source/src/main.ts` using plain three.js (the bundle is three.js v0.165.0; no Aura3D engine, `llms.txt`, or recipes file were present in the context bundle).

**What it renders:**
- **Humanoid figure** from primitives: **sphere** head, **cylinder** torso (+ small cylinder neck), and **box** arms and legs.
- Limbs are mounted on pivot groups at the shoulders/hips so they swing about the joint — not a rigid stick figure.
- A visible **ground plane** (green `PlaneGeometry`) with a grid overlay so travel across it reads clearly, plus shadows.
- **Procedural walk cycle**: legs swing in opposition, arms counter-swing, body bobs vertically twice per stride and sways slightly. The figure travels back and forth across the plane and turns to face its direction of travel, keeping it in frame. At any captured frame the limbs are mid-swing, implying motion.

**Build command:**
```
npm install && npm run build
```
(run from `source/`)

**Run command:**
```
npm run dev
```
(serves at http://127.0.0.1:5173; `npm run preview` serves the production build)

**Assumptions:**
- The context bundle ships plain three.js with no Aura3D API, `llms.txt`, or `benchmark-recipes.md`, so I built directly against the `three` public ES module and used core primitives/helpers only.
- Added `@types/three@0.165.0` to `devDependencies` (matching the pinned `three` version) because the `build` script runs `tsc --noEmit` first and three.js ships no bundled type declarations — without it the typecheck would fail.
- No assets were used (prompt requires fully procedural construction).
- Did not run `npm run dev`/`preview` or any visual capture per the benchmark rules.
