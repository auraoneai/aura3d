Implemented the app in `./source`, but `npm run build` failed at typecheck.

Build command:
```bash
npm run build
```

Run command:
```bash
npm run dev
```

Build failure:
```text
src/main.ts(157,3): error TS18047: 'contactCount' is possibly 'null'.
src/main.ts(235,3): error TS18047: 'contactCount' is possibly 'null'.
```

Assumptions:
- `context/llms.txt` and `context/docs/agents/benchmark-recipes.md` were not present, so I used the available local Three.js context.
- No external assets were used; the scene is fully procedural.
- I stopped after the build failure per the benchmark execution rules.