Implemented the procedural city block app in `./source`.

Build command: `npm run build`
Run command: `npm run dev`

Verification: `npm install && npm run build` completed successfully.

Assumptions:
- The provided context bundle did not include `./context/llms.txt` or benchmark recipe docs, so I used the available local Three/Vite scaffold.
- No external assets were used; all buildings, windows, streets, street lights, sky, and lighting are procedural.