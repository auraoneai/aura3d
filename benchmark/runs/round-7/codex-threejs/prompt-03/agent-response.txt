Implemented the procedural solar system in [source/src/main.ts](/Users/gurbakshchahal/aura3d/benchmark/runs/round-7/codex-threejs/prompt-03/source/src/main.ts): sun with bloom/glow, six planets at distinct orbit distances and speeds, orbit rings, readable labels, starfield, and orbit camera framing.

Build result: `npm run build` passed. Vite emitted only a chunk-size warning.

Build command:
```bash
cd source && npm run build
```

Run command:
```bash
cd source && npm run dev
```

Assumptions:
- `./context/llms.txt` and `./context/docs/agents/benchmark-recipes.md` were not present in the provided bundle, so I used the available scaffold and bundled Three.js APIs.
- No assets were used; the scene is fully procedural.