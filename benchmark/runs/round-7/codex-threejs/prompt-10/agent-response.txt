Implemented the product viewer in `./source` and ran the build successfully.

Build command:
```bash
npm run build
```

Run command:
```bash
npm run dev
```

Assumptions:
- The allowed asset is served from `source/public/benchmark/assets/sneaker.glb` as `/benchmark/assets/sneaker.glb`.
- `context/llms.txt` and `context/docs/agents/benchmark-recipes.md` were not present in the provided context bundle, so I used the available Three.js scaffold/context only.
- No long-running dev server or visual verification was run, per the benchmark rules.