The build is complete and verified. Stopping here per benchmark rules.

## Summary

Built a product viewer for the provided sneaker by copying Recipe 10 (Product Viewer Sneaker) from `benchmark-recipes.md` with only prompt-required edits.

**What was done:**
- Ran the bundled Aura3D CLI: `assets add benchmark/assets/sneaker.glb --name sneaker` → produced the hashed `public/aura-assets/sneaker.e1d7cb19.glb` and typed `src/aura-assets.ts`. No invented paths; the typed `assets.sneaker` ref is used.
- Fixed one CLI-generated import (`../engine/index.js` → `@aura3d/engine`) so the typed module resolves through the public package.
- Wrote `src/main.ts` using `prefabs.productViewer(assets.sneaker)` (centered + auto-scaled model, plinth, contact shadow, softboxes, turntable cue) plus `lights.studio`, `interactions.orbit()`, `camera.product()`, and `timeline.loop` for turntable rotation.

This satisfies every required visual evidence item: model loaded/visible, centered & auto-scaled, plinth/base, studio lighting, orbit controls, and turntable rotation — all via public `@aura3d/engine` helpers and the typed asset.

**Build command:**
```bash
npm run build
```
(passes: `tsc --noEmit` typecheck + `vite build`; GLTFLoader and the hashed GLB are bundled into `dist/`)

**Run command (for the runner to execute later):**
```bash
npm run dev -- --port <assigned-port>
```

**Assumptions:**
- The provided `benchmark/assets/sneaker.glb` was the only asset used. The runner had also pre-copied it to `source/public/benchmark/assets/`, but the scene loads the typed CLI-hashed copy at `/aura-assets/sneaker.e1d7cb19.glb` (the allowed hashed URL), not that path.
- The Aura CLI ships inside `@aura3d/engine/dist/aura3d-cli/cli.js`, so I invoked it via `node` from the package rather than `npx @aura3d/cli@latest` (no network/asset discovery).
- `npm install` and `npm run build` were run as finite commands; no dev server, preview, Playwright, or screenshot capture was run in-process.
