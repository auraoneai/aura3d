The build succeeded. Per the benchmark rules, I'll stop here and report.

## Summary

Built **Prompt 09: Animated Primitive Humanoid** by copying benchmark recipe 09 with only prompt-required edits. The scene uses the public `character.primitiveHumanoid({ showJoints: true, motionTrail: true, clip: "walk", pose: "mid-stride" })` prefab — which supplies the connected sphere head, cylinder torso, box limbs, joint hinges, motion trail, and a `walk` animation clip — over a ground plane with studio lighting, a `camera.humanoid()` frame, and a 4-second `timeline.loop` for the walk cycle. `character.visualQA(...)` runs at startup to log limb-connection/proportion checks.

Verified the recipe against the installed engine's type definitions before writing (corrected `qa.passed` → the actual `connected`/`impossibleProportions`/`problems` fields). `npm run build` (typecheck + vite build) passes cleanly.

**Files changed:** `source/src/main.ts` only (no edits outside the source directory).

### Commands for the runner
- **Build command:** `npm run build`
- **Run command:** `npm run dev -- --port <assigned-port>`

### Assumptions
- The benchmark recipe 09 is the intended shape; no custom animation system was written since `character.primitiveHumanoid` exposes the required walk clip and connected primitives.
- A static screenshot of the looping scene will land on a `mid-stride` pose (per the `pose` option), satisfying the "implies animation, not a static stick figure" requirement.
- No assets were added — the prompt is fully procedural.
