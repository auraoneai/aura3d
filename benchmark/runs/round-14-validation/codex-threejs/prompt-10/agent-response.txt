Implemented the Three.js product viewer in [source/src/main.ts](/Users/gurbakshchahal/aura3d/benchmark/runs/round-14-validation/codex-threejs/prompt-10/source/src/main.ts). It loads only `/benchmark/assets/sneaker.glb`, centers and auto-scales the sneaker onto a visible plinth, adds studio-style lighting, enables orbit controls, and rotates the turntable.

Build completed successfully.

Build command:
```bash
cd source && npm run build
```

Run command for the runner:
```bash
cd source && npm run dev
```

Assumptions: the benchmark runner serves `source/public` at the Vite public root, so `source/public/benchmark/assets/sneaker.glb` is available as `/benchmark/assets/sneaker.glb`.