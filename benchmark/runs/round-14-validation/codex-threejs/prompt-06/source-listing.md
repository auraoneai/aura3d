# Generated Source Listing

This file is generated by the Round 14 Validation capture runner for neutral scoring.

## index.html

```
<!doctype html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Aura3D Round 14 Validation Benchmark</title>
  </head>
  <body>
    <div id="app"></div>
    <script type="module" src="/src/main.ts"></script>
  </body>
</html>

```

## src/main.ts

```
import * as THREE from 'three';

const app = document.querySelector<HTMLDivElement>('#app');

if (!app) {
  throw new Error('Missing #app container');
}

document.body.style.margin = '0';
document.body.style.overflow = 'hidden';
document.body.style.background = '#87b6d8';
app.style.width = '100vw';
app.style.height = '100vh';

const hud = document.createElement('div');
hud.style.position = 'fixed';
hud.style.left = '18px';
hud.style.top = '16px';
hud.style.padding = '12px 14px';
hud.style.border = '2px solid rgba(255,255,255,0.65)';
hud.style.borderRadius = '8px';
hud.style.background = 'rgba(17, 38, 26, 0.82)';
hud.style.color = '#f7fff0';
hud.style.font = '700 18px/1.35 system-ui, -apple-system, BlinkMacSystemFont, "Segoe UI", sans-serif';
hud.style.boxShadow = '0 8px 24px rgba(0,0,0,0.22)';
hud.style.userSelect = 'none';
hud.innerHTML = '<div id="score">Strokes: 0</div><div id="status" style="font-size:13px;font-weight:600;color:#cfeec8">Click and drag from the ball to aim</div>';
document.body.appendChild(hud);

const scoreEl = document.querySelector<HTMLDivElement>('#score');
const statusEl = document.querySelector<HTMLDivElement>('#status');

const scene = new THREE.Scene();
scene.background = new THREE.Color(0x8cc6e8);
scene.fog = new THREE.Fog(0x8cc6e8, 26, 58);

const camera = new THREE.PerspectiveCamera(48, window.innerWidth / window.innerHeight, 0.1, 120);
camera.position.set(0, 9, 12);

const renderer = new THREE.WebGLRenderer({ antialias: true });
renderer.setPixelRatio(Math.min(window.devicePixelRatio, 2));
renderer.setSize(window.innerWidth, window.innerHeight);
renderer.shadowMap.enabled = true;
renderer.shadowMap.type = THREE.PCFSoftShadowMap;
app.appendChild(renderer.domElement);

const hemi = new THREE.HemisphereLight(0xe8f7ff, 0x476d36, 2.6);
scene.add(hemi);

const sun = new THREE.DirectionalLight(0xffffff, 2.3);
sun.position.set(-7, 14, 8);
sun.castShadow = true;
sun.shadow.mapSize.set(2048, 2048);
sun.shadow.camera.left = -18;
sun.shadow.camera.right = 18;
sun.shadow.camera.top = 18;
sun.shadow.camera.bottom = -18;
scene.add(sun);

const greenGroup = new THREE.Group();
scene.add(greenGroup);

const greenMaterial = new THREE.MeshStandardMaterial({ color: 0x2fad54, roughness: 0.88 });
const fairway = new THREE.Mesh(new THREE.BoxGeometry(9.5, 0.22, 22), greenMaterial);
fairway.position.y = -0.11;
fairway.receiveShadow = true;
greenGroup.add(fairway);

const fringeMaterial = new THREE.MeshStandardMaterial({ color: 0x236f39, roughness: 0.9 });
const fringe = new THREE.Mesh(new THREE.BoxGeometry(11, 0.18, 23.5), fringeMaterial);
fringe.position.y = -0.2;
fringe.receiveShadow = true;
greenGroup.add(fringe);

const laneStripeMaterial = new THREE.MeshStandardMaterial({ color: 0x45bd68, roughness: 0.94 });
for (let i = -4; i <= 4; i += 2) {
  const stripe = new THREE.Mesh(new THREE.PlaneGeometry(1.1, 21.6), laneStripeMaterial);
  stripe.rotation.x = -Math.PI / 2;
  stripe.position.set(i, 0.012, 0);
  stripe.receiveShadow = true;
  greenGroup.add(stripe);
}

const railMaterial = new THREE.MeshStandardMaterial({ color: 0xe8d3a0, roughness: 0.72 });
const railGeometry = new THREE.BoxGeometry(0.38, 0.42, 22.7);
for (const x of [-5.2, 5.2]) {
  const rail = new THREE.Mesh(railGeometry, railMaterial);
  rail.position.set(x, 0.18, 0);
  rail.castShadow = true;
  rail.receiveShadow = true;
  greenGroup.add(rail);
}

for (const z of [-11.2, 11.2]) {
  const rail = new THREE.Mesh(new THREE.BoxGeometry(10.8, 0.42, 0.38), railMaterial);
  rail.position.set(0, 0.18, z);
  rail.castShadow = true;
  rail.receiveShadow = true;
  greenGroup.add(rail);
}

const obstacleMaterial = new THREE.MeshStandardMaterial({ color: 0xd45435, roughness: 0.58, metalness: 0.05 });
const obstacle = new THREE.Mesh(new THREE.BoxGeometry(4.3, 1.05, 1.15), obstacleMaterial);
obstacle.position.set(0, 0.53, 0.8);
obstacle.castShadow = true;
obstacle.receiveShadow = true;
scene.add(obstacle);

const obstacleTop = new THREE.Mesh(
  new THREE.CylinderGeometry(0.38, 0.38, 4.55, 24),
  new THREE.MeshStandardMaterial({ color: 0xf2c84b, roughness: 0.5 }),
);
obstacleTop.rotation.z = Math.PI / 2;
obstacleTop.position.set(0, 1.15, 0.8);
obstacleTop.castShadow = true;
scene.add(obstacleTop);

const cup = new THREE.Mesh(
  new THREE.CylinderGeometry(0.54, 0.54, 0.035, 48),
  new THREE.MeshStandardMaterial({ color: 0x101816, roughness: 1 }),
);
cup.position.set(0, 0.035, -8.4);
cup.receiveShadow = true;
scene.add(cup);

const flagPole = new THREE.Mesh(
  new THREE.CylinderGeometry(0.035, 0.035, 2.3, 16),
  new THREE.MeshStandardMaterial({ color: 0xf7f7f0, roughness: 0.4 }),
);
flagPole.position.set(0.38, 1.16, -8.4);
flagPole.castShadow = true;
scene.add(flagPole);

const flagShape = new THREE.Shape();
flagShape.moveTo(0, 0);
flagShape.lineTo(1.05, 0.25);
flagShape.lineTo(0, 0.5);
flagShape.lineTo(0, 0);
const flag = new THREE.Mesh(
  new THREE.ShapeGeometry(flagShape),
  new THREE.MeshStandardMaterial({ color: 0xfff15a, side: THREE.DoubleSide, roughness: 0.65 }),
);
flag.position.set(0.41, 2.02, -8.4);
flag.rotation.y = -0.45;
flag.castShadow = true;
scene.add(flag);

const ballRadius = 0.32;
const ball = new THREE.Mesh(
  new THREE.SphereGeometry(ballRadius, 48, 32),
  new THREE.MeshStandardMaterial({ color: 0xffffff, roughness: 0.38, metalness: 0.02 }),
);
ball.position.set(0, ballRadius, 8.3);
ball.castShadow = true;
scene.add(ball);

const ballMark = new THREE.Mesh(
  new THREE.TorusGeometry(ballRadius * 0.72, 0.012, 8, 32),
  new THREE.MeshStandardMaterial({ color: 0x57a5e6, roughness: 0.4 }),
);
ballMark.rotation.x = Math.PI / 2;
ball.add(ballMark);

const aimLine = new THREE.ArrowHelper(new THREE.Vector3(0, 0, -1), ball.position, 2, 0xfff15a, 0.45, 0.28);
aimLine.visible = true;
scene.add(aimLine);

const powerRing = new THREE.Mesh(
  new THREE.TorusGeometry(0.64, 0.025, 8, 64),
  new THREE.MeshBasicMaterial({ color: 0xfff15a }),
);
powerRing.rotation.x = Math.PI / 2;
powerRing.position.copy(ball.position);
powerRing.position.y = 0.045;
scene.add(powerRing);

const groundPlane = new THREE.Plane(new THREE.Vector3(0, 1, 0), 0);
const raycaster = new THREE.Raycaster();
const pointer = new THREE.Vector2();
const dragStart = new THREE.Vector3();
const dragPoint = new THREE.Vector3();
const velocity = new THREE.Vector3();
const clock = new THREE.Clock();

let isDragging = false;
let strokes = 0;
let holed = false;
let cameraTarget = new THREE.Vector3();

const playBounds = {
  minX: -4.55 + ballRadius,
  maxX: 4.55 - ballRadius,
  minZ: -10.55 + ballRadius,
  maxZ: 10.55 - ballRadius,
};

function setPointer(event: PointerEvent): void {
  pointer.x = (event.clientX / window.innerWidth) * 2 - 1;
  pointer.y = -(event.clientY / window.innerHeight) * 2 + 1;
}

function pointerToGreen(event: PointerEvent, target: THREE.Vector3): boolean {
  setPointer(event);
  raycaster.setFromCamera(pointer, camera);
  return raycaster.ray.intersectPlane(groundPlane, target) !== null;
}

function speed(): number {
  return Math.hypot(velocity.x, velocity.z);
}

function updateHud(message: string): void {
  if (scoreEl) scoreEl.textContent = `Strokes: ${strokes}`;
  if (statusEl) statusEl.textContent = message;
}

function updateAimVisual(direction: THREE.Vector3, power: number): void {
  const clampedPower = THREE.MathUtils.clamp(power, 0.1, 1);
  const arrowLength = 1.15 + clampedPower * 3.1;
  aimLine.position.copy(ball.position);
  aimLine.position.y = ballRadius + 0.08;
  aimLine.setDirection(direction);
  aimLine.setLength(arrowLength, 0.45, 0.28);
  aimLine.visible = true;
  powerRing.visible = true;
  powerRing.position.set(ball.position.x, 0.045, ball.position.z);
  powerRing.scale.setScalar(1 + clampedPower * 1.35);
}

function obstacleCollision(): void {
  const halfX = 2.15 + ballRadius;
  const halfZ = 0.575 + ballRadius;
  const dx = ball.position.x - obstacle.position.x;
  const dz = ball.position.z - obstacle.position.z;

  if (Math.abs(dx) >= halfX || Math.abs(dz) >= halfZ || ball.position.y > 1.2) {
    return;
  }

  const overlapX = halfX - Math.abs(dx);
  const overlapZ = halfZ - Math.abs(dz);

  if (overlapX < overlapZ) {
    ball.position.x = obstacle.position.x + Math.sign(dx || 1) * halfX;
    velocity.x *= -0.72;
    velocity.z *= 0.92;
  } else {
    ball.position.z = obstacle.position.z + Math.sign(dz || 1) * halfZ;
    velocity.z *= -0.72;
    velocity.x *= 0.92;
  }
}

function boundsCollision(): void {
  if (ball.position.x < playBounds.minX) {
    ball.position.x = playBounds.minX;
    velocity.x = Math.abs(velocity.x) * 0.78;
  } else if (ball.position.x > playBounds.maxX) {
    ball.position.x = playBounds.maxX;
    velocity.x = -Math.abs(velocity.x) * 0.78;
  }

  if (ball.position.z < playBounds.minZ) {
    ball.position.z = playBounds.minZ;
    velocity.z = Math.abs(velocity.z) * 0.78;
  } else if (ball.position.z > playBounds.maxZ) {
    ball.position.z = playBounds.maxZ;
    velocity.z = -Math.abs(velocity.z) * 0.78;
  }
}

renderer.domElement.addEventListener('pointerdown', (event) => {
  if (holed || speed() > 0.06) return;
  if (!pointerToGreen(event, dragStart)) return;
  isDragging = true;
  renderer.domElement.setPointerCapture(event.pointerId);
  updateHud('Release to shoot');
});

renderer.domElement.addEventListener('pointermove', (event) => {
  if (!isDragging || !pointerToGreen(event, dragPoint)) return;
  const shot = ball.position.clone().sub(dragPoint);
  shot.y = 0;
  const distance = shot.length();
  if (distance > 0.05) {
    updateAimVisual(shot.normalize(), distance / 4);
  }
});

renderer.domElement.addEventListener('pointerup', (event) => {
  if (!isDragging) return;
  isDragging = false;
  renderer.domElement.releasePointerCapture(event.pointerId);

  if (!pointerToGreen(event, dragPoint)) return;
  const shot = ball.position.clone().sub(dragPoint);
  shot.y = 0;
  const distance = shot.length();
  if (distance < 0.15) {
    updateHud('Click and drag from the ball to aim');
    return;
  }

  const power = THREE.MathUtils.clamp(distance * 3.2, 1.4, 10.5);
  velocity.copy(shot.normalize().multiplyScalar(power));
  strokes += 1;
  updateHud('Ball rolling...');
});

function animate(): void {
  requestAnimationFrame(animate);

  const dt = Math.min(clock.getDelta(), 0.033);
  const rolling = speed();

  if (!holed && rolling > 0.005) {
    ball.position.x += velocity.x * dt;
    ball.position.z += velocity.z * dt;

    obstacleCollision();
    boundsCollision();

    const friction = Math.pow(0.28, dt);
    velocity.multiplyScalar(friction);
    if (speed() < 0.045) velocity.set(0, 0, 0);

    const rollAxis = new THREE.Vector3(velocity.z, 0, -velocity.x);
    if (rollAxis.lengthSq() > 0.00001) {
      ball.rotateOnWorldAxis(rollAxis.normalize(), (rolling * dt) / ballRadius);
    }
  }

  const cupDistance = Math.hypot(ball.position.x - cup.position.x, ball.position.z - cup.position.z);
  if (!holed && cupDistance < 0.45 && speed() < 2.8) {
    holed = true;
    velocity.set(0, 0, 0);
    ball.position.set(cup.position.x, 0.16, cup.position.z);
    aimLine.visible = false;
    powerRing.visible = false;
    updateHud(`Holed in ${strokes} ${strokes === 1 ? 'stroke' : 'strokes'}`);
  } else if (!holed && speed() < 0.06 && !isDragging) {
    const defaultDirection = new THREE.Vector3(0, 0, -1);
    updateAimVisual(defaultDirection, 0.35);
    updateHud('Click and drag from the ball to aim');
  }

  if (!isDragging && speed() > 0.06) {
    aimLine.visible = false;
    powerRing.visible = false;
  }

  cameraTarget.set(ball.position.x, 0.5, ball.position.z);
  const desiredCamera = new THREE.Vector3(ball.position.x, 7.4, ball.position.z + 9.2);
  camera.position.lerp(desiredCamera, 1 - Math.pow(0.02, dt));
  camera.lookAt(cameraTarget);

  renderer.render(scene, camera);
}

window.addEventListener('resize', () => {
  camera.aspect = window.innerWidth / window.innerHeight;
  camera.updateProjectionMatrix();
  renderer.setSize(window.innerWidth, window.innerHeight);
});

updateHud('Click and drag from the ball to aim');
animate();

```
