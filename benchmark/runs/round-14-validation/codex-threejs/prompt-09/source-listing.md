# Generated Source Listing

This file is generated by the Round 14 Validation capture runner for neutral scoring.

## index.html

```
<!doctype html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Aura3D Round 14 Validation Benchmark</title>
  </head>
  <body>
    <div id="app"></div>
    <script type="module" src="/src/main.ts"></script>
  </body>
</html>

```

## src/main.ts

```
import * as THREE from 'three';

const app = document.querySelector<HTMLDivElement>('#app');

if (!app) {
  throw new Error('Missing #app container');
}

document.body.style.margin = '0';
document.body.style.overflow = 'hidden';
document.body.style.background = '#d8e8f0';
app.style.width = '100vw';
app.style.height = '100vh';

const scene = new THREE.Scene();
scene.background = new THREE.Color(0xd8e8f0);
scene.fog = new THREE.Fog(0xd8e8f0, 10, 34);

const camera = new THREE.PerspectiveCamera(45, window.innerWidth / window.innerHeight, 0.1, 100);
camera.position.set(6.2, 4.4, 8.2);
camera.lookAt(0, 1.35, 0);

const renderer = new THREE.WebGLRenderer({ antialias: true });
renderer.setPixelRatio(Math.min(window.devicePixelRatio, 2));
renderer.setSize(window.innerWidth, window.innerHeight);
renderer.shadowMap.enabled = true;
renderer.shadowMap.type = THREE.PCFSoftShadowMap;
app.appendChild(renderer.domElement);

const hemiLight = new THREE.HemisphereLight(0xffffff, 0x78909c, 2.2);
scene.add(hemiLight);

const sun = new THREE.DirectionalLight(0xffffff, 3.8);
sun.position.set(5, 8, 4);
sun.castShadow = true;
sun.shadow.mapSize.set(2048, 2048);
sun.shadow.camera.near = 0.5;
sun.shadow.camera.far = 30;
sun.shadow.camera.left = -10;
sun.shadow.camera.right = 10;
sun.shadow.camera.top = 10;
sun.shadow.camera.bottom = -10;
scene.add(sun);

const groundMaterial = new THREE.MeshStandardMaterial({
  color: 0x6f8f63,
  roughness: 0.9,
  metalness: 0.0,
});
const ground = new THREE.Mesh(new THREE.PlaneGeometry(18, 12), groundMaterial);
ground.rotation.x = -Math.PI / 2;
ground.receiveShadow = true;
scene.add(ground);

const grid = new THREE.GridHelper(18, 18, 0x3f5b3e, 0x8aa37f);
grid.position.y = 0.012;
scene.add(grid);

const bodyMaterial = new THREE.MeshStandardMaterial({ color: 0x2f6fed, roughness: 0.55 });
const headMaterial = new THREE.MeshStandardMaterial({ color: 0xf0c7a0, roughness: 0.65 });
const limbMaterial = new THREE.MeshStandardMaterial({ color: 0x24344d, roughness: 0.58 });
const handMaterial = new THREE.MeshStandardMaterial({ color: 0xf0c7a0, roughness: 0.65 });
const shoeMaterial = new THREE.MeshStandardMaterial({ color: 0x171a20, roughness: 0.7 });
const trailMaterial = new THREE.MeshStandardMaterial({
  color: 0x2f6fed,
  transparent: true,
  opacity: 0.18,
  roughness: 0.8,
});

const character = new THREE.Group();
scene.add(character);

const torso = new THREE.Mesh(new THREE.CylinderGeometry(0.48, 0.6, 1.42, 24), bodyMaterial);
torso.position.y = 1.72;
torso.castShadow = true;
torso.receiveShadow = true;
character.add(torso);

const neck = new THREE.Mesh(new THREE.CylinderGeometry(0.18, 0.2, 0.16, 16), headMaterial);
neck.position.y = 2.51;
neck.castShadow = true;
character.add(neck);

const head = new THREE.Mesh(new THREE.SphereGeometry(0.38, 32, 20), headMaterial);
head.position.y = 2.92;
head.castShadow = true;
character.add(head);

const nose = new THREE.Mesh(new THREE.BoxGeometry(0.1, 0.1, 0.16), headMaterial);
nose.position.set(0, 2.91, 0.37);
nose.castShadow = true;
character.add(nose);

const shoulderBar = new THREE.Mesh(new THREE.BoxGeometry(1.55, 0.18, 0.28), bodyMaterial);
shoulderBar.position.y = 2.24;
shoulderBar.castShadow = true;
character.add(shoulderBar);

type Side = -1 | 1;

interface LimbSet {
  upper: THREE.Group;
  lower: THREE.Group;
  foot?: THREE.Group;
}

function shadowed(mesh: THREE.Mesh): THREE.Mesh {
  mesh.castShadow = true;
  mesh.receiveShadow = true;
  return mesh;
}

function makeArm(side: Side): LimbSet {
  const upper = new THREE.Group();
  upper.position.set(side * 0.78, 2.18, 0);
  character.add(upper);

  const upperMesh = shadowed(new THREE.Mesh(new THREE.BoxGeometry(0.22, 0.72, 0.22), limbMaterial));
  upperMesh.position.y = -0.36;
  upper.add(upperMesh);

  const lower = new THREE.Group();
  lower.position.y = -0.72;
  upper.add(lower);

  const lowerMesh = shadowed(new THREE.Mesh(new THREE.BoxGeometry(0.2, 0.66, 0.2), limbMaterial));
  lowerMesh.position.y = -0.33;
  lower.add(lowerMesh);

  const hand = shadowed(new THREE.Mesh(new THREE.SphereGeometry(0.15, 18, 12), handMaterial));
  hand.position.y = -0.72;
  lower.add(hand);

  return { upper, lower };
}

function makeLeg(side: Side): LimbSet {
  const upper = new THREE.Group();
  upper.position.set(side * 0.28, 1.03, 0);
  character.add(upper);

  const upperMesh = shadowed(new THREE.Mesh(new THREE.BoxGeometry(0.28, 0.86, 0.28), limbMaterial));
  upperMesh.position.y = -0.43;
  upper.add(upperMesh);

  const lower = new THREE.Group();
  lower.position.y = -0.86;
  upper.add(lower);

  const lowerMesh = shadowed(new THREE.Mesh(new THREE.BoxGeometry(0.24, 0.78, 0.24), limbMaterial));
  lowerMesh.position.y = -0.39;
  lower.add(lowerMesh);

  const foot = new THREE.Group();
  foot.position.set(0, -0.78, 0.1);
  lower.add(foot);

  const shoe = shadowed(new THREE.Mesh(new THREE.BoxGeometry(0.34, 0.16, 0.58), shoeMaterial));
  shoe.position.z = 0.12;
  foot.add(shoe);

  return { upper, lower, foot };
}

const leftArm = makeArm(-1);
const rightArm = makeArm(1);
const leftLeg = makeLeg(-1);
const rightLeg = makeLeg(1);

function makeTrailFigure(x: number, opacity: number): THREE.Group {
  const trail = new THREE.Group();
  trail.position.set(x, 0, -0.42);

  const material = trailMaterial.clone();
  material.opacity = opacity;

  const trailTorso = new THREE.Mesh(new THREE.CylinderGeometry(0.4, 0.5, 1.25, 18), material);
  trailTorso.position.y = 1.66;
  trail.add(trailTorso);

  const trailHead = new THREE.Mesh(new THREE.SphereGeometry(0.3, 18, 12), material);
  trailHead.position.y = 2.63;
  trail.add(trailHead);

  for (const side of [-1, 1] as Side[]) {
    const arm = new THREE.Mesh(new THREE.BoxGeometry(0.16, 1.05, 0.16), material);
    arm.position.set(side * 0.62, 1.62, side * 0.12);
    arm.rotation.x = side * 0.55;
    trail.add(arm);

    const leg = new THREE.Mesh(new THREE.BoxGeometry(0.2, 1.18, 0.2), material);
    leg.position.set(side * 0.2, 0.62, -side * 0.12);
    leg.rotation.x = -side * 0.52;
    trail.add(leg);
  }

  return trail;
}

scene.add(makeTrailFigure(-2.0, 0.12), makeTrailFigure(-1.35, 0.2), makeTrailFigure(-0.72, 0.29));

const clock = new THREE.Clock();

function animate(): void {
  const elapsed = clock.getElapsedTime() + 0.45;
  const stride = elapsed * 5.4;
  const walk = Math.sin(stride);
  const counterWalk = Math.sin(stride + Math.PI);
  const bob = Math.abs(Math.sin(stride)) * 0.08;

  character.position.x = THREE.MathUtils.pingpong(elapsed * 0.95, 6) - 3;
  character.position.y = bob;
  character.rotation.y = 0.12 * Math.sin(elapsed * 0.8);

  torso.rotation.z = 0.05 * Math.sin(stride);
  torso.rotation.x = 0.04 * Math.sin(stride * 2);
  head.position.y = 2.92 + bob * 0.35;
  head.rotation.z = -torso.rotation.z * 0.7;
  shoulderBar.rotation.z = torso.rotation.z * 1.2;

  leftArm.upper.rotation.x = counterWalk * 0.82;
  rightArm.upper.rotation.x = walk * 0.82;
  leftArm.lower.rotation.x = 0.18 + Math.max(0, walk) * 0.5;
  rightArm.lower.rotation.x = 0.18 + Math.max(0, counterWalk) * 0.5;

  leftLeg.upper.rotation.x = walk * 0.76;
  rightLeg.upper.rotation.x = counterWalk * 0.76;
  leftLeg.lower.rotation.x = Math.max(0, -walk) * 0.9;
  rightLeg.lower.rotation.x = Math.max(0, -counterWalk) * 0.9;

  if (leftLeg.foot && rightLeg.foot) {
    leftLeg.foot.rotation.x = -0.18 - Math.max(0, walk) * 0.28;
    rightLeg.foot.rotation.x = -0.18 - Math.max(0, counterWalk) * 0.28;
  }

  renderer.render(scene, camera);
  requestAnimationFrame(animate);
}

function resize(): void {
  camera.aspect = window.innerWidth / window.innerHeight;
  camera.updateProjectionMatrix();
  renderer.setSize(window.innerWidth, window.innerHeight);
}

window.addEventListener('resize', resize);
animate();

```
