# Generated Source Listing

This file is generated by the Round 14 Validation capture runner for neutral scoring.

## index.html

```
<!doctype html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Aura3D Round 14 Validation Benchmark</title>
  </head>
  <body>
    <div id="app"></div>
    <script type="module" src="/src/main.ts"></script>
  </body>
</html>

```

## src/main.ts

```
import * as THREE from 'three';
import { OrbitControls } from 'three/addons/controls/OrbitControls.js';
import { CSS2DObject, CSS2DRenderer } from 'three/addons/renderers/CSS2DRenderer.js';
import { EffectComposer } from 'three/addons/postprocessing/EffectComposer.js';
import { RenderPass } from 'three/addons/postprocessing/RenderPass.js';
import { UnrealBloomPass } from 'three/addons/postprocessing/UnrealBloomPass.js';

const app = document.querySelector<HTMLDivElement>('#app');

if (!app) {
  throw new Error('Missing #app container');
}

document.body.style.margin = '0';
document.body.style.overflow = 'hidden';
document.body.style.background = '#02030a';
app.style.width = '100vw';
app.style.height = '100vh';

const style = document.createElement('style');
style.textContent = `
  .planet-label {
    color: #f6fbff;
    font: 700 13px/1.1 system-ui, -apple-system, BlinkMacSystemFont, "Segoe UI", sans-serif;
    letter-spacing: 0;
    padding: 4px 7px;
    border: 1px solid rgba(210, 230, 255, 0.55);
    border-radius: 6px;
    background: rgba(5, 10, 20, 0.78);
    box-shadow: 0 0 12px rgba(120, 180, 255, 0.35);
    text-shadow: 0 1px 2px #000;
    white-space: nowrap;
    pointer-events: none;
  }
  .sun-label {
    border-color: rgba(255, 215, 105, 0.8);
    box-shadow: 0 0 16px rgba(255, 190, 55, 0.55);
  }
`;
document.head.appendChild(style);

const scene = new THREE.Scene();
scene.background = new THREE.Color(0x02030a);

const camera = new THREE.PerspectiveCamera(45, window.innerWidth / window.innerHeight, 0.1, 300);
camera.position.set(0, 34, 58);

const renderer = new THREE.WebGLRenderer({ antialias: true });
renderer.setPixelRatio(Math.min(window.devicePixelRatio, 2));
renderer.setSize(window.innerWidth, window.innerHeight);
renderer.toneMapping = THREE.ACESFilmicToneMapping;
renderer.toneMappingExposure = 1.05;
app.appendChild(renderer.domElement);

const labelRenderer = new CSS2DRenderer();
labelRenderer.setSize(window.innerWidth, window.innerHeight);
labelRenderer.domElement.style.position = 'fixed';
labelRenderer.domElement.style.inset = '0';
labelRenderer.domElement.style.pointerEvents = 'none';
app.appendChild(labelRenderer.domElement);

const controls = new OrbitControls(camera, renderer.domElement);
controls.enableDamping = true;
controls.target.set(0, 0, 0);
controls.minDistance = 16;
controls.maxDistance = 105;
controls.autoRotate = true;
controls.autoRotateSpeed = 0.35;

scene.add(new THREE.AmbientLight(0x27354f, 0.8));

const sunLight = new THREE.PointLight(0xffd56a, 2700, 160, 1.25);
sunLight.position.set(0, 0, 0);
scene.add(sunLight);

const sun = new THREE.Mesh(
  new THREE.SphereGeometry(4.0, 96, 64),
  new THREE.MeshStandardMaterial({
    color: 0xffb22f,
    emissive: 0xff8c12,
    emissiveIntensity: 4.5,
    roughness: 0.55,
  }),
);
sun.name = 'Sun';
scene.add(sun);

const halo = new THREE.Mesh(
  new THREE.SphereGeometry(5.9, 96, 64),
  new THREE.MeshBasicMaterial({
    color: 0xffb33a,
    transparent: true,
    opacity: 0.18,
    blending: THREE.AdditiveBlending,
    depthWrite: false,
  }),
);
scene.add(halo);

function addLabel(text: string, parent: THREE.Object3D, y: number, className = '') {
  const element = document.createElement('div');
  element.className = `planet-label ${className}`.trim();
  element.textContent = text;
  const label = new CSS2DObject(element);
  label.position.set(0, y, 0);
  parent.add(label);
}

addLabel('Sun', sun, 5.1, 'sun-label');

type PlanetSpec = {
  name: string;
  radius: number;
  distance: number;
  speed: number;
  color: number;
  emissive?: number;
  phase: number;
};

const planetSpecs: PlanetSpec[] = [
  { name: 'Aster', radius: 0.85, distance: 8.5, speed: 0.78, color: 0xbfc7d5, phase: 0.2 },
  { name: 'Cinder', radius: 1.15, distance: 13.0, speed: 0.52, color: 0xd38a45, emissive: 0x301100, phase: 1.4 },
  { name: 'Maris', radius: 1.35, distance: 18.5, speed: 0.36, color: 0x3d8bdd, emissive: 0x061225, phase: 2.7 },
  { name: 'Verdan', radius: 1.05, distance: 24.5, speed: 0.27, color: 0x63b870, emissive: 0x061806, phase: 3.8 },
  { name: 'Orun', radius: 2.05, distance: 32.0, speed: 0.18, color: 0xd9b06c, emissive: 0x241506, phase: 4.9 },
  { name: 'Nivis', radius: 1.55, distance: 41.0, speed: 0.12, color: 0x9fc8ff, emissive: 0x07182a, phase: 5.8 },
];

const planetPivots: Array<{ pivot: THREE.Object3D; planet: THREE.Mesh; spec: PlanetSpec }> = [];

const orbitMaterial = new THREE.LineBasicMaterial({
  color: 0x37506f,
  transparent: true,
  opacity: 0.52,
});

function makeOrbit(distance: number) {
  const points: THREE.Vector3[] = [];
  for (let i = 0; i <= 192; i += 1) {
    const angle = (i / 192) * Math.PI * 2;
    points.push(new THREE.Vector3(Math.cos(angle) * distance, 0, Math.sin(angle) * distance));
  }
  const geometry = new THREE.BufferGeometry().setFromPoints(points);
  const orbit = new THREE.Line(geometry, orbitMaterial);
  scene.add(orbit);
}

for (const spec of planetSpecs) {
  makeOrbit(spec.distance);

  const pivot = new THREE.Object3D();
  pivot.rotation.y = spec.phase;
  scene.add(pivot);

  const planet = new THREE.Mesh(
    new THREE.SphereGeometry(spec.radius, 48, 32),
    new THREE.MeshStandardMaterial({
      color: spec.color,
      emissive: spec.emissive ?? 0x000000,
      emissiveIntensity: 0.35,
      roughness: 0.78,
      metalness: 0.02,
    }),
  );
  planet.position.x = spec.distance;
  planet.name = spec.name;
  pivot.add(planet);
  addLabel(spec.name, planet, spec.radius + 0.9);

  if (spec.name === 'Orun') {
    const ring = new THREE.Mesh(
      new THREE.RingGeometry(spec.radius * 1.45, spec.radius * 2.15, 96),
      new THREE.MeshBasicMaterial({
        color: 0xe7d19b,
        transparent: true,
        opacity: 0.55,
        side: THREE.DoubleSide,
      }),
    );
    ring.rotation.x = Math.PI / 2.7;
    planet.add(ring);
  }

  planetPivots.push({ pivot, planet, spec });
}

const starGeometry = new THREE.BufferGeometry();
const starPositions: number[] = [];
for (let i = 0; i < 900; i += 1) {
  const radius = 85 + Math.random() * 80;
  const theta = Math.random() * Math.PI * 2;
  const phi = Math.acos(THREE.MathUtils.randFloatSpread(2));
  starPositions.push(
    radius * Math.sin(phi) * Math.cos(theta),
    radius * Math.cos(phi),
    radius * Math.sin(phi) * Math.sin(theta),
  );
}
starGeometry.setAttribute('position', new THREE.Float32BufferAttribute(starPositions, 3));
scene.add(
  new THREE.Points(
    starGeometry,
    new THREE.PointsMaterial({ color: 0xdde8ff, size: 0.18, sizeAttenuation: true }),
  ),
);

const composer = new EffectComposer(renderer);
composer.addPass(new RenderPass(scene, camera));
const bloomPass = new UnrealBloomPass(
  new THREE.Vector2(window.innerWidth, window.innerHeight),
  1.15,
  0.72,
  0.08,
);
composer.addPass(bloomPass);

const clock = new THREE.Clock();

function animate() {
  requestAnimationFrame(animate);
  const delta = clock.getDelta();
  const elapsed = clock.elapsedTime;

  sun.rotation.y += delta * 0.24;
  halo.scale.setScalar(1 + Math.sin(elapsed * 1.7) * 0.035);

  for (const { pivot, planet, spec } of planetPivots) {
    pivot.rotation.y += delta * spec.speed;
    planet.rotation.y += delta * (0.65 + spec.speed);
  }

  controls.update();
  composer.render();
  labelRenderer.render(scene, camera);
}

function resize() {
  camera.aspect = window.innerWidth / window.innerHeight;
  camera.updateProjectionMatrix();
  renderer.setSize(window.innerWidth, window.innerHeight);
  labelRenderer.setSize(window.innerWidth, window.innerHeight);
  composer.setSize(window.innerWidth, window.innerHeight);
  bloomPass.setSize(window.innerWidth, window.innerHeight);
}

window.addEventListener('resize', resize);
animate();

```
