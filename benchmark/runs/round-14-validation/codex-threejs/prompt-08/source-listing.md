# Generated Source Listing

This file is generated by the Round 14 Validation capture runner for neutral scoring.

## index.html

```
<!doctype html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Aura3D Round 14 Validation Benchmark</title>
  </head>
  <body>
    <div id="app"></div>
    <script type="module" src="/src/main.ts"></script>
  </body>
</html>

```

## src/main.ts

```
import * as THREE from 'three';
import { OrbitControls } from 'three/addons/controls/OrbitControls.js';

const app = document.querySelector<HTMLDivElement>('#app');

if (!app) {
  throw new Error('Missing #app root element');
}

document.body.style.margin = '0';
document.body.style.overflow = 'hidden';
document.body.style.fontFamily = 'Inter, ui-sans-serif, system-ui, sans-serif';

app.innerHTML = `
  <button id="day-night-toggle" class="mode-toggle" type="button" aria-pressed="false">
    Switch to night
  </button>
  <div class="legend">Procedural city block: 20 buildings, streets, windows, and lights</div>
`;

const style = document.createElement('style');
style.textContent = `
  .mode-toggle {
    position: fixed;
    top: 18px;
    left: 18px;
    z-index: 5;
    border: 1px solid rgba(255, 255, 255, 0.48);
    border-radius: 6px;
    padding: 10px 14px;
    color: #111827;
    background: rgba(255, 255, 255, 0.9);
    font-size: 14px;
    font-weight: 700;
    cursor: pointer;
    box-shadow: 0 10px 24px rgba(0, 0, 0, 0.2);
  }

  .legend {
    position: fixed;
    left: 18px;
    bottom: 18px;
    z-index: 5;
    max-width: min(420px, calc(100vw - 36px));
    color: #f8fafc;
    background: rgba(15, 23, 42, 0.68);
    border: 1px solid rgba(255, 255, 255, 0.22);
    border-radius: 6px;
    padding: 9px 11px;
    font-size: 13px;
    line-height: 1.35;
    text-shadow: 0 1px 2px rgba(0, 0, 0, 0.45);
  }
`;
document.head.appendChild(style);

const scene = new THREE.Scene();
scene.background = new THREE.Color(0x91c7f2);
scene.fog = new THREE.Fog(0x91c7f2, 58, 130);

const camera = new THREE.PerspectiveCamera(50, window.innerWidth / window.innerHeight, 0.1, 220);
camera.position.set(42, 34, 46);
camera.lookAt(0, 8, 0);

const renderer = new THREE.WebGLRenderer({ antialias: true });
renderer.setPixelRatio(Math.min(window.devicePixelRatio, 2));
renderer.setSize(window.innerWidth, window.innerHeight);
renderer.shadowMap.enabled = true;
renderer.shadowMap.type = THREE.PCFSoftShadowMap;
renderer.outputColorSpace = THREE.SRGBColorSpace;
app.appendChild(renderer.domElement);

const controls = new OrbitControls(camera, renderer.domElement);
controls.enableDamping = true;
controls.target.set(0, 8, 0);
controls.maxPolarAngle = Math.PI * 0.48;
controls.minDistance = 28;
controls.maxDistance = 95;

const hemiLight = new THREE.HemisphereLight(0xddeeff, 0x58616d, 1.35);
scene.add(hemiLight);

const sun = new THREE.DirectionalLight(0xfff0ca, 2.4);
sun.position.set(-28, 42, 24);
sun.castShadow = true;
sun.shadow.mapSize.set(2048, 2048);
sun.shadow.camera.left = -60;
sun.shadow.camera.right = 60;
sun.shadow.camera.top = 60;
sun.shadow.camera.bottom = -60;
sun.shadow.camera.near = 1;
sun.shadow.camera.far = 110;
scene.add(sun);

const moon = new THREE.DirectionalLight(0xb8ccff, 0);
moon.position.set(24, 38, -30);
scene.add(moon);

const groundMaterial = new THREE.MeshStandardMaterial({ color: 0x5c7b5a, roughness: 0.9 });
const ground = new THREE.Mesh(new THREE.PlaneGeometry(86, 86), groundMaterial);
ground.rotation.x = -Math.PI / 2;
ground.receiveShadow = true;
scene.add(ground);

const roadMaterial = new THREE.MeshStandardMaterial({ color: 0x252a31, roughness: 0.82 });
const crosswalkMaterial = new THREE.MeshStandardMaterial({ color: 0xdce1e5, roughness: 0.7 });
const sidewalkMaterial = new THREE.MeshStandardMaterial({ color: 0x8d939a, roughness: 0.8 });

function addPlane(width: number, depth: number, x: number, z: number, material: THREE.Material, y = 0.025) {
  const mesh = new THREE.Mesh(new THREE.PlaneGeometry(width, depth), material);
  mesh.rotation.x = -Math.PI / 2;
  mesh.position.set(x, y, z);
  mesh.receiveShadow = true;
  scene.add(mesh);
  return mesh;
}

addPlane(86, 7.2, 0, 0, roadMaterial, 0.05);
addPlane(7.2, 86, 0, 0, roadMaterial, 0.055);
addPlane(86, 2.1, 0, 5.15, sidewalkMaterial, 0.06);
addPlane(86, 2.1, 0, -5.15, sidewalkMaterial, 0.06);
addPlane(2.1, 86, 5.15, 0, sidewalkMaterial, 0.065);
addPlane(2.1, 86, -5.15, 0, sidewalkMaterial, 0.065);

for (let i = -2; i <= 2; i += 1) {
  addPlane(0.8, 5.4, i * 1.55, 0, crosswalkMaterial, 0.08);
  addPlane(5.4, 0.8, 0, i * 1.55, crosswalkMaterial, 0.085);
}

const buildingPalette = [0x617184, 0x8b8274, 0x596675, 0x9a8f7f, 0x6f7e89, 0x746b68];
const windowDay = new THREE.MeshStandardMaterial({
  color: 0x2c425a,
  emissive: 0x07111a,
  roughness: 0.35,
  metalness: 0.05,
});
const windowNight = new THREE.MeshStandardMaterial({
  color: 0xffd36a,
  emissive: 0xffc247,
  emissiveIntensity: 1.3,
  roughness: 0.25,
});
const windowMeshes: THREE.Mesh[] = [];

function addWindow(x: number, y: number, z: number, width: number, height: number, rotationY: number) {
  const pane = new THREE.Mesh(new THREE.PlaneGeometry(width, height), windowDay);
  pane.position.set(x, y, z);
  pane.rotation.y = rotationY;
  scene.add(pane);
  windowMeshes.push(pane);
}

function addWindowsForBuilding(x: number, z: number, width: number, depth: number, height: number) {
  const floors = Math.max(2, Math.floor(height / 2.35));
  const frontCols = Math.max(2, Math.floor(width / 1.45));
  const sideCols = Math.max(2, Math.floor(depth / 1.45));
  const baseY = 1.45;
  const yStep = (height - 2.2) / floors;

  for (let floor = 0; floor < floors; floor += 1) {
    const y = baseY + floor * yStep;
    for (let col = 0; col < frontCols; col += 1) {
      const offsetX = -width * 0.36 + (frontCols === 1 ? 0 : (col / (frontCols - 1)) * width * 0.72);
      addWindow(x + offsetX, y, z + depth / 2 + 0.012, 0.48, 0.62, 0);
      addWindow(x - offsetX, y, z - depth / 2 - 0.012, 0.48, 0.62, Math.PI);
    }
    for (let col = 0; col < sideCols; col += 1) {
      const offsetZ = -depth * 0.34 + (sideCols === 1 ? 0 : (col / (sideCols - 1)) * depth * 0.68);
      addWindow(x + width / 2 + 0.012, y, z + offsetZ, 0.48, 0.62, Math.PI / 2);
      addWindow(x - width / 2 - 0.012, y, z - offsetZ, 0.48, 0.62, -Math.PI / 2);
    }
  }
}

const buildingHeights = [
  8, 14, 22, 11, 18,
  26, 10, 16, 30, 12,
  20, 15, 28, 9, 24,
  13, 32, 17, 21, 27,
];
const buildingPositions: Array<[number, number]> = [];

for (const sx of [-1, 1]) {
  for (const sz of [-1, 1]) {
    const localPositions: Array<[number, number]> = [
      [10, 10],
      [20, 10],
      [30, 10],
      [12, 21],
      [25, 23],
    ];
    for (const [x, z] of localPositions) {
      buildingPositions.push([sx * x, sz * z]);
    }
  }
}

buildingPositions.forEach(([x, z], index) => {
  const height = buildingHeights[index];
  const width = 5.3 + ((index * 1.7) % 2.4);
  const depth = 5.1 + ((index * 2.1) % 2.7);
  const geometry = new THREE.BoxGeometry(width, height, depth);
  const material = new THREE.MeshStandardMaterial({
    color: buildingPalette[index % buildingPalette.length],
    roughness: 0.72,
    metalness: 0.08,
  });
  const building = new THREE.Mesh(geometry, material);
  building.position.set(x, height / 2, z);
  building.castShadow = true;
  building.receiveShadow = true;
  scene.add(building);
  addWindowsForBuilding(x, z, width, depth, height);

  const roof = new THREE.Mesh(
    new THREE.BoxGeometry(width + 0.35, 0.28, depth + 0.35),
    new THREE.MeshStandardMaterial({ color: 0x303944, roughness: 0.88 }),
  );
  roof.position.set(x, height + 0.14, z);
  roof.castShadow = true;
  scene.add(roof);
});

const lampGroup = new THREE.Group();
scene.add(lampGroup);
const lampPoleMaterial = new THREE.MeshStandardMaterial({ color: 0x20252b, roughness: 0.55, metalness: 0.35 });
const lampBulbDay = new THREE.MeshStandardMaterial({ color: 0xfff1b8, emissive: 0x3d3010, emissiveIntensity: 0.15 });
const lampBulbNight = new THREE.MeshStandardMaterial({ color: 0xfff0b0, emissive: 0xffd36a, emissiveIntensity: 2.4 });
const lampBulbs: THREE.Mesh[] = [];
const streetLights: THREE.PointLight[] = [];

function addStreetLight(x: number, z: number) {
  const pole = new THREE.Mesh(new THREE.CylinderGeometry(0.08, 0.1, 4.1, 14), lampPoleMaterial);
  pole.position.set(x, 2.05, z);
  pole.castShadow = true;
  lampGroup.add(pole);

  const arm = new THREE.Mesh(new THREE.BoxGeometry(0.18, 0.18, 1.35), lampPoleMaterial);
  arm.position.set(x, 4.02, z + (z > 0 ? -0.58 : 0.58));
  arm.castShadow = true;
  lampGroup.add(arm);

  const bulb = new THREE.Mesh(new THREE.SphereGeometry(0.24, 18, 12), lampBulbDay);
  bulb.position.set(x, 3.82, z + (z > 0 ? -1.18 : 1.18));
  lampGroup.add(bulb);
  lampBulbs.push(bulb);

  const light = new THREE.PointLight(0xffd77a, 0, 14, 2.2);
  light.position.copy(bulb.position);
  lampGroup.add(light);
  streetLights.push(light);
}

[-31, -21, -11, 11, 21, 31].forEach((x) => {
  addStreetLight(x, 4.15);
  addStreetLight(x, -4.15);
});
[-31, -21, -11, 11, 21, 31].forEach((z) => {
  addStreetLight(4.15, z);
  addStreetLight(-4.15, z);
});

const isNight = { value: false };
const toggle = document.querySelector<HTMLButtonElement>('#day-night-toggle');

function applyDayNight() {
  if (isNight.value) {
    scene.background = new THREE.Color(0x08112a);
    scene.fog = new THREE.Fog(0x08112a, 42, 118);
    hemiLight.intensity = 0.42;
    hemiLight.color.set(0x5c73a8);
    hemiLight.groundColor.set(0x0b1020);
    sun.intensity = 0.08;
    moon.intensity = 1.35;
    groundMaterial.color.set(0x25384b);
    roadMaterial.color.set(0x111827);
    windowMeshes.forEach((pane) => {
      pane.material = windowNight;
    });
    lampBulbs.forEach((bulb) => {
      bulb.material = lampBulbNight;
    });
    streetLights.forEach((light) => {
      light.intensity = 1.75;
    });
    if (toggle) {
      toggle.textContent = 'Switch to day';
      toggle.setAttribute('aria-pressed', 'true');
      toggle.style.background = 'rgba(15, 23, 42, 0.92)';
      toggle.style.color = '#f8fafc';
    }
  } else {
    scene.background = new THREE.Color(0x91c7f2);
    scene.fog = new THREE.Fog(0x91c7f2, 58, 130);
    hemiLight.intensity = 1.35;
    hemiLight.color.set(0xddeeff);
    hemiLight.groundColor.set(0x58616d);
    sun.intensity = 2.4;
    moon.intensity = 0;
    groundMaterial.color.set(0x5c7b5a);
    roadMaterial.color.set(0x252a31);
    windowMeshes.forEach((pane) => {
      pane.material = windowDay;
    });
    lampBulbs.forEach((bulb) => {
      bulb.material = lampBulbDay;
    });
    streetLights.forEach((light) => {
      light.intensity = 0;
    });
    if (toggle) {
      toggle.textContent = 'Switch to night';
      toggle.setAttribute('aria-pressed', 'false');
      toggle.style.background = 'rgba(255, 255, 255, 0.9)';
      toggle.style.color = '#111827';
    }
  }
}

toggle?.addEventListener('click', () => {
  isNight.value = !isNight.value;
  applyDayNight();
});

function onResize() {
  camera.aspect = window.innerWidth / window.innerHeight;
  camera.updateProjectionMatrix();
  renderer.setSize(window.innerWidth, window.innerHeight);
}

window.addEventListener('resize', onResize);

function animate() {
  controls.update();
  renderer.render(scene, camera);
}

renderer.setAnimationLoop(animate);
applyDayNight();

```
