Implemented the Material Lab in `source`.

Build completed successfully:
```bash
npm run build
```

Runner command:
```bash
npm run dev
```

Assumptions:
- This prompt is governed by `context/llms.txt`, which specifies raw Three.js rather than Aura3D.
- No assets are used; the environment/reflections are procedural via `RoomEnvironment`.
- I did not run dev server, preview, Playwright, or screenshots per benchmark rules.