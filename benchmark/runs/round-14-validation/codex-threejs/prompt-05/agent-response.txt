Implemented the browser 3D app in `source/src/main.ts`.

Build command run:

```bash
npm run build
```

Build result: failed during TypeScript typecheck:

```text
src/main.ts(261,17): error TS18047: 'app' is possibly 'null'.
src/main.ts(262,18): error TS18047: 'app' is possibly 'null'.
```

Runner command for later:

```bash
npm run dev
```

Assumptions/run notes: the app is procedural only, with 36 bars in a 6x6 grid, animated varying heights, height-based color, OrbitControls, CSS2D axis/tick labels, and raycaster hover highlighting that brightens the hovered bar and updates the HUD. Per the benchmark rule, I stopped after the build failure.