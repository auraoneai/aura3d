Implemented the mini-golf app in `source/src/main.ts` using the procedural `prefabs.miniGolfHole()` helper, visible HUD score/power controls, an aim-and-shoot button, pointer interaction, and a follow camera targeting `white physics golf ball`.

Build completed successfully.

Build command:
```bash
cd source && npm run build
```

Run command for the runner:
```bash
cd source && npm run dev
```

Assumptions: no external assets are required or used; the Aura3D mini-golf prefab supplies the green, obstacle, ball, cup, aim line/trail, and physics visual evidence.