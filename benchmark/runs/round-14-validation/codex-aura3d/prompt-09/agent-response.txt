Implemented the animated primitive humanoid app in `source/src/main.ts` using Aura3D’s `character.primitiveHumanoid` helper with walk clip, mid-stride pose, joints, motion trails, visible ground/path cues, humanoid camera, studio light, and looped timeline.

Build completed successfully.

Build command:
```bash
cd source && npm run build
```

Run command for runner:
```bash
cd source && npm run dev
```

Assumptions: no external assets are used; runtime visual evidence will be captured by the benchmark runner, not by the agent process.