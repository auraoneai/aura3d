Implemented the Neon Tunnel Flythrough in [source/src/main.ts](/Users/gurbakshchahal/aura3d/benchmark/runs/round-14-validation/codex-aura3d/prompt-04/source/src/main.ts:1) using the matching `prefabs.neonTunnel` recipe shape, with explicit `effects.bloom`, `effects.fog`, `camera.neon()`, and looping timeline animation.

Build command run:

```bash
cd source && npm run build
```

Build result: failed at typecheck because `@aura3d/engine` was not installed/resolved:

```text
src/main.ts(9,8): error TS2307: Cannot find module '@aura3d/engine' or its corresponding type declarations.
```

Runner run command for later:

```bash
cd source && npm run dev
```

Assumption: the runner will install dependencies first, or otherwise make the local `@aura3d/engine` package referenced in `package.json` resolvable before building/running.