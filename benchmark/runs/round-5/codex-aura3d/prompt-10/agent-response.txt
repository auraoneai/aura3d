Implemented the Aura3D product viewer in `source/src/main.ts` using only typed `assets.sneaker`, `prefabs.productStage()`, studio lights, orbit controls, and a looping turntable animation.

Build command:
`cd source && npm run build`

Run command:
`cd source && npm run dev`

Build result:
`npm run build` failed at TypeScript resolution for `@aura3d/engine`. The error says the package types exist, but `tsconfig` needs `moduleResolution` set to `node16`, `nodenext`, or `bundler`.

Assumptions:
The generated `assets.sneaker` entry is the provided `benchmark/assets/sneaker.glb` symlink already present under `source/public/aura-assets`.