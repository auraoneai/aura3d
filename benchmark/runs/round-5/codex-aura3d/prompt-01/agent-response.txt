Implemented the app in `source` using the provided Prompt 01 Aura3D recipe shape.

Build command:
```bash
cd source
npm run build
```

Run command:
```bash
cd source
npm run dev
```

Build result: failed at typecheck because `@aura3d/engine` is not currently resolvable in `source/node_modules`:

```text
src/main.ts(1,77): error TS2307: Cannot find module '@aura3d/engine' or its corresponding type declarations.
```

Assumption: run `npm install` in `source` before building in the benchmark environment so the local file dependency from `package.json` is installed.