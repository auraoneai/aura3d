Implemented the procedural solar system in `./source` with Aura3D primitives: sun, six planets, dotted orbit paths, readable planet labels, bloom, orbit interaction, and orbit camera framing.

Build command:
```bash
cd source && npm run build
```

Run command:
```bash
cd source && npm run dev
```

Build result: failed during TypeScript resolution:
```text
Cannot find module '@aura3d/engine' ... Consider updating to 'node16', 'nodenext', or 'bundler'.
```

Assumptions:
- Labels are implemented as a small DOM overlay, which the benchmark recipe permits for labels.
- No assets were used; everything is procedural.
- I stopped after the build failure per the benchmark execution rule.