Implemented the procedural neon tunnel flythrough in `source`.

Build result: `npm run build` completed successfully.

Build command:
```bash
npm run build
```

Run command:
```bash
npm run dev
```

Assumptions:
- `context/llms.txt` and `context/docs/agents/benchmark-recipes.md` were not present in the provided bundle, so I used the supplied Three.js context and existing Vite scaffold.
- No assets were used; the tunnel, emissive segments, glow/bloom, fog, and camera flythrough are all procedural.