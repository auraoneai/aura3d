Implemented the procedural Three.js humanoid in `source/src/main.ts`: sphere head, cylinder torso, box arms/legs, visible ground plane/grid/path, and a runtime walk cycle with limb swing, foot lift, body bob, and horizontal movement.

Build completed successfully.

Build command:
```bash
npm run build
```

Run command:
```bash
npm run dev
```

Assumptions:
- `./context/llms.txt` and `./context/docs/agents/benchmark-recipes.md` were not present in the provided bundle.
- No assets were used, per prompt requirements.
- I did not run a dev server or browser verification, per benchmark rules.