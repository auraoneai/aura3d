Implemented the app in `./source` with procedural 50-cube physics, tilted ramp, orbit camera controls, reset button, and live contact count overlay.

Build command run:

```bash
npm run build
```

Build failed at TypeScript resolution:

```text
src/main.ts(3,31): error TS2307: Cannot find module 'three/addons/controls/OrbitControls.js'
```

Run command:

```bash
npm run dev
```

Assumptions:
- The provided context bundle did not include `./context/llms.txt` or `./context/docs/agents/benchmark-recipes.md`.
- The bundle exposed only three.js, so the physics behavior is implemented procedurally inside the app.
- I stopped after the build failure as required by the benchmark execution rules.