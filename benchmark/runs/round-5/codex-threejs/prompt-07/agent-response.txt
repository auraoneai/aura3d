Implemented the Material Lab in `./source` with a procedural Three.js scene: five distinct spheres for metal, glass, rubber, emissive, and clearcoat materials, studio lighting panels, soft shadows, environment reflections via `RoomEnvironment`, and orbit controls.

Build command:
```bash
npm run build
```

Run command:
```bash
npm run dev
```

Build result: failed at TypeScript type resolution for Three.js addon imports:
```text
Cannot find module 'three/addons/controls/OrbitControls.js'
Cannot find module 'three/addons/environments/RoomEnvironment.js'
```

Assumptions:
- The provided context bundle did not include `llms.txt` or `docs/agents/benchmark-recipes.md`, so I used the existing Three.js scaffold.
- No external assets were used.
- I stopped after the build failure per the benchmark execution rules.