Implemented the procedural city block in `./source`.

Build command:
```bash
npm run build
```

Run command:
```bash
npm run dev
```

Build result: passed.

Assumptions:
- `./context/llms.txt` and `./context/docs/agents/benchmark-recipes.md` were not present, so I used the available context bundle files.
- The `source` project is a Three.js Vite scaffold, so the app is implemented procedurally with Three.js and no external assets.
- No dev server or browser verification was run, per benchmark rules.