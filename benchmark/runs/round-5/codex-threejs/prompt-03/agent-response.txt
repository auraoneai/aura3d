Implemented the procedural solar system in `source/src/main.ts` with:

- One glowing/bloomed sun
- Six labeled planets with distinct orbit distances and speeds
- Visible orbit rings
- OrbitControls camera framing the full system
- Procedural star field, no external assets

Build succeeded.

Build command:
```bash
cd source && npm run build
```

Run command:
```bash
cd source && npm run dev
```

Assumptions: `context/llms.txt` and `context/docs/agents/benchmark-recipes.md` were not present in the provided bundle, so I used the clean Vite/Three.js source scaffold and only edited files inside `source`.