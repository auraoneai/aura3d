Implemented the mini-golf 3D app in [source/src/main.ts](/Users/gurbakshchahal/aura3d/benchmark/runs/round-2/codex-threejs/prompt-06/source/src/main.ts).

It includes a procedural green, rails, one visible obstacle, ball physics with friction and wall/obstacle bounce, drag-to-aim/release-to-shoot interaction, stroke counter HUD, hole/flag, and a follow camera centered on the ball.

Build command:
```bash
cd source
npm run build
```

Run command:
```bash
cd source
npm run dev
```

Verification: `npm run build` passes. No long-running dev server was left attached.

Assumptions: no external assets were used, per prompt; the interaction is drag from near the ball and release to shoot.