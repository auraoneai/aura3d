Implemented the procedural solar system in [source/src/main.ts](/Users/gurbakshchahal/aura3d/benchmark/runs/round-2/codex-threejs/prompt-03/source/src/main.ts).

It includes one glowing/bloomed sun, 6 procedural planets with visibly different orbit distances and speeds, orbit rings, readable CSS2D labels, stars, lighting, and `OrbitControls` framing the full system.

Validation:
- `npm run typecheck` passed
- `npm run build` passed
- Headless render checked visually; temporary dev server was stopped

Commands:
```bash
cd source
npm run build
npm run dev
```

Assumptions:
- `llms.txt` was requested by AGENTS.md but is not present in the provided prompt directory, so I used only the provided context bundle and source scaffold.
- No assets were used; everything is procedural.