Implemented the product viewer in `source/src/main.ts` with:

- Provided sneaker GLB loaded from `benchmark/assets/sneaker.glb`
- Auto-centering and auto-scaling via bounding box
- Rotating turntable group
- Visible plinth/base with shadows
- Studio-style hemisphere, key, rim, and softbox lighting
- Orbit controls with damping
- Responsive fullscreen canvas

Verification passed:
- `npm run typecheck`
- `npm run build`
- Temporary Vite server + Playwright screenshot smoke check confirmed the sneaker is visible on the plinth. Server was stopped.

Build command:
```bash
npm run build
```

Run command:
```bash
npm run dev -- --port 5179
```

Assumptions: `llms.txt` was not present in the provided tree, so I used the provided prompt/context and the existing Vite/Three scaffold. No files outside `source` were edited.