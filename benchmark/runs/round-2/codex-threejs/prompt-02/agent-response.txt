Implemented the procedural particle fountain in [source/src/main.ts](/Users/gurbakshchahal/aura3d/benchmark/runs/round-2/codex-threejs/prompt-02/source/src/main.ts).

It includes a visible emitter/nozzle, gravity-driven particles with upward and falling arcs, lifetime-based color shifts, ground-plane collision/splash behavior, and an on-screen emission-rate slider.

Verification passed:

```bash
npm run build
```

Build command:

```bash
cd source && npm run build
```

Run command:

```bash
cd source && npm run dev
```

Assumptions: no assets are used; the missing `llms.txt` file was not present anywhere under the provided prompt directory, so I used only the provided prompt/context and kept all edits inside `source`. No dev server is left running.