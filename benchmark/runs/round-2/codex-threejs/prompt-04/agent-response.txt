Implemented the procedural neon tunnel flythrough in [source/src/main.ts](/Users/gurbakshchahal/aura3d/benchmark/runs/round-2/codex-threejs/prompt-04/source/src/main.ts).

What’s included:
- Procedural `TubeGeometry` tunnel interior
- Emissive neon rings and wall segments
- Animated camera flythrough along the tunnel curve
- `UnrealBloomPass` glow
- Exponential fog/depth falloff
- Runtime screenshot evidence at `source/runtime-evidence.png`

Verified:
- `npm install`
- `npm run build`
- Temporary dev server was started for screenshot capture and then stopped.

Build command:
```bash
cd source && npm run build
```

Run command:
```bash
cd source && npm run dev
```

Assumptions:
- No assets are used, per prompt.
- `llms.txt` was requested by instructions but was not present in the provided prompt directory tree.