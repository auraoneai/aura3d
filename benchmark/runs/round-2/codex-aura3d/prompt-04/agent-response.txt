Implemented the Neon Tunnel Flythrough in `./source`.

What changed:
- Procedural tunnel built from repeated ring panels and longitudinal neon ribs.
- Emissive cyan, magenta, and amber segments throughout the tube.
- Dolly camera loop through the tunnel.
- Bloom and fog enabled for glow and depth falloff.
- Full-viewport browser canvas styling in `index.html`.

Verification run:
- `npm install`
- `npm run build`
- Started `npm run dev -- --port 4179`, confirmed HTTP 200, then stopped it.

Build command:
```bash
cd source
npm run build
```

Run command:
```bash
cd source
npm run dev
```

Assumptions:
- No assets are used because this prompt is fully procedural.
- The required runtime evidence is provided visually by the animated Aura3D scene and logged route/camera evidence in the browser console.