Implemented the procedural solar system in [source/src/main.ts](/Users/gurbakshchahal/aura3d/benchmark/runs/round-2/codex-aura3d/prompt-03/source/src/main.ts).

It includes one emissive sun with bloom/glow shells, six labeled planets at distinct orbital distances, dotted orbit paths, orbit camera controls framing the full system, and readable planet labels with relative orbit speeds.

Build verified:

```bash
npm run build
```

Run command:

```bash
npm run dev
```

Assumptions: Aura3D’s public primitive API does not expose text meshes, so labels are implemented as a browser HTML overlay. No external assets were used. No dev server is left running.