# 05 Animation Character

## Purpose
Prove public animation track, clip, action, and mixer APIs can drive a visible looped transform value.

## Run Command
Run a TypeScript-aware dev server from the repository root and open `examples/05-animation-character/index.html`.

## Acceptance Target
A looped animated marker moves deterministically and the public mixer snapshot reports an active action.
