# 10 Particles

## Purpose
Prove public CPU particle emitter, modules, system, and renderer-facing batch APIs produce a deterministic visible effect.

## Run Command
Run a TypeScript-aware dev server from the repository root and open `examples/10-particles/index.html`.

## Acceptance Target
A seeded CPU particle fountain is visible and particle stats report live, spawned, killed, and uploaded counts.
