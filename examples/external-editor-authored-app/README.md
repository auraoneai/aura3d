# External-Parity Editor-Authored App Source Fixture

The project and Canvas2D runtime remain here for editor serialization and
external-parity contract tests. The public `index.html` was removed for Aura3D
2.0 because the visible fox was a hand-drawn Canvas2D overlay rather than the
declared imported GLB. Browser tests mount the internal fixture at
`/tests/fixtures/external-editor-authored-app/index.html`.
