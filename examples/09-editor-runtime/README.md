# 09 Editor Runtime

## Purpose
Prove public editor runtime, selection, command history, and transform command APIs can perform an edit workflow.

## Run Command
Run a TypeScript-aware dev server from the repository root and open `examples/09-editor-runtime/index.html`.

## Acceptance Target
A selected cube is moved through command history and undo/redo-capable editor state is visible.
