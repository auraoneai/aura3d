# Foundation Editor-Authored App Source Fixture

The project and Canvas2D runtime remain here for serialized startup benchmark
and editor-export contract consumers. The public `index.html` was removed for
Aura3D 2.0 because personal inspection showed only primitive rectangles; the
declared Fox GLB was not rendered. Browser tests mount the internal fixture at
`/tests/fixtures/foundation-editor-authored-app/index.html`.
