# 03 Shadows

## Purpose
Exercise public scene light data, shadow pass, and renderer-facing geometry in the shadows roadmap slot.

## Run Command
Run a TypeScript-aware dev server from the repository root and open `examples/03-shadows/index.html`.

## Acceptance Target
A cube, directional light indicator, visible contact shadow proxy, and public `ShadowPass` execution evidence are present.
