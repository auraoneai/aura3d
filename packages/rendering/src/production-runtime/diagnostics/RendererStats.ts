export interface RendererStats { readonly drawCalls: number; readonly triangles: number; readonly materials: number; readonly textures: number; readonly frameTimeMs: number; }
export function mergeRendererStats(stats: readonly RendererStats[]): RendererStats { return stats.reduce((total, item) => ({ drawCalls: total.drawCalls + item.drawCalls, triangles: total.triangles + item.triangles, materials: total.materials + item.materials, textures: total.textures + item.textures, frameTimeMs: Math.max(total.frameTimeMs, item.frameTimeMs) }), { drawCalls: 0, triangles: 0, materials: 0, textures: 0, frameTimeMs: 0 }); }
