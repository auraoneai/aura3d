# G3D V6 Architecture Viewer

This starter uses the V6 production renderer workflow for inspection-oriented camera defaults, HDR lighting, and imported GLB proof.

Before running outside this monorepo, serve or copy the files listed in `asset-manifest.json` under `/fixtures/production-runtime`.
