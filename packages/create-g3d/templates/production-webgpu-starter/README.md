# G3D V6 WebGPU Starter

This starter renders through the production WebGL2 path and reports WebGPU adapter/device availability without claiming unsupported parity.

Before running outside this monorepo, serve or copy the files listed in `asset-manifest.json` under `/fixtures/production-runtime`.
