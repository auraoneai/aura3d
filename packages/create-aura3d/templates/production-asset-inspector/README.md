# A3D Asset Inspector

This starter renders a real imported GLB and exposes production metadata, draw-call, texture, and pixel proof through `window.__a3dProductionExample`.

Before running outside this monorepo, serve or copy the files listed in `asset-manifest.json` under `/fixtures/production-runtime`.
