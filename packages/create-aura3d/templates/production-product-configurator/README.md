# A3D V6 Product Configurator

This starter uses `@aura3d/engine/workflows` to render imported GLB assets with the V6 WebGL2 production renderer, HDR IBL, postprocess, and runtime proof metrics.

Before running outside this monorepo, serve or copy the files listed in `asset-manifest.json` under `/fixtures/production-runtime`.
