# A3D Architecture Viewer

This starter uses the Production production renderer workflow for inspection-oriented camera defaults, HDR lighting, and imported GLB proof.

Before running outside this monorepo, serve or copy the files listed in `asset-manifest.json` under `/fixtures/production-runtime`.
