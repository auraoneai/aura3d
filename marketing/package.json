{
  "name": "aura3d-marketing",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite --host 127.0.0.1 --port 7782 --strictPort",
    "build": "pnpm --dir ../apps/aura-clash-showcase build && vite build && node scripts/build-showcase-routes.mjs",
    "preview": "vite preview --host 127.0.0.1 --port 7782 --strictPort"
  },
  "dependencies": {
    "@aura3d/engine": "2.0.0"
  },
  "devDependencies": {
    "typescript": "^5.5.4",
    "vite": "^5.4.10"
  }
}
