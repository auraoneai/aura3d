# V4 Performance

Use renderer diagnostics, resource budgets, render-item sorting, transparent sorting policy, and LOD selection before treating a scene as production-shaped.

Evidence: `tests/browser/v4-large-scene.spec.ts`, `tests/performance/v4-performance-baselines.ts`, and `tools/v4-performance-readiness/index.ts`.

Boundary: the report proves bounded workflow scale and diagnostics; broad performance superiority remains blocked.
