name: V4 External Engine Baselines

on:
  workflow_dispatch:
    inputs:
      engine:
        description: "External engine baseline suite to run."
        required: true
        default: "all"
        type: choice
        options:
          - all
          - unity
          - unreal
      unity_project_path:
        description: "Unity project path on the self-hosted Unity runner."
        required: false
        default: "_external/unity-baseline-project"
        type: string
      unreal_project_path:
        description: "Unreal .uproject path on the self-hosted Unreal runner."
        required: false
        default: ""
        type: string

permissions:
  contents: read

concurrency:
  group: v4-external-engine-baselines-${{ github.ref }}
  cancel-in-progress: false

jobs:
  prepare-kit:
    name: Prepare external baseline kit
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 8

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: pnpm

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Package external baseline kit
        run: pnpm verify:v4-external-engine-baselines

      - name: Upload external baseline kit
        uses: actions/upload-artifact@v4
        with:
          name: v4-external-engine-baseline-kit
          path: |
            fixtures/external-engine-baselines/v4/**
            tests/reports/v4-external-engine-baselines.json
          retention-days: 30

  unity-baselines:
    name: Capture Unity baselines
    needs: prepare-kit
    if: ${{ inputs.engine == 'all' || inputs.engine == 'unity' }}
    runs-on: [self-hosted, unity]
    env:
      G3D_UNITY_EDITOR: ${{ vars.G3D_UNITY_EDITOR || secrets.G3D_UNITY_EDITOR }}
      G3D_UNITY_SEARCH_ROOTS: ${{ vars.G3D_UNITY_SEARCH_ROOTS || secrets.G3D_UNITY_SEARCH_ROOTS }}
      G3D_UNITY_PROJECT_PATH: ${{ inputs.unity_project_path }}
      G3D_RUN_UNITY_UNREAL_CLI_SMOKE: "true"
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 8

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: pnpm

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Regenerate external baseline kit
        run: pnpm verify:v4-external-engine-baselines

      - name: Run Unity editor CLI smoke
        run: node fixtures/external-engine-baselines/v4/run-editor-cli-smoke.mjs unity tests/reports/v4-unity-editor-cli-smoke.json

      - name: Capture Unity visual baselines
        run: node fixtures/external-engine-baselines/v4/unity/run-unity-baseline-captures.mjs --project "${G3D_UNITY_PROJECT_PATH}"

      - name: Audit Unity and Unreal parity gates
        run: pnpm audit:v4-unity-unreal-parity || true

      - name: Audit external evidence readiness
        run: pnpm audit:v4-external-evidence-readiness || true

      - name: Audit completion state
        run: pnpm audit:v4-completion || true

      - name: Upload Unity baseline evidence
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: v4-unity-baseline-evidence
          path: |
            tests/reports/v4-unity-editor-cli-smoke.json
            tests/reports/v4-unity-baseline-render.json
            tests/reports/v4-unity-product-visual-baseline.json
            tests/reports/v4-unity-pbr-visual-baseline.json
            tests/reports/v4-unity-shadow-visual-baseline.json
            tests/reports/v4-unity-hdr-render-target-baseline.json
            tests/reports/v4-unity-postprocess-suite-baseline.json
            tests/reports/v4-product-visual/**
            tests/reports/v4-pbr-visual/**
            tests/reports/v4-shadow-visual/**
            tests/reports/v4-hdr-render-target/**
            tests/reports/v4-postprocess-suite/**
            tests/reports/v4-unity-unreal-parity.json
            tests/reports/v4-external-evidence-readiness.json
            tests/reports/v4-external-evidence-missing-artifacts.md
            tests/reports/v4-completion-audit.json
            tests/reports/v4-completion-audit-runbook.md
          retention-days: 30

  unreal-baselines:
    name: Capture Unreal baselines
    needs: prepare-kit
    if: ${{ inputs.engine == 'all' || inputs.engine == 'unreal' }}
    runs-on: [self-hosted, unreal]
    env:
      G3D_UNREAL_EDITOR: ${{ vars.G3D_UNREAL_EDITOR || secrets.G3D_UNREAL_EDITOR }}
      G3D_UNREAL_SEARCH_ROOTS: ${{ vars.G3D_UNREAL_SEARCH_ROOTS || secrets.G3D_UNREAL_SEARCH_ROOTS }}
      G3D_UNREAL_PROJECT_PATH: ${{ inputs.unreal_project_path }}
      G3D_RUN_UNITY_UNREAL_CLI_SMOKE: "true"
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 8

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: pnpm

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Regenerate external baseline kit
        run: pnpm verify:v4-external-engine-baselines

      - name: Run Unreal editor CLI smoke
        run: node fixtures/external-engine-baselines/v4/run-editor-cli-smoke.mjs unreal tests/reports/v4-unreal-editor-cli-smoke.json

      - name: Capture Unreal visual baselines
        run: node fixtures/external-engine-baselines/v4/unreal/run-unreal-baseline-captures.mjs --project "${G3D_UNREAL_PROJECT_PATH}"

      - name: Audit Unity and Unreal parity gates
        run: pnpm audit:v4-unity-unreal-parity || true

      - name: Audit external evidence readiness
        run: pnpm audit:v4-external-evidence-readiness || true

      - name: Audit completion state
        run: pnpm audit:v4-completion || true

      - name: Upload Unreal baseline evidence
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: v4-unreal-baseline-evidence
          path: |
            tests/reports/v4-unreal-editor-cli-smoke.json
            tests/reports/v4-unreal-baseline-render.json
            tests/reports/v4-unreal-product-visual-baseline.json
            tests/reports/v4-unreal-pbr-visual-baseline.json
            tests/reports/v4-unreal-shadow-visual-baseline.json
            tests/reports/v4-unreal-hdr-render-target-baseline.json
            tests/reports/v4-unreal-postprocess-suite-baseline.json
            tests/reports/v4-product-visual/**
            tests/reports/v4-pbr-visual/**
            tests/reports/v4-shadow-visual/**
            tests/reports/v4-hdr-render-target/**
            tests/reports/v4-postprocess-suite/**
            tests/reports/v4-unity-unreal-parity.json
            tests/reports/v4-external-evidence-readiness.json
            tests/reports/v4-external-evidence-missing-artifacts.md
            tests/reports/v4-completion-audit.json
            tests/reports/v4-completion-audit-runbook.md
          retention-days: 30

  final-audits:
    name: Merge external evidence and audit status
    needs:
      - prepare-kit
      - unity-baselines
      - unreal-baselines
    if: ${{ always() && !cancelled() }}
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup pnpm
        uses: pnpm/action-setup@v4
        with:
          version: 8

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: pnpm

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Regenerate external baseline kit
        run: pnpm verify:v4-external-engine-baselines

      - name: Download external baseline evidence artifacts
        uses: actions/download-artifact@v4
        continue-on-error: true
        with:
          pattern: v4-*-baseline-evidence
          path: _v4-external-baseline-evidence
          merge-multiple: true

      - name: Ingest downloaded evidence
        run: |
          if [ -d _v4-external-baseline-evidence ]; then
            node fixtures/external-engine-baselines/v4/ingest-external-baseline-artifacts.mjs --no-audit _v4-external-baseline-evidence
          fi

      - name: Audit external evidence readiness
        run: pnpm audit:v4-external-evidence-readiness || true

      - name: Audit product visual parity
        run: pnpm audit:v4-product-visual-parity || true

      - name: Audit PBR and glTF readiness
        run: pnpm audit:v4-pbr-gltf-readiness || true

      - name: Audit Unity and Unreal parity
        run: pnpm audit:v4-unity-unreal-parity || true

      - name: Audit production readiness
        run: pnpm audit:v4-production-readiness || true

      - name: Audit broad parity
        run: pnpm audit:v4-broad-parity || true

      - name: Audit completion
        run: pnpm audit:v4-completion || true

      - name: Verify V4 report freshness
        run: pnpm verify:v4-report-freshness || true

      - name: Upload merged external evidence audit reports
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: v4-external-baseline-final-audits
          path: |
            tests/reports/v4-external-engine-baselines.json
            tests/reports/v4-external-evidence-readiness.json
            tests/reports/v4-external-evidence-missing-artifacts.md
            tests/reports/v4-product-visual-parity.json
            tests/reports/v4-pbr-gltf-readiness.json
            tests/reports/v4-unity-unreal-parity.json
            tests/reports/v4-production-readiness.json
            tests/reports/v4-broad-parity-readiness.json
            tests/reports/v4-completion-audit.json
            tests/reports/v4-completion-audit-runbook.md
            tests/reports/v4-report-freshness.json
          retention-days: 30
