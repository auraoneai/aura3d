name: Muse 3.0.1 Aurora 560c98db
on:
  push:
    branches: [ops/muse301-browser-refresh]
permissions: {contents: read}
env:
  SOURCE_COMMIT: 560c98db83f197a22fea5d339074ef17a10e4ebb
jobs:
  browser:
    runs-on: ubuntu-24.04
    timeout-minutes: 55
    steps:
      - uses: actions/checkout@v4
        with: {ref: 560c98db83f197a22fea5d339074ef17a10e4ebb, lfs: true}
      - uses: pnpm/action-setup@v4
        with: {version: 11.1.3}
      - uses: actions/setup-node@v4
        with: {node-version: '22', cache: pnpm}
      - run: test "$(git rev-parse HEAD)" = "$SOURCE_COMMIT" && test -z "$(git status --porcelain --untracked-files=all)"
      - run: pnpm install --frozen-lockfile && pnpm exec playwright install --with-deps chromium
      - run: pnpm build:raw
      - name: Run lane
        shell: bash
        run: |
          set -euo pipefail
          PLAYWRIGHT_JSON_OUTPUT_NAME=tests/reports/aurora-560c98db.json pnpm exec playwright test tests/browser/aurora-lander-campaign.spec.ts tests/browser/aurora-lander-terrain.spec.ts --reporter=line,json
          pnpm --dir apps/showcase-aurora-lander evidence:route-health
      - if: always()
        uses: actions/upload-artifact@v4
        with:
          name: muse301-aurora-560c98db
          path: |
            tests/reports/
            apps/showcase-aurora-lander/route-health.json
          if-no-files-found: warn
