# G3D V6 Asset Inspector

This starter renders a real imported GLB and exposes V6 metadata, draw-call, texture, and pixel proof through `window.__g3dV6Example`.

Before running outside this monorepo, serve or copy the files listed in `asset-manifest.json` under `/fixtures/v6`.
