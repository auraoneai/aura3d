# G3D V4 Interactive Scene Template

This template starts an interactive workflow from the public `@galileo3d/engine` package.

```sh
npm install
npm run dev
```

It uses `createG3DApp`, `workflows.interactiveScene`, `createEnvironment`, and `createDiagnosticsPanel`. It does not import workspace source files or test helpers.
