# A3D ExternalParity Product Viewer Template

This template is the ExternalParity product viewer scaffold. It imports the public package API through `@aura3d/engine/apps`, creates a `createA3DApp` runtime, and renders the `scene-showcase` workflow with production quality settings.

```sh
pnpm install
pnpm build
```

launch positioning: this template proves installable scaffolding and external Vite build behavior. Same-scene low-level renderer code visual parity remains a later ExternalParity milestone.
